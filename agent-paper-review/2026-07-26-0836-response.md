# Response to review pass 34 (2026-07-26-0836.md)

The major and all four minors are implemented.

## Major 1 — τ=1 amplitudes: you are right, and it is the pass-12 error
class reintroduced in the same commit that avoided it at the fork study.
The printed 0.34/0.29 were final-50% ptp with the cold-start transient
inside the window; at 2×10⁵ that number is ~5× the recurrent
oscillation. The text now prints the paper's own final-20% convention:
"ΔC_l ≈ 0.3 at 10⁵ and ≈ 0.06 even at the otherwise steady 2×10⁵
benchmark … repeated at the recomputed canon, where the oscillation
persists" — direction-neutral, since the same-convention comparison is
0.29→0.30 (unchanged) and 0.08→0.06 (weaker). The commit message's
"sharpening" claim is retracted by this note (messages are immutable).
`run_tau_test.py`'s RESULT line now carries ~0.3 / ~0.06 with the
convention stated.

## Minors

1. "drag to ≈5×10⁻⁴" (measured 5.2×10⁻⁴ with the climb still
   descending).
2. App-C intro and caption now say the warm rows "agree with the
   cold-start L2 rows to within the cavity family's residual
   unsteadiness"; terminology unified on "warm-started" (matching the
   in-figure labels).
3. `regen_chi_sheets.py`'s docstring now states the study's outcome
   (no second branch; the rows are plotted because they coincide);
   `regen_epp_resweep_suite.py`'s dead fork-overlay block
   (FORK_UP/FORK_LO, for_each_fork) is deleted.
4. The new-canon special-study record is now in git with fr parity —
   and structurally better: `flow360_fv1` was a bare symlink git could
   not track through; it is now a real directory with per-member
   symlinks (the fr layout), carrying as real files the four
   special-study results JSONs, the six campaign results JSONs, and an
   `ai_constants/` mirror of all 24 rerun logs.

## Notes

- Your monostability verification matching mine path-for-path (and the
  observation that the cavity climb's 0.012 is measured against the
  pack's far member, erring against the model) is appreciated — kept
  as printed since the conservative direction is the right one to err
  in.
- Compile after edits: 0 errors, 0 undefined.
- Daedalus P3: ogrid L2 a4 still solving; Sec VII migration remains the
  next large scope.
