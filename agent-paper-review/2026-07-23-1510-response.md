# Response to review pass 6 (`2026-07-23-1421.md`)
*2026-07-23, ~15:10. NOTE ON STALENESS: pass 6 reviewed commit ed01426 plus a
mid-edit 14:30 working copy — i.e. a moving snapshot taken WHILE the pass-5
fix round was landing. The pass-5 response file it reports missing
(`2026-07-23-1450-response.md`) and the full pass-5 fix commit (f405585,
including the rebuilt PDF) landed minutes after the review's cutoff. Items
the review verified "fixed in the working copy" are all committed in
f405585; the statuses below are relative to the current head.*

## New majors

1. (Secs. III–V refresh landing piecemeal; internal inconsistency) —
   ACKNOWLEDGED AND IN FLIGHT, with two corrections to the review's
   evidence:
   - (c) is wrong in substance: the legacy-NAMED L1 sweep cases
     (`sweep_[str_]Re*_a5`) were re-converged at the committed
     whole-equation canon this morning (`run_sphere_campaign.py
     epp_sweep`, 8/8 converged, JSON committed in ed01426, `ai_constants.log`
     provenance per case). The new suite figure therefore does NOT mix
     kernels across thicknesses; the generator docstring now states the
     provenance explicitly.
   - (d) the flat plate WAS re-run at the committed canon (case dirs
     `flatplate_sphere_Tu*`, converged ~10:57 today); the review looked
     for the old naming. NLF is 22/24 at this writing (the two
     structured-L2 high-α cases converging), and the L0/L2 sweep suite +
     fully-turbulent polar baselines are queued behind it.
   - What IS still true: the printed Secs. III–V numbers, Tables 5–9,
     the polar/benchmark figures, and the caption agreement claims all
     predate the re-run, `eppler_resweep_*.pdf` are placeholders (stated
     in f405585's commit message), and l. 1322's "every coupled-RANS
     computation" is not yet true of the printed values. The full
     refresh (figures, tables, quoted numbers, sweep text to the L0–L2
     suite — including new minor 11) follows once the queue drains;
     failing that before any submission, the dated-canon disclosure
     goes in.

2. (App. B temporal-units attribution vs "no wave-speed convention") —
   ADDRESSED: the App. B opener now credits the conversion to
   Sec. VI + the appendix's own ledger + the one place Sec. II still
   uses it (the deep-separated inviscid-ceiling aside of
   Table `tab:ratelimit`); the Sec. VI sentence now cites the
   frozen-profile ledger of Appendix B rather than Sec. II for the
   ν̃-peak/half-convection-speed measurement.

3. (fig:calibrate H=4.03 stale) — ADDRESSED: caption now 3.98. Verified
   numerically: H(β=−0.1988) = 3.982 grid-converged (native and uniform
   grids to 60k points agree to 4 digits; fig04's own `profile_ints`
   returns 3.982, so the figure's dotted line is drawn at the same
   profile the caption now names). This closes the H half of P1-17.

4. (receptivity-move loose ends) — ADDRESSED, all three: the Daedalus
   N_crit≈13.6 now cites `sec:calib_tu`; the fig:nuhat caption and the
   blend's airfoil-seed sentence both carry "(the seed convention of
   Sec. III)" forward pointers.

## New minors

1. (0.86–0.91 vs Table 3's 0.82 at Re_θ=200) — ADDRESSED: "0.82–0.91
   across the decade".
2. (abstract: Mack anchors the seed, not the rate) — ADDRESSED: "…the
   Blasius e^N envelope; its freestream seed is set by Mack's
   receptivity correlation."
3. (intro "fixes its constants in one pass") — ADDRESSED: "fixes all its
   constants but the freestream seed in one pass".
4. (bare ⟨Ŝg⟩ at l. 566) — ADDRESSED: a(Ŝg)ω.
5. ("parametrisation") — ADDRESSED.
6. (three variants of the ramp-width name) — ADDRESSED: "onset ramp width
   scale" in the nomenclature, at eq:onset, and in the constants list;
   repro comments already swept in f405585.
7. ("The rate is Reynolds-free" at the II.C→II.D seam) — ADDRESSED: "The
   algebraic rate (eq:rate) carries no explicit Reynolds number".
8. (tie back-reaction numbers predate the re-anchor) — STILL OWED, as
   stated in the pass-5 response: the with-tie march will be re-measured
   in the P1-17 number-sweep pass (the quantity is a with/without
   difference, but the favorable-family term is ceiling-sensitive, so it
   does need the re-measurement rather than an insensitivity argument).
9. (β=0.35 rate/onset numbers at the new canon) — CONFIRMED, no change
   needed: re-read from the same converged whole-equation-canon marcher
   that produced the 0.45 boundary (same run, `/tmp/fav_recheck.log`):
   onset 2.74× critical (text: ≈3×), late-secant rate 0.11× Drela
   (text: ≈0.1×).
10. (eq:frozeneig well-posedness in the text) — ADDRESSED: one sentence
    after the equation now gives the BCs (v=0 wall/far field), the
    u^{−1/2} symmetrization ("one symmetric tridiagonal eigenvalue"),
    and the reversed-flow floor with its insensitivity range.
11. (sweep table/footnote re-verification at the L0–L2 suite) — QUEUED
    with major 1's refresh.

## Prior-thread items

Unchanged from the pass-5 response: P1-1/P1-2 (Daedalus predecessor
kernel), P1-7 (evenness argument), P1-17 remainder ("~30%" vs "0.9–1.9×"
plus the number sweep incl. minor 8), P1-18a, P1-18c, P1-m13, P1-m17.
