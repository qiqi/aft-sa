# Review pass 21 of `paper/sa-ai.tex` — deletion audits (tab:nlftrans, Eppler AFT-absence) + chi-sheet regeneration + Daedalus cavity-L2 a4

*Reviewed 2026-07-25, ~07:29, against HEAD 3d94e22. Scope: adb8d02
("Annotated round 5: drop tab:nlftrans"), 70ec237 ("Regenerate all 16
chi sheets"), 6f4d153 ("Drop the AFT-absence discussion and table row"),
3d94e22 ("Daedalus cavity-L2 a4 landed"). The pass-20 fix round
(6e944ee) treated as closed per task; 64f2a43/24b2b83/215e775/962ef34/
1330b5c/faf6c05 touch only RESPONSES.md. Verification: both tex diffs
read line by line and every deleted claim grepped for surviving
dependents; the digitized-XFOIL claim checked against
`data/aft_nlf0416_digitized.json` (curve ranges recomputed); **two chi
sheets rerun** from the committed generator against the shipped
`flow360_fr` data (eppler387 a5 upper, nlf0416 a15 lower) — both
reproduce content-stream-identically (only /CreationDate differs;
overwritten files restored via `git checkout --`) and were rasterized
and inspected; `daedalus_polar_sectional.pdf` likewise rerun and
content-identical; the a5/a6 surface PDFs confirmed CreationDate-only
re-runs; the new canon entry recomputed from
`case_cavity_L2_saai_a4/total_forces_v2.csv` with the canon window
(last 500 pseudo-steps, 51 samples) — exact; `ai_constants.log`
extracted and diffed across the L2 pair; tex compiled in the scratchpad
(0 errors, 0 undefined/multiply-defined references, 89 pages);
doubled-word scan clean. No paper files edited.*

## Summary

Three of the four commits hold up well under audit. The tab:nlftrans
deletion is executed cleanly at the mechanical level — zero orphaned
`\ref{tab:nlftrans}`, no hard-coded "Table 5" anywhere (every literal
Table/Fig number in the tex refers to an external NASA document), and
the pass-20 XFOIL cross-reference that pointed at the table was
rewritten in the same commit to point at the digitized $e^9$ curve of
fig:nlfaft, a claim the committed JSON actually supports (the digitized
XFOIL data spans $\alpha=-8°$ to $16°$; at $9°$ its $c_l=1.501$,
consistent with FlexFoil's quoted 1.51). The pass-20 non-convergence
disclosure survives in the fig:nlfaft caption. The Eppler AFT-absence
deletion is fully clean. The Daedalus a4 landing is numerically exact
everywhere I could recompute it. The pass finds three real problems,
all of the caption/claim-vs-artifact staleness class this review has
flagged twice before: (1) adb8d02's commit message and RESPONSES entry
both state the closing aphorism was "deleted per strikeout" — **it is
still in the tex** (l. 1523–24), an annotated author direction claimed
done but not applied; (2) the appendix sheet-conventions text was not
updated for the regenerated sheets — it still says the solid contours
are "χ=1 and c_v1" (the sheets now carry labeled solid contours at 30
and 10²) and still quotes the Eppler upper frame as 0.0067c with a
1/√Re scaling story (it is now 0.0335c, 5×, deliberately breaking that
scaling); (3) the fig:daepolar caption still says the sectional panel
uses "(structured L2, unstructured L1)" while the regenerated panel
demonstrably uses unstructured L2 at α=4° (generator log confirms).

## Deletion audit — tab:nlftrans and its comparison paragraph (adb8d02)

| Deleted item | Verdict |
|---|---|
| The table's per-case fronts (SA-AI / $e^9$ / Exp, 4 α × 2 surfaces) | **Carried by fig:nlfaft** — all six grids plotted against the measurement circles and both $e^N$ references. The *exact* values (SA-AI 0.57 vs measured 0.55 at 0° lower, etc.) are no longer stated anywhere; the Conclusion's "reproduced to within $0.02\,c$" (l. 2714–15) is now supported visually only (still true against the campaign JSON). Acceptable, noted as minor 1. |
| ∂x_tr/∂N sensitivity column + its XFOIL provenance disclosure ("re-converged at $N_\mathrm{crit}=8$ and $10$ … computed with XFOIL at the original study's conditions", the pass-9 fix) | **Deleted with no residue.** No surviving `\ref`, no surviving use of 0.067c or the "one unit / a third of a unit of N" contextualization (grep clean). The disclosure is no longer owed since the numbers it qualified are gone. Author call — flagged (minor 2) so the disappearance is a decision, not an accident. |
| Pass-20 fix "as does XFOIL in the $N_\mathrm{crit}$ sweep of Table~\ref{tab:nlftrans}" | **Rewired, does not dangle.** Now "as does XFOIL (its digitized $e^9$ curve in Fig.~\ref{fig:nlfaft} spans these incidences)" (l. 1563–64). Verified against the committed JSON: the digitized XFOIL lift curve spans α −8.0° to +15.98° (at 9°: $c_l$ 1.501; at ~15°: 1.774, its own stall), and the transition curve reaches $c_l=1.77$ — XFOIL demonstrably converges at both incidences. The pass-20 attribution discipline is preserved: the $C_l=1.51$ claim stays FlexFoil's. |
| FlexFoil's introduction (independent XFOIL-method implementation, <1% three-way cross-check) | **Moved intact** into the fig:nlfpolar caption (l. 1539–42), its first appearance; fig:nlfcfhigh's "independently written implementation" wording still coheres. |
| Extrapolation daggers (upper 4° beyond recorded $c_l\!\approx\!0.68$; lower 15° beyond $c_{l,\max}\!\approx\!1.68$) + "Somers' Fig.~9d" provenance | **Compressed** into the cf-caption clause "Somers's transition orifices read at the computed section $c_l$ … open where extrapolated beyond the recorded data" (l. 1595–97). The specifics and the Fig.~9d pointer are gone, and the clause carries **no citation** — `somers_1981` is now cited only in the polar caption. Minor 1. |
| Oil-flow corroboration (Somers Fig. 7, $Re=2\times10^6$ forward march) | **Deleted, no dependents.** All surviving "oil-flow" mentions are Eppler/McGhee. Clean. |
| "handover" → "laminar-to-turbulent handover"; closing aphorism | Wording change fine. **The aphorism was NOT deleted** — see major 1. |
| cavL1 15° non-convergence disclosure (pass-20 major 1 restoration) | **Still present** in the fig:nlfaft caption (l. 1439), including "the one non-converged case of the twenty-four". Held. |

**Table-numbering shift**: 12 table envs, 12 labels, all cross-references
symbolic; compile reports 0 undefined/multiply-defined; the only literal
"Table N"/"Fig. N" strings in the tex (l. 1672, 1814, 2136, 1240, 1339,
1534, 2166) all refer to external NASA reports or source-figure numbers.
Safe.

## Deletion audit — Eppler AFT-absence discussion + tab:epplit row (6f4d153)

**Clean.** The deleted sentence ("The AFT family—the model closest in
spirit to SA-AI—offers no comparison at this condition … the table row
records that absence") and the table's AFT row have no surviving
dependents: "closest in spirit", "records that absence", and the
journal-validation-set enumeration appear nowhere else (grep clean).
`coder_maughmer_2014` remains cited at its three other sites (l. 145,
231, 1656), exactly as the commit message claims. The tab:epplit caption
never mentioned AFT. The intro sentence "published results from the
other local-correlation transition models" (l. 1852–53) now matches the
table's remaining rows *better* — AFT is not a local-correlation model,
so the absence row was arguably outside that sentence's scope anyway.
The NLF section's "the paper's one direct comparison with the AFT model"
(l. 1502) remains true and is if anything strengthened; the pass-20
fig:nlfpolar AFT-curves caption fix (provenance + $c_d=0.025$
truncation) is NLF-side, untouched, and still coheres with the Eppler
discussion gone — the reader merely loses the explanation of *why* no
Eppler AFT comparison exists, which the commit message records as the
literature verdict (no published AFT E387 data; the one third-party
point paywalled). Author-directed, no load-bearing loss.

## Verification — chi-sheet regeneration (70ec237)

- Generator diff (`repro/cfd/regen_chi_sheets.py`): `CHI_MAJOR` gains
  $\log_{10}30$ and $2.0$ with labels '30'/'$10^2$'; Eppler
  `L_up` 0.0067→0.0335 (5×, commented with the annotated request);
  `n_probe` 140→320. The Re-sweep sheets inherit the new `L_up` through
  their $\sqrt{2\times10^5/Re}$ scaling, as the commit message says.
- **Spot-regeneration**: `chi_sheet_eppler387_a5_upper.pdf` and
  `chi_sheet_nlf0416_a15_lower.pdf` rerun from the committed script
  against the shipped `flow360_fr` tree — decompressed PDF content
  streams byte-identical to the committed files except the 4-byte
  /CreationDate (committed 23:33, matching the 23:37 commit).
  Overwritten files restored via `git checkout --`; working tree clean.
- **Visual check** (rasterized): Eppler a5 upper frame now runs to
  $dU_\infty/\nu=6700$ ($=0.0335\times2\times10^5$) with the handover
  fan above the bubble shear layer in frame and labeled solid contours
  at $c_{v1}$ and 30; NLF a15 lower frame to $10^4$
  ($=0.0025\times4\times10^6$) with the lower front at
  $x\approx0.63$–$0.67$, consistent with the campaign values. Dashed
  laminar / solid turbulent rendering confirmed (matplotlib dashes the
  negative $\log_{10}\chi$ levels).
- **Captions/conventions NOT updated** where the ranges and levels
  changed — major 2.
- Count nit: the commit message's "all 16 chi sheets" covers 24 PDFs
  (16 α-matrix + 8 inherited Re-sweep). Message-only.

## Verification — Daedalus cavity-L2 a4 (3d94e22)

- **Gate**: `case_cavity_L2_saai_a4/total_forces_v2.csv` has 2001 data
  rows (the `complete()` ≥2001-line gate of
  `regen_daedalus_polar_sectional.py`, same as pass 19's a6-L2 check);
  cavity a5 is partial (1488 rows, running) and a6 has no CSV — both
  correctly excluded everywhere.
- **Totals recomputed** with the canon window (rows with pseudo_step ≥
  last−500, i.e. 51 samples): cavity L2 a4 CL 1.0208 / CD 0.02004,
  ogrid L2 a4 1.0282 / 0.01997 — `canon_results.json` and the new
  tab:daetotals cell are exact.
- **Derived text claims all check**: ΔCL 0.72% → "0.7%" ✓; ΔCD 0.7
  counts / 0.35% ✓; fronts 0.549 vs 0.551 → "0.002c apart" ✓;
  separation 0.481 vs 0.489 → 0.008 ✓; reattachment 0.645 vs 0.661 →
  0.016c ✓ (all within the "0.02c on both edges" claim); "Ten converged
  RANS solutions" enumeration (6 L1 + 3 str L2 + 1 cav L2) ✓; the
  matched-lift 4° deficit (str 12.8 counts; cavity 12.1, quoted only in
  RESPONSES) consistent.
- **Figures**: `daedalus_polar_sectional.pdf` rerun — content-stream
  identical except CreationDate; the generator log confirms the
  sectional panel now uses `case_cavity_L2_saai_a4` at 4° and cavity L1
  at 5°/6°. `daedalus_surface_a4.pdf` rasterized: three rows
  (structured O-grid L2 / unstructured L2 / $e^N$ strips), matching its
  updated caption; the pending clause correctly moved to the a5/a6
  captions. a5/a6 surface PDFs differ from their parents only in
  /CreationDate (pure re-runs, content unchanged — correct, their
  cavity-L2 cases are pending).
- **ai_constants.log diffed** across the L2 pair — see minor 4.

## Compile / typos

Full pdflatex+bibtex build in the scratchpad at HEAD: 0 errors, 0
undefined or multiply-defined references, 0 LaTeX warnings, 89 pages.
Doubled-word scan clean (hits are tabular column specs only). The
reworded passages of all four commits read grammatically; the
"Because a favorable gradient…" paragraph stands alone correctly as
§V's closer.

## New major comments

1. **The closing aphorism the author struck out was not deleted, while
   both the commit message and RESPONSES.md record it as done.**
   adb8d02's message says "closing aphorism dropped" and the RESPONSES
   entry says it was "deleted per strikeout", quoting it verbatim — but
   "The two closures place their empiricism differently; this case
   cannot separate them." is still at l. 1523–24 (one occurrence before
   the commit, one after; no hunk in the diff touches it). An annotated
   author direction was claimed applied and wasn't — the inverse of a
   deletion casualty, and worth a process look since the round's
   response record is now wrong on it.

2. **The appendix sheet-conventions text was not updated for the
   regenerated sheets.** Three statements are now stale against the
   committed PDFs: (a) l. 2813–14 "dashed contours are the laminar
   levels $\chi<1$, solid are $\chi\!=\!1$ and $c_{v1}$" — the sheets
   now carry labeled solid contours at 30 and $10^2$ as well (that was
   the point of the upgrade); (b) l. 2819–22 "the wall-normal range is
   chosen to hold the laminar band in frame … scaled with $1/\sqrt{Re}$
   for the other cases" — the Eppler upper frame was deliberately
   raised 5× *beyond* that scaling to hold the handover above the
   bubble shear layer in frame; (c) Appendix B intro l. 2909–11 still
   quotes "wall-normal frames $0.0067c$ upper … the NLF frames scaled
   by $\sqrt{Re_\mathrm{NLF}/Re}$" — the upper frame is now $0.0335c$.
   Appendix C's $\sqrt{2\times10^5/Re}$ statement remains correct
   (it scales from the new reference). Same staleness class as pass
   19's major 2 and pass 20's major 2.

3. **The fig:daepolar caption's panel-(b) parenthetical is stale
   against the regenerated panel.** l. 2510–11 still reads "Sectional
   lift on the finest completed grids (structured L2, unstructured
   L1)" — the committed, regenerated
   `daedalus_polar_sectional.pdf` uses unstructured **L2** at
   $\alpha=4^\circ$ (generator: "finest completed level for this
   family/incidence"; rerun log confirms `case_cavity_L2_saai_a4`).
   Cheapest durable fix given a5/a6 land within hours: "(structured
   L2; unstructured at its finest completed level, L2 at $4^\circ$)"
   or simply "finest completed grids per family and incidence".

## New minor comments

1. **The Somers orifice provenance lost its citation and its numeric
   anchor.** The cf-caption clause "Somers's transition orifices read
   at the computed section $c_l$" (l. 1595) carries no
   `\cite{somers_1981}` and no longer names Fig.~9d;
   `somers_1981` is now cited only in the polar caption. Relatedly, the
   Conclusion's "tracking the Somers fronts … reproduced to within
   $0.02\,c$" (l. 2714–15) has no in-paper numeric support left — it is
   true (re-checked against the campaign JSON and the orifice data) but
   now rests on reading fig:nlfaft. One `\cite` at l. 1595 (ideally
   with "Fig.~9d") closes both.

2. **The ∂x_tr/∂N sensitivity material is now nowhere in the paper**
   (column, caption provenance, and the "one unit / a third of a unit
   of accumulated amplification" reading of the $e^9$ offsets). No
   orphans — flagged so its disappearance is a decision, not an
   accident; it was the paper's only quantitative statement of how
   sharp the $N_\mathrm{crit}$ lever is on these fronts.

3. **`regen_chi_sheets.py` cannot reproduce the sheets from a fresh
   checkout as shipped**: its default root is
   `/home/qiqi/flexcompute/sa-ai/flow360_tie` (absent from the repo;
   every sibling repro script defaults to the shipped `flow360_fr`,
   which I verified regenerates the sheets exactly), and `main()`
   catches the resulting FileNotFoundError as "SKIP (case incomplete)"
   — a default run silently writes nothing. Also two doc nits in the
   same file: the module docstring still says "solid = chi = 1 and
   c_v1" (pre-dating the new levels), and the new comment "f_v1 90% by
   chi ~ 30" is off — $f_{v1}(30)=0.987$ at $c_{v1}=7.1$ (90% is at
   $\chi\!\approx\!15$).

4. **The celebrated L2 family pair ran on two different solver
   builds.** `ai_constants.log` diff: the cavity a4 run (build stamp
   22:09) logs an extra constant `ai_fv1Bypass: 0.000000` that the
   ogrid a4 log (10:27) lacks; all shared constants are identical.
   With `ai_fv1Bypass=0` presumably inert this is almost certainly
   harmless, but given the repo's consistent-build SOP the
   family-agreement claim deserves a one-line repro note confirming
   the flag's default is a no-op.

5. **Cosmetic**: the table deletion left double blank lines at
   l. 1636–37 and 1643–44 (same tidiness class as pass-20 minor 4;
   harmless to LaTeX).

6. **Log-accuracy nits (no tex impact)**: 3d94e22's RESPONSES entry
   says the 4° L2 lift agreement is "tighter than L1's 0.9%" — L1's
   4° CL gap is 0.65% (the 0.9% is L1's *drag*); L2 is tighter in drag
   (0.35% vs 0.92%), marginally looser in lift (0.72% vs 0.65%). The
   tex itself makes no such claim. And 70ec237's "16 sheets" is 24
   PDFs (minor count, noted above).

## Still open

- **P1-18c** (320×80 flat-plate single grid) — GPU-gated, untouched.
- **Invariant-kernel 2D verification** (pass-19 response; b0e3bad adds
  the cavity s_hat-direction case) — still GPU-gated behind the
  cavity-L2 campaign; not yet reviewable.
- **Pass-19 minor 1 residue** — with-floor march option on
  `floor_backreaction_table.py` remains a stated nice-to-have.
- **Daedalus cavity L2 a5/a6 pending** (ETAs ~09:00/14:30 UTC today) —
  when they land: the two "will be added when that run completes"
  captions, the "Ten converged" count, the pending-set sentence, the
  tab:daetotals dashes, and (per the generator header) a rerun of
  `avl_fixed_cl_reference.py` if a structured target moves; major 3's
  caption is also cheapest to fix once, then.
