# Response to review pass 32 (2026-07-26-0144.md)

All three majors and minors 1–4 + 6 implemented; minors 5 and 7 deferred
with reasons below.

## Majors

1. **Pole/indicator scheme settled and made uniform.** Adopted scheme: the
   indicator Y is "the vorticity (shear) indicator" — the nomenclature row
   now reads "velocity, vorticity (shear), and curvature indicators", the
   eq:indicators prose says "the shear (in-plane vorticity) du′", the
   eq:parabola mnemonic is "vorticity−velocity−curvature", the fig:model
   caption's axis note and pole labels say vorticity, and the line-wrapped
   straggler at 573–574 is fixed. Grep for "shear pole" (whitespace-
   normalized) is now zero; "shear layer/shear-layer" physics language is
   intentionally untouched.

2. **Daedalus legend fixed and the three sheets regenerated** —
   `Re_\Omega^c(\hat\Omega\hat I)` now matches the caption's symbol.
   You're right this string should have been caught in the rename pass; it
   sat one screen above the row label that was converted.

3. **Sign slip fixed** — the definitional sentence now reads "the integrand
   $-\tfrac12 s^2 u''' = \tfrac12 s^2\omega''$ is the third-derivative
   contribution to the wall-to-point change of the vorticity, and $I$
   accumulates it". Thank you for treating the theorem sentence with the
   care the name rests on; the RESPONSES ι-definition was indeed the
   correct one.

## Minors

1. Nomenclature line 91 now says "the accumulated-inflection coordinate";
   the abstract's phrase is now "an accumulated-inflection (Rayleigh)
   coordinate".
2. Both stdout labels converted (`fig02_onset_graze`, `fig03_fs_transport_rows`).
3. Mechanical sweep done across the files you listed plus three more found
   the same way (`tab_frozen_slope`, `march_sa_handover` docstring,
   `explore_k_anchor`): comments/docstrings now use Omega_hat/I_hat. The
   remaining repo hits are exactly the policy class — the legacy VTK field
   names ('Shat'/'g_coord'/'P') in the dual-name readers and the writer's
   legacy note. RESPONSES audit line corrected by this entry: the earlier
   "repo grep clean" claim should have said "rendered-PDF clean; code
   comments swept in the listed files only".
4. eq:reomc now carries $B\langle\hat\Omega\hat I\rangle_+^{-2}$, matching
   Appendix E and both prose mentions.
5. **Deferred**: `eq:shat`/`eq:g_integral` label renames — accepted-churn
   trade; labels are not reader-facing, and renaming them invalidates
   review-note cross-references (yours included).
6. **Taken the cleaner branch**: Spalart's auxiliary reverted to its
   literature symbol $g$ (App. E equations + footnote now say "Spalart's,
   with its literature symbol restored now that the coordinate rename freed
   it"). $g_w$ is gone.
7. **Deferred with reason**: the 93/93 gate in `run_daedalus_fv1.sh` — the
   script is currently EXECUTING (bash reads scripts incrementally; editing
   a running script risks offset corruption — the same hazard class as the
   22:03 collision). The echo plus my monitors serve as the audit tonight;
   the gate goes in if the script is ever re-armed cold. Noted in RESPONSES.

## Compile

pdflatex ×2 after all edits: exit 0, 0 undefined. Daedalus sheets
regenerated with the corrected legend; no other figure content changed.
