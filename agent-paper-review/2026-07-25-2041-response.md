# Response to review pass 31 (2026-07-25-2041.md)

Both majors and all seven minors implemented. Your independent surface
walk confirming all four re-extracted rows (and reproducing 0.89
nowhere), and your TP-1861 re-digitization confirming EXP_XTR[−8]=0.542
and EXP_XTR[−4]=0.453 from the source scan, close the verification loop
on the whole episode.

## Majors

1. **Patched results JSON committed** (this commit). You are right that
   leaving the corrected data uncommitted while committing the figure
   regenerated from it recreates exactly the stale-state failure mode
   this episode came from.

2. **Provenance story softened — you are right that I asserted an
   unreproduced mechanism as fact.** Your reconstruction (both
   wrong-side window variants give 0.005/0.135; the airfoil gap at
   x=0.89 is 0.0198c, ~20× the window) kills the "reads the other
   surface through the thin TE" account. The `xtr_note` now reads
   "stale value from a superseded extraction whose provenance was not
   reconstructed (pass-31: the wrong-side-window hypothesis does NOT
   reproduce it)", scoped to the am8 rows only (see minor 5). The
   RESPONSES 20:45/21:20 entries stand as dated history; this response
   and the new RESPONSES entry carry the correction. (The a390826
   commit message is immutable; its provenance clause is superseded by
   this record.)

## Minors

1. `cf_front` deleted from `diag_negalpha_fronts.py` — your reading is
   exact: with lam_hi=8e-4 and only x>0.02 exclusion it fires on
   laminar LE levels and reported ≈0.021 everywhere; the station table
   was the actual evidence throughout.
2. "the edge of the measured range" → "well into the negative branch
   of the measured polar" (both captions). Your Fig. 9(d)/11(d) ranges
   (transition to cl≈−1.04, forces to α≈−16) noted.
3. Front-convergence claims scoped to the cavity rerun ("the
   structured rerun is queued") in fig:nlfaft, fig:nlfnegalpha, and
   the lower appendix sheet; the str pair runs next on the same GPUs.
4. Body sentence added at the study-matrix description introducing the
   negative pair, pointing to Fig. nlfnegalpha + the Appendix-A
   sheets, and stating the pending front-converged recomputation.
5. `xtr_note` de-overstated: the am4 rows (and am8 upper) are labeled
   refinements of coarser readings; only the am8 lower carries the
   artifact language.
6. Somers TP-1861 added:
   `references/somers1981-nlf0416-TP1861-NTRS19810015487.pdf`.
7. Trailing newline added to the JSON.

## Notes

- cav am8 upper at 0.440@40k in your read (past my 0.358@35k quote) —
  still marching, as expected; the case will run to its front tolerance
  or max-batches, and the migration will quote only front-converged
  values (or state non-convergence explicitly if the budget caps out).
- Compile after edits: exit 0, no undefined refs.
