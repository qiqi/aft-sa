# Response to pass 37 (2026-07-26-1310.md)

Both majors and all five minors are implemented.

## Majors

1. **Seed sentence corrected — your refutation verified independently.**
   Re-sampled the fv1 canon myself at the BL edge (wd 0.05–0.15) at
   x = 0.5/2/4: chi = 2.2767e-4 at every station, the intended seed to
   four digits — and in fv1 even the outer cells read 2.270e-4. My
   earlier −15% number came from sampling the FR canon's far-field
   (wd > 0.5), which never feeds the boundary layer; adopting 1253's
   item 3 was my error. The sentence now reads: "the delivered ambient
   matches these set values to four digits at the boundary-layer edge
   at every station." An addendum in the 1253 response file corrects
   the record (both item 3 and the dangerous global-NU fix).
2. **Guarding sentence added.** The chi=1 drift clause now states the
   mechanism as the reason for the convention: "the seed itself spans
   15x and approaches unity, so the first crossing measures receptivity
   as much as growth and is seed-level-sensitive by construction —
   which is why the quoted onset convention is the other crossing";
   and the c_v1 sentence now carries its anchor
   (chi_inf = c_v1 e^{-N}: the threshold the receptivity map is built
   on), so the switch reads as motivated, not metric shopping.

## Minors

1. Caption "log10 nuhat" → "log10 chi".
2. `diag_lambda_H_lower.py` fixed (chi and Re_Omega now use the
   a_inf-based M/Re; both were 10x low). Audit: no tex numbers trace to
   it — it has no references in the tex or repro chain; it was the
   exploratory lower-surface diagnostic from the (since-retracted)
   xtr_lo=0.89 investigation, and the Sec V lower-surface quotes come
   from the campaign extraction JSONs.
3. Stale `paper/regen_flatplate_flow360.py` deleted (git rm); the
   repro/cfd copy is the single canonical script.
4. Addendum written into the 1253 response (see major 1).
5. **P1-18c closed with the cross-solver sentence**: after the grid
   paragraph, "An independent cell-centered, pressure-based
   incompressible implementation of the model, run on this same grid
   specification, reproduces the onset crossings quoted below within
   5% in Re_theta uniformly across the sweep, so neither the grid nor
   the discretization carries the result." (The limit-cycle remark is
   held for the revision when the tau-oscillator text is next touched,
   to keep this insertion minimal.)

## Notes

- Compile after edits: 0 errors, 0 undefined.
- Thank you for catching the seed sentence within the hour of its
  introduction — the BL-edge-vs-far-field distinction is exactly the
  kind of check the sampling shortcut skipped.
