# Review pass 14 of `paper/sa-ai.tex` — light verification of the pass-13 fix round (8ba3932)

*Reviewed 2026-07-24, ~18:09. Scope: commit 8ba3932 (+ 1a35f04
response record) against the pass-13 review (2026-07-24-1401) and its
response. Working copy clean for all paper files; `sa-ai.pdf`/`.log`
(18:04) current with HEAD, 0 errors, 0 undefined references.
Verification: `paper/.gitignore` read and every `\includegraphics`
target in the tex checked against `git ls-files` (all tracked); the
tex delta 9535a2b..HEAD diffed in full (5+/4−, exactly the three
claimed edits); `fork_extension_results.json` read entry-by-entry;
the cold cavL2 10⁵ median and all ten CFD cells of `tab:eppresweep`
recomputed from the case CSVs; the six warm-path settle times
recomputed under the stated rolling-2000-iteration criterion; the four
touched repro scripts read; the 0930-response amendment and ladder
docstring diffs inspected. No paper files edited.*

## Summary

**The fix round is verified in full — all ten pass-13 items land as
claimed, and the root-cause fix is real.** The blanket `figs/` ignore
that produced three consecutive majors is gone; all eight Re-sweep chi
sheets are in git at 8ba3932, and I confirmed every figure the tex
includes is now tracked, so a clean checkout builds. The three tex
edits are exactly the response's three (0.860 cell, criterion-stated
convergence band, "collapses onto the burst branch"), and each checks
against the data. The results JSON carries all six extension entries
with the corrected slope scale, the runners read-merge-write, the
sweep-table script's warm row is gone with its output matching
`tab:eppresweep` cell-for-cell, and the tau test constructs both Re
cases. Nothing new is broken at the edit sites. One small residue: the
results JSONs the fork/ladder/tau tables rest on still live only in
the gitignored `flow360_fr/` (below). Substantial *unreviewed* work
is queued for the next pass: the gate v1–v5 rig campaign, the SA
low-Re sustainment findings, and the untracked lsb-zoom/fv1-bypass
exploration.

## Verification of claimed fixes

✓ = verified against the repo/committed data.

- ✓ **Major 1 (figs/ gitignore, root fix).** `paper/.gitignore` now
  ignores only `figs_archive_*/` and `figs_pre_recalib/`, with a
  comment recording the three-pass failure history (P11-M2, P12-M7,
  P13-M1). All eight `chi_sheet_eppler387_Re{60k,100k,300k,460k}_
  {upper,lower}.pdf` are in git at 8ba3932 (Bin adds in the commit;
  present in `git ls-files`). The three previously force-added sweep
  figures (`eppler_resweep_{forces,lowRe,highRe}.pdf`) remain tracked.
  Sweep of **every** `\includegraphics` target in the tex (with
  `\graphicspath{{figs/}}`) against the index: all tracked. And with
  no `figs/` pattern left in any applicable ignore file, a stray new
  file in `paper/figs/` appears as untracked in `git status` — the
  loud-failure property holds (the four current `??` entries under
  `paper/repro/` demonstrate the repo isn't suppressing new paths).
- ✓ **Minor 1 (JSON clobber).** `run_fork_extensions.py` re-loads and
  merges `fork_extension_results.json` before each dump (comment
  "read-merge-write: concurrent family runners share this file");
  `run_dn_extensions.py` likewise. The JSON (17:58) has all six
  entries; spot-checks match the response and pass 13's recomputation:
  ext_fork_cavL2 CL 0.8598 / CD 0.03405 (drift 8.8e-5) ✓,
  ext_up_cavL2 0.8592 / 0.03412 (drift 7.7e-5) ✓.
- ✓ **Minor 2 (slope ×10).** `history_diag` now uses `a*100` with the
  comment stating the row cadence; the stored slopes are 10× smaller
  than the pass-13 values (e.g. ext_up_strL2 −3.67e-5/1000 it ✓
  response "−3.7e-5"; ext_fork_strL2 1.6e-4 = pass 13's per-10⁴
  measurement relabeled correctly).
- ✓ **Minor 3 (0.860 cell).** `tab:eppfork` cold-unstructured cell is
  0.860, matching `tab:eppresweep`. Recomputed from
  `sweep_cavL2_Re100k_a5`: final-20% median CL = 0.85952 → 0.860 —
  the rounding is right, not merely consistent.
- ✓ **Minor 4 (convergence band).** Tex now reads "3–4.5×10⁴
  iterations (rolling-2000-iteration range below 10⁻³)". Recomputed on
  the concatenated fork/ladder+extension histories: settle at 2.9/2.9
  ×10⁴ (str fork/up) and 3.9/4.3×10⁴ (cav fork/up) — the quoted band
  is a fair round of the paths the sentence describes (the dn paths,
  not part of the "enormous transients" narrative, settle slightly
  earlier, 2.4/3.9×10⁴; the band's lower edge is conservative).
- ✓ **Minor 5 (caption wording).** "every warm start collapses onto
  the burst branch within 20,000 iterations" — the softening option
  pass 13 offered, correctly leaving branch identity to the table.
- ✓ **Minor 6 (warm row).** The `LEVEL=='L2'` warm-row block in
  `regen_resweep_table.py` is replaced by a comment recording why.
  Recomputed all ten CFD cells (str/cav L2 × 5 Re, cl/cd and the
  quarter-chord cm transfer): identical to `tab:eppresweep`
  cell-for-cell, including the cm roundings (−0.0975→−0.098 etc.).
  (e⁹/Exp columns unchanged since pass 13's full re-run; not re-run.)
- ✓ **Minor 7 (tau repro).** `run_tau_test.py` iterates a two-entry
  JOBS list constructing both `tautest_strL2_Re100k_a5_tau1` and
  `..._Re200k_...`; the docstring records the falsification outcome
  ("relaxation oscillators … the bottleneck is the amplitude ladder,
  not the ramp") with both ΔCL numbers.
- ✓ **Minor 8 (record amendment).** The 0930 response's major-3 entry
  carries the bracketed amendment exactly as described.
- ✓ **Minor 9 (ladder docstring).** `run_continuation_ladders.py`
  up-ladder paragraph now past-tense with "RESULT: monostable …
  initialization-independence evidence, not a bistable band".
- ✓ **Minor 10** — no action needed, none taken.
- ✓ **Edit-site typo scan**: the three tex hunks are clean (math mode
  consistent, no doubled words); the commit message follows the
  succinct convention.

## New comments

1. (minor, repro provenance) **The results JSONs the paper's tables
   rest on are still only on disk.** `flow360_fr/` is gitignored
   (root `.gitignore:98` `/flow360_*/`), so
   `fork_extension_results.json`, `fork_bistability_results.json`,
   `ladder_{str,cav}_results.json`, and all the case
   `total_forces_v2.csv` histories are untracked — while the four
   `sphere_campaign_*_results.json` in the same directory *were*
   force-added and are tracked. Pass 13's minor 1 asked only for the
   re-emit + read-merge-write (done), but its premise "the CSVs are
   committed" is not true of this repo: nothing under `flow360_fr/`
   except the four sphere-campaign JSONs is in git. For the same
   loud-failure logic as major 1, force-add the small results JSONs
   (and `ai_constants.log` files) that `tab:eppfork`/`tab:eppresweep`
   and the τ=1 numbers rest on; the multi-GB case dirs can stay
   ignored.

## Still open / upcoming

- **P1-1/P1-2 (Daedalus final-kernel recomputation)** — in flight:
  both L1 sets done; ogrid L2 α=4° (14:11) and α=5° (18:06) complete,
  α=6° pending; cavity L2 not started (case dirs exist, no forces).
- **P1-17 tie back-reaction re-march** — open (queued per the 0930
  response); untouched this round.
- **P1-18c (single 320×80 flat-plate grid)** — open, GPU-gated.
- **Upcoming (not reviewed, flag only)**: untracked exploration work —
  `paper/repro/cfd/lsb_zoom_sheets.py` (whole-bubble signed-u_t/χ
  zoom sheets + reverse-layer momentum budget),
  `paper/repro/cfd/run_fv1bypass_test.py` (AI_FV1BYPASS battery), and
  `paper/repro/analytic/figs_explore/lsb_zoom_Re{60,100}k.png` — plus
  the large committed-but-thread-only campaign since 9535a2b (gate
  v1–v5 march rig, c_b2 restoration, SA low-Re sustainment
  χ_eq≈0.09 Re_τ and the 60k half-strength-turbulence floor, rig #2
  prior-art search). Several of those addenda have direct paper
  implications (the Δx(1→30) metric re-anchoring at low Re; the 60k
  floor statement) — take them when they reach the tex.
