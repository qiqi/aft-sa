# Response to review pass 30 (2026-07-25-1900.md)

Both majors and all seven minors are implemented.

## Majors

1. **m_chart mis-highlight — fixed, both figures regenerated.** You
   diagnosed it exactly: the style matcher's `'algebraic' in
   fam.lower()` caught m_chart's free-text note ("bypass-like/algebraic
   behavior"). The condition is now `'B-C' in fam` in both scripts, so
   only the genuine Bas–Cakmakcioglu submittal (l, the Case-3 palette
   green) draws sea-green; m_chart falls back to light gray with the
   other unknowns. Verified in the regenerated PDFs: one sea-green
   polyline per panel, and the extreme near-LE / high-drag outlier is
   now anonymous gray, as its unknown attribution warrants. This was
   the most consequential kind of digitization error — a correct JSON
   rendered into a false accusation — thank you.

2. **Uniqueness claim scoped.** "The one published SA-based
   \emph{transported-intermittency} ($\gamma$-equation) result on this
   benchmark…" — your word-class fix. SA-BC on fig:epppolar no longer
   contradicts the sentence.

## Minors

1. Both captions now carry the honest count: fig:nlfaft says
   "(fourteen submittals, nineteen curves: two participants supplied
   second model variants and three line-only series carry no letter
   code)"; fig:nlfpolar drops the bare "fourteen".
2. Stale "royalblue" comment → mediumpurple in `regen_nlf_polar.py`.
3. Greens disambiguated in the caption: "Dark-green crosses" for the
   Denison Langtry–Menter curve, "thin sea-green" for the B-C
   submittal.
4. `figs/nlf_workshop_envelope.pdf` git-rm'd (no longer includable or
   regenerable; `workshop_nlf_envelope.json` stays — the PZ curve
   reads from it).
5. "≈0.0153 (structured L2) interpolated at the same lift" — family
   pinned; your cavity-L2 0.0146 read is what a recomputing reader
   would have found.
6. anushka_2019 authors re-braced as {Anushka R.} and
   {Satish Wadel S.} (literal given-name-first blocks; renders
   unabbreviated but correctly attributed).
7. Legend en-dashes: "Piotrowski–Zingg" and "B–C" now use the literal
   en-dash character (matplotlib does not collapse TeX double
   hyphens).

## Campaign note

Your "GPUs idle at review time" observation prompted a health check:
the orchestrator (PID 698770) is alive and the final two structured-L2
NLF batches (a9/a15) are on GPUs 3/5 at 97% — the idle GPUs are the
six the nlf set no longer needs. 27/93 stamped, consistent with your
count. (Your pgrep would also have found its own shell — that pattern
produced a false duplicate-orchestrator scare earlier today too.)

Compile after all edits: 87 pages, 0 errors, 0 undefined, 0 caption
overflows (pixel audit).
