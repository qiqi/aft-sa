# Pass 31 — the −8° extraction-artifact re-diagnosis (3466eaa)

**Scope note.** The brief said HEAD = 3466eaa with a clean tree. Actual HEAD is
**a390826** ("RETRACTION: alpha=-8 upper front is an unconverged aft-march, not a
model defect", 20:45 UTC), which supersedes half of the scoped commit, and
`flow360_fr/sphere_campaign_nlf_results.json` is **modified but uncommitted**. This
review covers 3466eaa and the retraction on top of it, since the two are one story.

## Summary

The pass-24 anomaly is now resolved in two steps, both of which I verified
independently against the case fields. (1) The α=−8° lower front at 0.89c **was an
extraction artifact**: my own from-scratch surface walk (camber-split geometric
surfaces, signed Cf from the surface VTU, χ from volume near-wall points assigned by
nearest-contour KD-tree — no rectangular probe windows) shows the lower (suction,
−Cp≈4.7) surface turbulent from the leading edge (Cf≈1.2×10⁻² at x=0.02; χ>1 from
x≈0.0–0.01), reproducing the patched values on all four rows and reproducing 0.89
nowhere. (2) The 3466eaa headline "upper early-trip is real" was itself **wrong and
was retracted 14 minutes later** in a390826: the fv1 front-convergence rerun (which I
read directly from `xtr_history.csv`) shows the upper front marching aft
0.082→0.440c between 15k and 40k steps with no sign of settling, while the lower
locks at 0.0104 immediately — the 0.13–0.19c markers are mid-march snapshots of a
fixed-budget cold start. The pass-24 response's diagnoses (lower = "Sec. epphandover
mechanism at its extreme", upper = stagnation-inflation) are therefore **both dead**,
and the RESPONSES chain says so explicitly and candidly. The captions at HEAD are
coherent with all of this and the paper compiles clean. The one genuine process gap:
the corrected results JSON that Fig. 6 was regenerated from is still sitting
uncommitted in the working tree.

## Verification

- **Independent extraction walk (artifact half).** Own script (scratchpad
  `indep_extract.py`), methodology deliberately different from `extract_xtr`:
  geometric upper/lower split via x-binned camber midline; suction side identified by
  Cp minimum; χ front from `volume.pvtu` points with wd<10⁻³ assigned to a surface by
  nearest contour point. Results vs patched JSON:
  | case | mine up/lo | JSON up/lo |
  |---|---|---|
  | str am8 | 0.135 / 0.000 | 0.1302 / 0.0025 |
  | cav am8 | 0.195 / 0.010 | 0.1945 / 0.0103 |
  | str am4 | 0.425 / 0.075 | 0.4247 / 0.0729 |
  | cav am4 | 0.455 / 0.120 | 0.4523 / 0.1215 |
  All four rows confirmed to grid resolution (Δx=0.005). Lower-surface Cf at −8°:
  1.18×10⁻²@0.02c, 9.0×10⁻³@0.05c (str) — turbulent from the LE; no physical walk
  produces 0.89. The current extractor is right; the old stored value corresponds to
  nothing in the fields. The pass-24 "suction side held laminar to 0.89c by the
  handover mechanism at its extreme" diagnosis was wrong — it explained a number that
  never existed in the solution.
- **The artifact's claimed mechanism is NOT reproduced** (see major 2). I rebuilt
  both plausible wrong-side probe variants (lower-surface z with upper-style window;
  upper-surface z with lower-style window): they give fronts of 0.005 and 0.135, not
  0.89. Geometry kills the stated story: the airfoil gap at x=0.89 is 0.0198c, ~20×
  the ±0.001+wd_max window, so a wrong-side window cannot "read the other surface
  through the thin TE" at 0.89 with the current window sizes. The artifact is
  confirmed; its forensic explanation is speculation written down as fact.
- **Retraction half (upper front).** `flow360_fv1/cavL2.../am8/xtr_history.csv`:
  xtr_upper 0.0→0.082@15k→0.137→0.201→0.280→0.358→0.440@40k (≈0.08c/5k steps, still
  marching, now past the 0.358@35k quoted in RESPONSES); xtr_lower locked at
  0.0104 from 15k. am4 cav: converged 0.45288/0.12204 (last two batches identical to
  6 decimals) — on the references. The old-canon negatives ran
  `run_negalpha_nlf.py` = fixed-budget cold start with no front check (confirmed by
  reading the script) — the retraction's account is correct and my read of the
  histories independently supports it.
- **e⁹/mfoil claims.** `flow360_fr/mfoil_nlf0416_Re4M.pkl` extended to −4/−8:
  −8 conv=True, cl=−0.47624 (CFD str −0.47632 — "matches to 3 decimals" ✓),
  xtr_upper 0.6621 ("0.662" ✓), xtr_lower 0.0151 ("0.015" ✓); −4 conv=False → the
  FlexFoil fill at −4 is justified. `flow360_ai/flexfoil_nlf0416_Re4M.pkl` N=9
  crossings: −8 lower 0.0177 ("0.018" ✓), −8 upper 0.6618 ("0.663" ≈ ✓), −4 upper
  0.4920 (~caption "references" ✓).
- **Literature re-verification with independent digitization** (standing
  instruction). Somers TP-1861 is **not in the repo**; I fetched the NTRS scan
  (19810015487, 104 pp) and digitized Fig. 9(d) (R=4.0×10⁶ transition, p. 51 of the
  report) myself: gridline-periodicity calibration (small square = 0.01c, x=0 at
  px 390, 90.44 px/0.1c; OCR label fit for the cl axis), blob detection with
  open/filled classification. Upper-surface open-symbol curve at the computed lift
  cl≈−0.46..−0.48: x_T = 0.536–0.544 → **EXP_XTR[−8] upper = 0.542 confirmed**
  (filled symbols ≈0.57; the caption's "≈0.54" is the onset reading). Near zero
  lift (computed cl +0.018..+0.036): open/filled band 0.40–0.50 with the fitted
  curve through ≈0.45 → **EXP_XTR[−4] upper = 0.453 confirmed** (±0.01). Lower
  surface: no square symbols below cl≈0.45 anywhere in Fig. 9(d) → `'lower': None`
  at both negative incidences is correct. Cross-check: the independently digitized
  workshop LTPT points (`workshop_nlf_submittals.json`, upper α=−3.12→0.445) extrapolate
  to 0.462@−4°, 0.537@−8° — consistent. Fig. 11(d) polar: cd≈0.0105 at cl −0.46 →
  "within seven counts" (CFD 0.01110/0.01125) holds.
- **Figure regeneration verified.** Rendered `nlf_aft_transition.pdf` at
  3466eaa⁻¹ vs current and pixel-diffed: changes are exactly the four negative-row
  marker moves (the −8 lower markers leave x≈0.89 on the right panel edge and appear
  at the LE; the −4 lower and all upper markers shift per the re-extraction). No
  other content changed. Working-tree figures untouched by this review (nothing to
  `git checkout --`).
- **Consistency sweep.** `grep` over the tex: the old "holds the suction surface
  laminar to 0.89c" and "boundary of applicability" text is gone; no stale
  0.89/0.14–0.22 references anywhere; fig:nlfaft caption (lower = on the e⁹ LE trip;
  upper = *not front-converged*, marching toward 0.54–0.66, "front-converged values
  will replace these markers"), fig:nlfnegalpha caption (upper rows read the
  mid-march state), and the two new Appendix-A sheet captions all carry the same
  account. The workshop-underlay part of the caption (fourteen submittals, α=−4°–8°
  sweep, cavity-L1 15° non-convergence note) is undisturbed. The upper half keeps a
  disclosure — reclassified from "real model miss" to "unconverged transient,
  markers to be replaced", which the evidence supports.
- **Honesty of the record.** RESPONSES 20:45 states outright that "the old 'holds
  the suction surface laminar to 0.89c' claim was built on the artifact"; the 21:20
  entry retracts the interim rectified-noise mechanism, credits the user's
  hypothesis, voids the amplifying-zone-gate "second use case", and records the
  protocol lesson (never quote a front from a fixed-budget cold start).
  `1123-response.md` is left unedited as a dated historical document with the
  correction living in RESPONSES.md — acceptable practice; the chain is traceable
  and nothing is silently rewritten. The caption no longer asserts the lower-surface
  miss, which is correct because there never was one.
- **Compile.** `pdflatex` exit 0; all three new PDFs
  (`nlf_cf_negalpha.pdf`, `chi_sheet_nlf0416_negalpha_{upper,lower}.pdf`) included;
  no undefined references; no overfull boxes.

## New major comments

1. **Commit the patched results JSON.** `flow360_fr/sphere_campaign_nlf_results.json`
   carries the corrected xtr values (and the xtr_note) only in the working tree;
   3466eaa committed the regenerated `nlf_aft_transition.pdf` but not the data it was
   regenerated from. At any committed revision, re-running the figure resurrects
   0.89. This is exactly the stale-state failure mode the artifact came from.
2. **The artifact's provenance story is asserted as fact but is unreproducible.**
   The commit message, the RESPONSES 20:45 entry, and the `xtr_note` in the JSON all
   state the 0.89 came from "a wrong-side probe window that saw no chi signal until
   the airfoil is thin enough (x~0.89) to read the OTHER surface's BL through the
   TE". I could not reproduce this with either wrong-side window variant (they give
   0.005/0.135), and the geometry contradicts it: the airfoil gap at x=0.89 is
   0.0198c, ~20× the probe window, and gap<0.002 is only reached at x>0.99. The
   original extraction code that produced 0.89 is not in the repo (`run_negalpha_nlf.py`
   writes no xtr), so the mechanism cannot be checked. Either reproduce it or soften
   the note to "stale value from a superseded extraction; provenance not
   reconstructed" — especially in the `xtr_note`, which is the permanent record.

## New minor comments

1. `diag_negalpha_fronts.py`'s `cf_front` detector is broken: with `lam_hi=8e-4`
   and no LE exclusion beyond x>0.02, laminar LE Cf already exceeds the threshold,
   so it reports ≈0.021 for every case and side (its own output shows this). The
   docstring's "1.5× the local laminar envelope" is not what the code does. The
   RESPONSES claim "(Cf jump confirms)" is actually carried by the station table,
   not this detector. Fix or delete the column.
2. "−8° (the edge of the measured range)" in the fig:nlfaft caption: Somers
   Fig. 9(d) has upper-surface transition measured to cl≈−1.04 (α≈−13) and Fig. 11(d)
   has forces to α≈−16 (negative stall cl≈−1.15). −8° is the edge of the *workshop*
   sweep (−4°–8° is even narrower), not of the measurement. Reword (e.g., "well into
   the negative branch of the measured polar").
3. The str-family fv1 negatives have no front-convergence history yet
   (`strL2.../am8/xtr_history.csv` empty, no am4 str) — the "these two cases"
   transient claim and the appendix lower-sheet's "both front-converged" generalize
   from the cavity rerun plus analogy. True for the lower/−4 values (old-canon ≈
   fv1-converged cav), but the str half should be stated as pending or the rerun
   completed before migration.
4. The negative-incidence story lives entirely in captions: fig:nlfnegalpha and the
   two appendix sheets are never referenced from body text (only from the fig:nlfaft
   caption), and Sec. nlfval's narrative and the conclusions still describe the
   study as α∈{0,4,9,15}×24 cases. One body sentence introducing the negative pair
   (and its pending front-converged recomputation) would anchor the three figures.
5. `xtr_note` says "re-extracted ... previous values were a stale artifact" for all
   four rows, but only the am8 lower 0.89 was an artifact; the other six old values
   were coarse (0.46/0.5/0.14/0.22-style) readings that the re-extraction merely
   refined. The note as written overstates.
6. Somers TP-1861 is cited (Fig. 9d, Fig. 11d) but the PDF is not in `references/`
   — every other digitized source is. Add it (NTRS 19810015487) so the next
   verification pass doesn't re-fetch.
7. JSON still ends without a trailing newline (`\ No newline at end of file`).

## Still open

- Root flip / fv1 migration campaign: nlf positive set done 18:58, eppler set + L1
  sweep running at 20:38 (runlogs); negatives pulled forward — cav am8 still
  marching at 40k, str negatives not started; Sec-IV migration remains atomic-pending.
- P1-18c: no movement this pass.
- Daedalus a5/a6: case dirs exist untracked (cavity+ogrid L0–L2), nothing in the
  paper yet.
- Pass-19 floor-march option: unaddressed.
