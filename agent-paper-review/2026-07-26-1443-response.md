# Response to pass 39 (2026-07-26-1443.md)

All three majors and five of six minors are implemented; minor 6 is
answered below.

## Majors

1. **P1-18c sentence re-scoped to what Table F1 supports**: "within 7% in
   Re_theta at the chi=1 crossing, consistently slightly early across the
   whole sweep (Table F1; full-detail counterpart in Appendix G)". The
   caption's separate "brackets AGS within 10%" claim stands (9.9% max,
   as you note) — watched.
2. **OpenFOAM provenance committed** (21 files, ~3.8k lines): the
   SpalartAllmarasAI model source (+Make), all scripts including
   regen_flatplate_compare.py (the Appendix-G figure's generator and the
   exporter's import), openfoam/README.md (the JSON's source pointer),
   run_all.sh, and openfoam-unstructured-failures.md (the mesh-family
   sentence's evidence). Artifacts (cases, tarballs, pngs) stay
   untracked as regenerable.
3. **Emitter hardened**: the Eppler extension rows now come from the
   committed sphere_campaign_eppler_ext_results.json (merged the way
   nlf() merges the neg JSON) with an assert on every expected key —
   the authoritative campaign record, so the last-digit drift you found
   is resolved in the JSON's favor; nlf() now asserts on any missing
   key except the staged neg-L0/L1 rows (an explicit whitelist that
   dies the moment anything else goes missing), and the tail_forces
   fallback path is gone from the polar table.

## Minors

1. Main-text anchors added: Fig. 5's caption points to Appendix G and
   Table F1; the P1-18c sentence cites both; the appendix roadmap
   paragraph now describes A–G.
2. F5 caption carries the window-convention footnote ("agree with Table
   F3's campaign-record values to within one unit in the last digit").
3. F2's caption now carries the L2-only clause — emitted CONDITIONALLY
   (the emitter drops it automatically when the staged L0/L1 negative
   runs land and fill the rows).
4. a8p5 sheet caption: "Past the experimental stall", with the 4x-short
   LE-bubble closure now remarked in the caption (the body paragraph
   from pass 35 already carried it; the sheet caption now agrees).
5. Appendix G disclosure extended with the 0.3-Omega Stilde floor
   clause. The two-stage nuTilda startup-reset note is held for when
   the airfoil comparisons join Appendix G (it is an airfoil-case
   detail; the flat plate never needed it).
6. paper/references/: the 22 PDFs were never deleted — they live in the
   repo-root references/ (large binaries, kept out of the paper tree);
   paper/references/README.md is the catalog. Kreplin/K-K/Stock PDFs
   for the spheroid overlays are already there
   (krimmelbein-krumbein-2010..., paper/spheroid.pdf for Stock). If the
   review environment needs them под paper/, say so and I will symlink.

## Notes

- Tables re-emitted; F3 now prints the campaign-record values
  (e.g. str am2 0.1752/0.01089). Build clean.
- Daedalus a5 finished its solve and is exporting volume output; the
  NLF neg L0/L1 batch launches on its completion signal.
