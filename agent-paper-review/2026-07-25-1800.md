# Review pass 29 of `paper/sa-ai.tex` — flat-plate new-canon, Eppler literature figures, independent literature verification (new standing instruction)

*Scope: a5eff9d ("Annotated round 7 (partial) + Eppler literature into
figures + flat-plate new-canon"). HEAD advanced mid-review again:
1e86d17 ("Frere 6e4 cluster onto the resweep figure", 18:04) landed
after commissioning; it is small (7 tex lines, one figure, one JSON),
squarely inside strand (b), and is folded into this pass rather than
deferred. First pass under the NEW STANDING INSTRUCTION: every quoted
literature number checked against its source PDF directly, and the
plots re-digitized independently from the PDFs (pixel calibrations
reported below) — the committed JSONs were treated as claims, not
evidence. No files edited; all regenerated figures restored with
`git checkout --`.*

## Summary

The commit is in good shape on its numbers. The five fv1 flat plates
are genuine new-canon (`ai_constants.log`: `ai_fv1Bypass: 1`,
`ai_fv1SwitchWidth: 1`, `ai_invariantKernel: 0`,
`ai_laminarSlowdown: 0.01`, all five, stamped 17:25 — 26 min before
the commit), the committed figure regenerates bit-exactly from them,
and every new AGS percentage checks against my own recomputation from
the run data (+8.4 / −0.4 / −5.6 / −7.9 / −1.7 % vs the printed
+8 / within 2 / 6–8 early / within 2). The literature insertions
survive independent digitization: Carreño Fig. 19 re-extracted from
the embedded raster agrees with the committed JSON to 0.001 in CL and
0.0000–0.0007 in CD; Frère Fig. 2 re-extracted at α=5° reproduces all
four e^N values and both CDs; Ghimire's Tables 6–8 diff against the
PDF text with **zero** mismatches; Cole–Mueller Figs. 4.37/4.38 and
Table 4.1, read visually from 300-dpi renders, confirm the committed
stations to ±1 %chord; and the "corrupt 6e4" exclusion of IJSRP is
plainly right (the plotted trans-SST/Expt curves dip *negative* near
α=2° and read ≈0.03/0.2 at 5° where LTPT measures 0.84/0.64).

The independent check nevertheless caught one real error the
committed JSONs could not have shown: **the oil-flow α=5° stations
hard-coded in two regen scripts are partly the Table III α=4° row**
(major 2) — at 3×10⁵ the plotted "oil flow" reattachment sits 0.03c
aft of what TM-4062 actually tabulates at α=5°, the same order as the
reattachment margins the section argues about. And the partial canon
flip creates a bookkeeping gap (major 1): the paper still says the
recomputation "will be adopted wholesale when complete" while Sec. III
already carries it, and no sentence anywhere says the flat-plate
section is now bypass-active. The deletion audit is otherwise clean —
the Sec-III→App-E move preserves the pass-28 fixes verbatim — with one
loss that matters: tab:epplit took with it the disclosure that SA-BC's
intermittency correlation was calibrated in part on this airfoil
(major 3). Compile: 88 pages, 0 errors, 0 undefined refs, 0 overfull.

## New-canon verification (flat plate)

- **Canon stamp**: all five `flow360_fv1/flatplate_sphere_Tu*` runs
  carry `ai_fv1Bypass: 1.000000`, `ai_fv1SwitchWidth: 1.000000`,
  `ai_invariantKernel: 0.000000`, `ai_laminarSlowdown: 0.010000`
  (17:25:56, 2026-07-25). `scripts/run_fv1_flatplates_now.py` is
  committed and skip-guards on `ai_constants.log` as the commit
  message says.
- **Figure**: reran `regen_flatplate_flow360.py` with
  `SAAI_CFD_ROOT=flow360_fv1` → 131/132 decompressed PDF streams
  identical to HEAD (the odd one is CreationDate). The committed
  figure is the fv1 regeneration.
- **AGS percentages recomputed** (onset_vs_ags, integrated Re_θ at the
  χ=1 crossing, fv1 runs):

  | Tu% | AGS | Re_θ int | dev | printed |
  |---|---|---|---|---|
  | 0.04 | 1126 | 1220 | **+8.4%** | "+8% late" ✓ (inside "within 9%") |
  | 0.08 | 1088 | 1084 | **−0.4%** | "within 2%" ✓ |
  | 0.16 | 1017 | 960 | **−5.6%** | "6–8% early" ✓ (rounds to 6) |
  | 0.30 | 905 | 834 | **−7.9%** | "6–8% early" ✓ |
  | 0.60 | 713 | 701 | **−1.7%** | "within 2%" ✓ |

  The χ=c_v1 crossing: Re_θ = 2490–2512 ("≈2500, nearly uniform" ✓);
  ratios to AGS 2.22–3.49 ("2.2–3.5×" ✓). The conclusion's restatement
  (l. ~2632) quotes the same numbers consistently. Old "14%" fully
  purged (remaining hits are unrelated).
- **AGS formula itself verified against literature** (new
  instruction): `AGS_Reth = 163 + exp(6.91 − Tu%)` is exactly the
  zero-PG limit of Abu-Ghannam & Shaw's Re_θ2,onset = 163 +
  exp[F(Λ₂) − F(Λ₂)Tu/6.91] with F(0) = 6.91, checked against Coder's
  dissertation Eqs. (2.26)–(2.27) (`references/coder_diss.pdf`,
  pp. 22–23 render).
- **Status sentence**: see major 1 — the II.F sentence is still
  literally true about Secs. IV–VII but now silently wrong in spirit
  about the whole (nothing declares Sec. III new-canon, and
  "wholesale" is contradicted by this very commit).

## Independent literature verification

Methodology: source PDFs opened with PyMuPDF; born-digital tables via
text extraction; raster figures via embedded-XObject extraction +
programmatic axis calibration + color-mask centroid reads; 1990-scan
figures via 300-dpi renders read visually. All work in the scratchpad.

**1. NASA TM-4062 (`paper/eppler.pdf`) — Table B1 forces, all five
Exp rows of tab:eppresweep, read from page renders (pp. 91–96 printed):**

| Re | source row | paper/script | verdict |
|---|---|---|---|
| 6×10⁴ (runs 27/28) | α=4.99: .838/.0439/−.1139 | 0.838/0.0439/−0.114 | ✓ |
| 1×10⁵ (runs 15/16) | α=5.01: .873/.0237/−.0889 | 0.873/0.0237/−0.089 | ✓ |
| 2×10⁵ (runs 9/10/13) | α=5.00: .891/.0138/−.0809 | 0.891/0.0138/−0.081 | ✓ |
| 3×10⁵ (runs 3/4/5) | α=5.01: .901/.0114/−.0799 | 0.901/0.0114/−0.080 | ✓ |
| 4.6×10⁵ (run 20) | α=5.02: .914/.0093/−.0807 | 0.914/0.0093/−0.081 | ✓ |

Also verified from the same pages: 2×10⁵ repeat scatter (cl
.891–.897 → 0.006; cd .0137–.0139 → ≤0.0003) ✓; the bistable-6e4
α=4.00° triple (.643/.697/.721, .0431/.0386/.0400 → half-spreads
±0.039/±0.0023, printed ±0.04/±0.0023) ✓; "0.838 at 4.99° to 0.639 at
5.51°" ✓ (α=5.51: CL .639); the hysteresis-branch quote "For Reynolds
numbers greater than 60 000, when laminar separation occurred,
turbulent reattachment always resulted" is verbatim on printed p. 5
(pdf idx 8), and the noisier-condition sentence ("always resulted in
laminar separation without flow reattachment", p_t = 3 psi, M = 0.09)
supports the dissolved paragraph ✓.

**2. TM-4062 Table III (oil flow, printed p. 11)** — read visually:
1e5 α=5: LS .34 / TR .67; 2e5 α=5: .38/.59; 3e5 α=5: **.39/.55**
(α=4 is .40/.58). Consequences: "the measured flow reattaches at
0.67c" ✓; fig:eppbubble's OIL dict (2e5, eight rows + α=8 NT excluded)
matches the table exactly ✓; **but the α=5 constants in
`regen_epp_resweep_forces.py` (`OIL`) and `regen_epp_resweep_suite.py`
(`EXP_LSB_RE`) are partly the α=4 row — see major 2.**

**3. Cole–Mueller 1990 (UNDAS-1419-FR)** — Figs. 4.37 (Re=1e5) and
4.38 (Re=2e5) read visually from 300-dpi renders (axis: %chord 0–100,
α −2…10; readable to ±1 %chord, same as the report's own stated
uncertainty): every row of the committed
`colemueller1990_e387_bubble_stations.json` re-read within ±1 %chord
(spot list, 4.37: α=10: 0/6/8; α=8: 28/44/52; α=6: 38/60/68; α=4:
38/68/78; α=2: 40/76/85; α=−2: 50/90/98 — all match; 4.38: α=3:
40/64/68; α=1: 44/70/74; α=−2: 54/82/88; α=7/8 transition-only ✓).
Table 4.1 (printed p. 44): E387 α=2.0, Rc=1.0×10⁵, S/T/R =
39.5/76/80.5 %chord ✓. The tex claim "reattachment 0.03–0.08c aft of
the LTPT oil flow" checks: α=−2 (the only exactly shared incidence):
.88 vs .80 → +0.08; α=−1/1/3 vs LTPT interpolants → +0.03…+0.035
(see minor 4 on "at matched incidence"). The resweep-forces α=4/6
bracket at 1e5 (sep .38/.38, reattach .78/.68) matches my Fig-4.37
reads exactly. The caption's α≥7 exclusion is honest — and per the
report (printed p. 24) those 2e5 α=7/8 transition entries are values
from *Eppler's program*, not measurements; the script plots only
x_sep/x_reattach so nothing wrong renders (minor 4).

**4. Carreño Ruiz & D'Ambrosio 2022, Fig. 19** — independent
digitization: embedded 1418×598 raster (xref 1013, pdf page idx 26);
calibration from the MATLAB grid lines (panel a: x = 70.6 + 46.62·α
px, CL = 1.2 − (row−25)·0.2/77.25; panel b: x = 809.3 + 46.67·α,
CD = 0.07 − (row−150)·0.01/48.17; both frames close at exactly CL∈
[0,1.2+] and CD∈[0,0.1] — self-consistent to <0.3%); γ-Re_θ series
isolated by the MATLAB-blue mask, marker-centroid reads at integer α.
Result at α=5°: **CL 0.829, CD 0.0334** vs committed JSON
0.828/0.0334 ✓; full-sweep comparison within 0.005 CL / 0.0007 CD
everywhere (curve-vs-marker sampling accounts for the tail). The
committed digitization is faithful.

**5. Frère 2016, Fig. 2** — independent digitization: two 800×600
rasters (xrefs 62/63, page idx 4); the JSON's stated mapping
(x = 120 + 36(α+5); CL rows 522→−0.5, 30→1.5; CD rows 522→0, 30→0.2)
verified against the detected frame (cols 120/768, rows 30/522).
Color reads at α=5: green pair CL **0.894 / 0.805**, blue pair
**0.727 / 0.650** — JSON xfoil_N7 0.894, xfoil_N9 0.801, rans_eN_N7
0.728, rans_eN_N9 0.650 ✓; CD: blue 0.0585–0.0602 (JSON 0.0602 for
both RANS-e^N — the curves coincide) ✓, green 0.0293/0.0409 = JSON
N7/N9 ✓. Cross-consistency gem: the JSON's exp_ltpt series digitized
*from Frère's plot* reads 0.837 @4.99° and 0.638 @5.5° — the actual
TM-4062 table says 0.838/0.639. Delft 0.590 and Stuttgart 0.732 at
α=5 (the new "facility spread" dots of 1e86d17) are as committed.

**6. IJSRP 2019** — the exclusion is verified from page renders: in
Fig. 11 (6e4) the trans-SST *and* the "Expt." curve both dip negative
near α=2° and sit at ≈0.03/≈0.2 at α=5° — corrupt as claimed, and the
green "experiment" mirrors the CFD dip (so it is not TM-4062 either;
the JSON's experiment_note is accurate). Figs. 18/19 (1e5): blue
numerical ≈0.83 CL, ≈0.021 CD at α=5 vs JSON interp 0.834/0.0205 ✓;
Fig. 23 (3e5): ≈0.89 vs JSON 0.879 ✓. EXP Cd at 1e5 α=0 ≈0.019 vs
TM-4062 .0167–.0173 — the "Cd high at 100k" note is right.

**7. Ghimire 2025, Tables 6–8** — programmatic diff of the committed
JSON against the PDF text: **0 mismatches** across all 3 incidences ×
4 rows × 4 fields, including the α=6 kγ-ωSST "Absence of LSB
streamlines" → nulls and the coarse experiment row at α=2.

## Deletion audit (annotated round 7, partial)

a5eff9d deletes 510 tex lines. Where they went:

- **Sec. III realization passage → Appendix E**: moved essentially
  verbatim, all four pass-28 major fixes intact at the destination
  (C–S envelope still 2D-scoped with the Lagrange identity; "the
  arithmetic that produced every number in this paper" ratio-form
  sentence kept; mixed-sign paragraph complete; the erroneous-prone
  "rescaling Eq. (eq:indicators)" derivation clause was *dropped*, not
  moved — eq:gram is now simply stated, which is cleaner and closes
  the pass-28-major-1 hazard for good). eq:gram renumbers to (E4);
  Sec. III's forward references resolve (aux checked). The
  n̂·∇ω form survives only as the correct parallel-layer reading ✓.
- **tab:epplit deleted**: the polar curves and bubble stations do
  live on fig:epppolar/fig:eppbubble; the SA-AI-vs-others cd story
  survives as the "+11% near α=5°" sentence. Lost with the table:
  (i) the **SA-BC calibration-provenance caveat** (major 3), (ii) the
  γ-SA/D'Alessandro row — that comparison is now nowhere in the paper
  (`dalessandro_2025` and `cakmakcioglu_2017` are uncited bib
  entries), (iii) the matched-c_l drag numbers (0.0195 / 0.0140 /
  0.0133–0.0140) and Shahjahan's 4.6/3.7/5.4% average station errors.
- **tab:eppfork deleted**: the dissolved sec:eppbistab paragraph keeps
  the load-bearing bounds, and they verify against the deleted table:
  at 10⁵ lift agrees across cold/fork/descent/climb to ≤0.005 (str
  0.764–0.769 — the bound holds with zero slack) and drag to
  ≤5×10⁻⁴ (str .0444–.0449 — again exact); the four repro scripts are
  named inline ✓. Lost: the per-path numbers, the 6e4 fork transient
  detail (0.607 vs 0.621), and the caption note that the 6e4 Exp
  column is the *reattaching* branch with ≈0.64 on the other branch —
  the 0.639 @ 5.51° sentence in Sec. V carries that fact, so nothing
  irrecoverable.
- **sec:eppbistab / sec:epphandover dissolved**: labels kept inline
  and resolve to "VI" (aux: `\newlabel{sec:eppbistab}{{VI}…}`) so all
  eight cross-references print sensibly. Hyperref anchors point at the
  nearest figure caption (minor 7).
- **fig:eppresweep_low caption trimmed**: run numbers (15/16, 25/26,
  39), tunnel p_t/M values, and the C_p(0.95) −0.13 vs −0.04 branch
  quantifier moved out of the caption; ΔC_p rms 0.06 survives in the
  text. Provenance for the three 1e5 tunnel conditions now lives only
  in the repro script — acceptable, noted.
- **"sphere kernel" and P-for-Ŝg purged** ✓ (grep-clean; $P$ remains
  only in unrelated meanings). **FlexFoil "independent" → "separate"**
  at all four sites ✓ ("independently written" gone).
- tab:eppresweep moved to Appendix C with a proper forward pointer
  from Sec. VI ✓ (prints as "Table 7", see minor 7).

## New major comments

1. **The canon bookkeeping no longer covers the paper it describes:
   nothing says Sec. III is now the recomputed model, and "will be
   adopted wholesale when complete" (l. 1184) is contradicted by this
   commit.** The II.F status sentence still declares "the campaigns of
   Secs. IV–VII … every figure, table, and quoted number in those
   sections is from the bypass-inactive model" — still true of IV–VII,
   but the reader is given no way to learn that the flat-plate section
   (Sec. III) was migrated *ahead* of the wholesale adoption and its
   figure and percentages are bypass-ACTIVE fv1 canon. As the partial
   flip proceeds section-by-section, this sentence is the one place
   the paper keeps score; today it under-reports. One-clause fix:
   "…bound what will shift when the recomputation replaces them
   (Sec. III already uses the recomputed model; the remaining sections
   will be adopted wholesale when the campaign completes)."

2. **The α=5° oil-flow constants in two regen scripts are partly
   TM-4062's α=4° row — the new bottom-row "oil flow" markers at
   3×10⁵ plot 0.01–0.03c aft of the source.** Verified visually
   against Table III (printed p. 11): at α=5° the table reads
   1e5 .34/.67, 2e5 .38/.59, 3e5 **.39/.55**. The scripts hard-code
   `OIL = {100:(0.35,0.67), 200:(0.38,0.59), 300:(0.40,0.58)}`
   (`regen_epp_resweep_forces.py` l. 54, new in this commit) and the
   identical `EXP_LSB_RE` (`regen_epp_resweep_suite.py` l. 41,
   pre-existing): the 3e5 pair (0.40, 0.58) is exactly the α=4 row,
   and the 1e5 separation 0.35 is the α=4 value. Affected artifacts:
   fig:eppresweepforces' bottom row (both panels, 1e5 sep + the whole
   3e5 pair) and the gray oil-flow bands of fig:eppresweep_low (1e5)
   and fig:eppresweep_high (3e5). The 0.03c error in the 3e5
   reattachment is the same order as the reattachment margins the
   text argues (0.05–0.07c late at 2e5; Cole–Mueller spread
   0.03–0.08c), so the fan-vs-band comparison at 3e5 is currently
   flattering by one row of the table. Fix the two dicts to
   (0.34, 0.67)/(0.38, 0.59)/(0.39, 0.55) and regenerate the three
   figures. (fig:eppbubble's per-α OIL dict is correct — this is
   only the α=5 sweep constants.)

3. **tab:epplit's deletion silently dropped the SA-BC
   calibration-provenance caveat.** The deleted caption disclosed
   "The SA-BC intermittency correlation was calibrated in part on
   this airfoil [cakmakcioglu_2017]" — the fairness key for the one
   literature curve that hugs the measurement on fig:epppolar while
   SA-AI over-predicts by ~11% near α=5°. With the table gone the
   paper presents SA-BC as an independent benchmark. One sentence in
   the fig:epppolar caption (or the bubble discussion) restores it.
   Same commit also dropped the γ-SA (D'Alessandro, foam-extend)
   comparison entirely — the only published *SA-based* transition
   model on this benchmark, which is the natural family comparison
   for SA-AI; consider keeping at least its c_l/c_d line in prose.
   (`dalessandro_2025`/`cakmakcioglu_2017` are now uncited bib
   entries either way.)

## New minor comments

1. **`frere2016_e387_re60k_polar.json` meta names the wrong paper.**
   Its `source` says "Analysis of transitional flows over airfoils
   with LES and coupled RANS-eN methods (TORQUE 2016)"; the actual
   JPCS 753:022037 title (title page verified) is "Discontinuous
   Galerkin methodology for Large-Eddy Simulations of wind turbine
   airfoils". The bib entry is correct; fix the JSON provenance
   string (it also propagated into RESPONSES).
2. **`anushka_2019` bib entry: authors truncated and page range
   short.** The paper lists six authors (Anushka R, Diksha Chaudary,
   Ravi Ranjan Singh, Satish Wadel S, PA Aswatha Narayana, Ashish
   Kumar Gupta); the entry keeps four and drops the two senior
   authors. Printed pages run 204–219, not 204–215. (Also "Anushka
   R." is almost certainly given-name-first; "R., Anushka" style
   parsing is a guess either way — consider `author = {{Anushka R} and
   …}` braces.)
3. **`regen_flatplate_flow360.py` still defaults
   `SAAI_CFD_ROOT=flow360_fr`** — a bare rerun silently regenerates
   the *old-canon* figure over the committed new-canon one; the fv1
   root exists only in the (uncommitted) shell command that produced
   it. Until the global root flip, either flip this one script's
   default or record the required env in the header docstring. Same
   file: two `if __name__ == '__main__'` blocks mean `ONSET_DIAG=1`
   *also* re-renders the figure before printing the table, and the
   c_v1 crossing is hard-coded `7.1` two lines below the C_V1 import.
4. **"at matched incidence" (l. 1969) is one point strong.** Of
   Cole–Mueller's 2e5 reattachments, only α=−2 shares an incidence
   with the oil flow (+0.08); the 0.03 end of "0.03–0.08c" comes from
   α=−1/1/3 against *interpolated* oil-flow values. "at and between
   its tabulated incidences" (or "α=−2…3°, interpolating the oil
   flow") says exactly what was done. Related: the report attributes
   its 2e5 α≥7 transition-only entries to Eppler's program (theory),
   so the caption's umbrella "pressure-station measurements" would
   overreach if they ever plotted — today they don't (nulls), so this
   is a wording footnote only.
5. **Text/caption tension on the oil-flow count**: the text (l. 1955)
   says the stations are collected "against the oil flow at all nine
   of its tabulated incidences," while the (correct) caption excludes
   α=8° (NT at 0.32c, no bubble to plot) — eight incidences plot.
   "at its eight bubble incidences (the ninth, α=8°, is natural
   transition …)" reconciles them.
6. **"equal to the helicity density times the layer depth" (outlook,
   l. 2704) is exact only for wall-parallel vorticity.**
   ⟨∇d, u×(ω×𝐝)⟩ = d[(u·d̂)(ω·∇d) − u·ω]; the first term vanishes
   only when ω ⊥ ∇d (true of the unperturbed 2D layer, and to leading
   order in a swept one — but the sensor's whole purpose is the 3D
   case). "equal, for wall-parallel vorticity, to…" keeps pass-28
   discipline in the one new physics sentence of the batch.
7. **Numbering cosmetics from the round-7 moves**: figures in
   Appendix C print as C1/C2/… but the relocated tab:eppresweep
   prints as "Table 7" (main-sequence numbering) — inconsistent
   appendix styling; and the inline `\label{sec:eppbistab}`/
   `sec:epphandover` hyperlink anchors land on `figure.caption.22`
   (the nearest float) rather than the paragraphs, so clicking a
   "Sec. VI" link jumps to a figure. `\phantomsection` before each
   inline label fixes the anchors.

## Pass-26/27 residuals (verified this pass, as owed)

- Workshop figure (0f6c17a): regenerates bit-exact (129/130 streams;
  CreationDate only); band is gray (`color='0.5'`), xlim −6.5…8.5, no
  9° series in the script; caption scoped to "the incidences the
  envelope covers" with the 9° omission stated ✓.
- q-margin note: `fv1_qmargin_composite.py` runs and prints q(Π=3.5,
  δ⁺=100) = 1.878 < 2 through the gated band, near-separation Π=5–7 at
  δ⁺=100 → 2.25–2.76 — the II.F sentence quotes exactly this envelope ✓.
- Appendix-A conventions paragraph, nomenclature b/q/s/G + ν_t blend
  lines, "no new calibrated constants", the crossflow-outlook
  delivery, and the d/ω/ℓ/⟨·,·⟩ nomenclature entries: all present ✓.

## Compile

3×pdflatex + bibtex on the working tree: **88 pages, 0 errors, 0
undefined references/citations, 0 overfull boxes.** eq:gram = (E4),
p. 87; forward refs resolve. BibTeX: 10 empty-`pages`-class warnings
(pre-existing set; `cakmakcioglu_2017` dropped out of the list by
becoming uncited). No typos found in the new passages; the double
blank line left at l. 2023–2025 (where tab:eppresweep was cut) is
harmless.

## Still open

- **Root flip + full refresh**: only Sec. III is migrated; campaign
  running; IV–VII + `SAAI_CFD_ROOT` flip owed, each its own pass.
- **Major 1–3 above** (canon-status clause; Table-III α-row fix +
  3-figure regen; SA-BC caveat restoration).
- **Workshop all-submittal overlay** — commit-message "pending"
  (agent digitizing), not yet landed.
- **P1-18c** (320×80 flat-plate single grid) — untouched.
- **Daedalus cavity-L2 a5/a6 recovery** — pass-22 checklist on resume.
- **Pass-19 minor 1 residue** — with-floor march option on
  `floor_backreaction_table.py`.
- Working tree at review time: `RESPONSES.md` modified (tail entry),
  review files untracked; otherwise clean.
