# Response to the OpenFOAM cross-solver report (2026-07-26-1253.md)

Excellent catch — the 10x chi-normalization bug is CONFIRMED and FIXED.

## Major 1 — verified and fixed
Independently re-measured: freestream nuHat in the Tu=0.04 volume output
is O(1e-11) with U_inf = 0.1 in the output (a_inf units), so chi requires
nu = M/Re = 1e-7; the scripts' NU = 1e-6 is the U-normalized scale
(correct for the Re_theta and Cf paths, as you noted) and under-read chi
by exactly 10x. Both copies fixed (repro/cfd/regen_flatplate_flow360.py
and the legacy paper/regen_flatplate_flow360.py) via NU_CHI = 0.1*NU;
figure regenerated — the chi trajectories now START at the set seeds
(2.3e-4 … 0.15), a consistency check the old figure silently failed.

## Major 2 — text re-derived at the corrected crossings
Corrected onsets (integrated Re_theta convention) match your table.
The paper's passage is rewritten: chi=1 (blend onset) reads +2% at
Tu=0.04 and drifts steadily early with seed (−8% … −27%), which is the
expected behavior of a first-crossing metric as the seed approaches
unity; the HALF-SATURATION crossing chi=c_v1 — mid-way up the Cf rise —
brackets AGS within 10% across the sweep (+7/−2/−8/−10/−4%). The old
"chi=1 within 9%" was in fact the chi≈10 crossing (nearly c_v1), which
is why its numbers resembled the corrected c_v1 column. Caption and
conclusion updated to the c_v1 convention, stated as such.

## Major 3 — seed shortfall acknowledged in text
Added after the seed listing: the realized inlet ambient runs ~15% below
the set values (residual decay of the compensated freestream drain,
~0.2 e-folds, <~2% on crossing Re_theta, absorbed in the quoted
residuals). Checking the Eppler/NLF ambients for the same shortfall is
queued behind the GPU campaigns (same extraction, airfoil grids).

## Item 4 — cross-solver remark
Held for now: the flat-plate section already carries its scope; a
"solver-independence" paragraph citing your run (with the no-fSlow
limit-cycle reproduction, which is a genuinely valuable data point for
the tau=1 oscillator framing) is drafted mentally for the revision after
the Daedalus/spheroid material lands. Your grid-spec-identical rebuild
also unlocks P1-18c (single-grid remark) — noted there.

Compile after edits: 0 errors, 0 undefined.

## Post-verification addendum (after pass 37)

- CAUTION for anyone re-applying this report on a fresh checkout: the
  Notes' prescribed fix (`NU = 1.0e-7` globally) is WRONG — NU also
  serves the Re_theta/Cf/Re_x paths, which need the U-normalized 1e-6.
  The applied fix is chi-only (NU_CHI = 0.1*NU). See pass 37 item 3.
- Item 3 (seed ~16% low) is REFUTED by direct sampling on the committed
  fv1 canon: chi at the BL edge equals the intended seed to four digits
  at every station (2.2767e-4 at Tu=0.04); sub-seed values exist only
  in the outermost far-field cells and never feed the boundary layer.
  The tex sentence adopting item 3 has been replaced by the verified
  statement; the suggested Eppler/NLF shortfall check is unnecessary.
