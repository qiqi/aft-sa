# Review pass 9 of `paper/sa-ai.tex` — the Secs. III–V refresh

*Reviewed 2026-07-23, ~17:11–17:35. Scope: commits **727001c** ("NLF 24/24 +
Re-sweep L0/L2 suite 16/16 at the whole-equation canon; Green/tanh OS
instrument") and **44be6c0** ("H≤5 cap in Sec II; exact profile specs in
captions; ramp-width justification; full CFD-figure refresh at the
whole-equation canon"), plus the 17:00 working copy (uncommitted deltas on
`sa-ai.tex` and `figs/flat_plate_batch_flow360.pdf`). Line numbers refer to
the 2838-line 17:00 working copy. Build note: `sa-ai.pdf`/`sa-ai.log` (16:48,
zero errors/undefined refs) predate the 17:00 tex edits and the 16:54
flat-plate figure — a rebuild is owed before the PDF is read as current.
Numeric claims below were re-derived from the committed campaign JSONs, the
case histories (`total_forces_v2.csv`, `xtr_history.csv`), and by running the
committed generators (`tab_frozen_slope.py`, `fig02_onset_graze.py`,
`regen_epp_reattach.py`, `fig04_shapefactor.py` at perturbed `RAMP_W`, the
flat-plate `onset_vs_ags()` on the `flatplate_sphere_*` cases, and median
force extraction identical to `regen_nlf_polar.converged_clcd`).*

## Summary

The long-gated refresh (pass-5 major 1) is **real but landed unevenly — the
draft is now split down the middle of Sec. V.** What is refreshed and
verifies: every CFD figure (both polars 16:23, all five-row benchmark
figures, the resweep suite 16:33 — `eppler_resweep_*.pdf` are no longer the
md5-identical L1 placeholders pass 7 found, and now plot the 24-case L0–L2
suite from the committed new-canon data), the flat-plate section (working
copy; every quoted percentage reproduces from the `flatplate_sphere_Tu*`
cases to the stated rounding), the NLF transition table and its surrounding
prose (working copy; every entry matches `sphere_campaign_nlf_results.json`
strL2 exactly), and the new Sec.-II material, all of whose quantitative
claims I could verify (Tables 2–3 regenerate exactly; the graze caption's
0.93–1.14 / rms ~6% / ~1.3 favorable edge / reversed-profile 0.91 at
Re_θc≈26 all reproduce; the w=0.35 ramp arithmetic and the <1% at w/2,
<2.5% at 2w sensitivity reproduce on the committed marcher).

What is **not** refreshed: essentially the entire Eppler section. Both
tables (`tab:eppxtr`, `tab:eppresweep`), the polar-caption agreement claims,
the fully-turbulent-baseline percentages (both airfoils), the sweep
narrative and its captions' recovery stations, and the Conclusion's
quantitative recap all still print old-canon numbers — under figures that
now show the new canon. Worse than a number-sweep: the Re-sweep story
**changed qualitatively** at the new canon (at Re=10⁵ the upper surface no
longer reattaches mid-chord — single upward C_f crossing at 0.99c on every
committed grid — yet text and caption still say "regains zero near 0.71"),
the NLF cross-family L2 drag agreement degraded ~3× past its printed claim,
and the working copy itself introduced one new claim ("coarser levels
scattering by up to 0.03c") that the committed campaign JSON contradicts by
a factor of seven. Separately, 44be6c0's reference-pickle rebuild silently
replaced XFOIL with FlexFoil in the high-α NLF overlays while three captions
and two body sentences still attribute them to XFOIL. The H≤5 cap is
well-justified where stated but is contradicted by Sec. II's own Fig. 4,
which still marches to H≈10.6.

Net: the refresh is ~70% landed and the landed part verifies beautifully;
the remaining 30% (Eppler tables/prose, sweep text, turbulent-baseline
numbers, Conclusion recap, XFOIL→FlexFoil attribution sweep) is now the
gating item, and it is smaller and sharply enumerable — see majors 1–7.

## Refresh verification

Itemized against the task list; ✓ = verified against committed data/generators.

**Verified refreshed:**

- ✓ **`eppler_resweep_*.pdf` are real** (16:33, new md5s, 186–191 KB vs the
  old 43/0.9 KB placeholders); `regen_epp_resweep_suite.py` draws L1 from the
  re-converged legacy-named cases and L0/L2 from
  `sphere_campaign_epp_sweep_levels_results.json` (committed in 727001c,
  16/16 converged). No kernel mixing.
- ✓ **Flat plate** (working copy, l. 1486–1503 + caption l. 1534–1537):
  re-ran `onset_vs_ags()` on the `flatplate_sphere_Tu*` cases —
  χ=1 crossing: +9.8%, +0.6%, −6.6%, −9.6%, −7.2% → text "+10% late …
  essentially exact (+0.6%) … 7–10% early" ✓ (−6.6 rounds to 7);
  χ=c_v1: +22.8/+16.2/+13.7/+17.8/+35.7% → "+14 to +23% … +36% at 0.60%" ✓.
  The "±10% brackets" framing and the new two-root attribution (late bias =
  retained-drain footprint, consistent with Sec. II's +7% N=9) are
  internally consistent. Figure rebuilt 16:54 from the sphere cases.
- ✓ **NLF Table 5 (`tab:nlftrans`)** (working copy): every SA-AI entry and
  every c_l matches `sphere_campaign_nlf_results.json` strL2 exactly
  (0.5136/0.9797/1.513/2.000; fronts 0.3849/0.2537/0.0726/0.0036 upper,
  0.5663/0.6133/0.6329/0.662 lower → printed 0.51/0.98/1.51/2.00,
  0.38/0.25/0.07/0.00, 0.57/0.61/0.63/0.66). The e^9 column at 9°/15°
  matches the FlexFoil pickle (0.094/0.004 upper, 0.666/0.686 lower →
  0.09/0.00/0.67/0.69). Prose updated in step ("0.25c vs 0.33c", the
  0.57c/0.62c α=0° lower-front story, c_l=0.98, 0.06c).
- ✓ **"Two finest meshes agree to 0.01c on every front"** (l. 1665): max
  cav-vs-str L2 front difference in the JSON is 0.0088c (α=4° lower) ✓.
- ✓ **Fully-turbulent polar baselines exist and are new**
  (`turb_baseline_results.json` 16:10, `strL2prop_*_turb_a*` dirs;
  median-extracted per `regen_nlf_polar.py`) — but the *printed percentages*
  were not updated to them (major 3).
- ✓ **Sec. II H≤5 cap, tables**: `tab_frozen_slope.py` regenerates Tables
  2–3 exactly as printed (five profiles, deep row gone, Re_θ0 column
  827/242/97/36/26, all masked/unmasked entries, the 0.49→1.13 ladder). The
  "0.64–0.85 even on the separation branch" range is now exactly the table's
  span ✓ (pass-8 minor 2 resolved by the row deletion + stated cap).
- ✓ **Exact profile specs in captions**: Fig. 1 (β list, Stewartson
  β=−0.19 H≈4.9, Re_θ=500/200 evaluation stations, subcritical favorable
  carries no band) matches `fig01_indicator_sphere.py` (16:37) exactly;
  Fig. 2 likewise (generator 16:37, figure 16:43).
- ✓ **Onset-graze caption numbers**: running `fig02_onset_graze.py`:
  attached ratios 0.932–1.137 (rms 6.4%) ✓ "0.93–1.14 (rms ~6%)";
  β=+0.35 → 1.274 ✓ "~1.3"; reversed profile 0.911 at Re_θc=26 ✓ "closest
  approach 0.91 at its own, very low Re_θc≈26". The new immediate-ignition
  logic (Re_Ω ∝ Re_θ carries a bubble far above the floor) is sound and a
  genuine fix of the old "rides above the threshold" claim.
- ✓ **Ramp-width justification** (l. 772–781): S(0.65)=0.119, S(1.35)=0.881
  ✓ "12% to 88%"; measured on the committed marcher: Blasius N=1 station
  349.3→346.1 (−0.9%) at w/2, →356.5 (+2.1%) at 2w; moderate adverse −0.9%/
  +2.5% ✓ "under 1% when halved, under 2.5% when doubled". (No committed
  script performs this measurement — see minor 7.) w=0.35 is in the
  constants list (l. 1349) and nomenclature.

**Not refreshed (evidence per item; details in majors 1–7):**

- ✗ `tab:eppxtr` and its prose: re-running `regen_epp_reattach.py` gives
  materially different rows (strL2: 0.788/0.737/0.655/0.449 vs printed
  0.778/0.726/0.643/0.449; strL0 a2 0.672 vs 0.656; cavL0 a7 **0.087** and
  cavL1 a7 **no reattachment** vs printed 0.458/0.449).
- ✗ `tab:eppresweep`: unstructured-L1 column is old-canon everywhere
  (4.6×10⁵: printed 0.970/0.0097 vs actual 0.897/0.0155; 2×10⁵ printed
  0.947/0.0145 vs 0.887/0.0210); structured column survives at 3–4.6×10⁵
  but is off at 6×10⁴–10⁵ (0.832/0.0342 vs 0.818/0.0364).
- ✗ Eppler polar caption "ΔC_d ≲ 5×10⁻⁴" (l. 1913): actual L2 family spreads
  6.4/7.8/**11**/**21** ×10⁻⁴ at α=0/2/5/7°. The α=5° spread pass 6 flagged
  (1.1×10⁻³) is unchanged and the caption was not updated.
- ✗ "over-predicts by up to ~7% near α=5°" (l. 1922): strL2 0.01552 vs exp
  0.0138 = +12%; cavL2 +20%.
- ✗ "laminar to the trailing edge at every incidence" (caption l. 2032, body
  l. 2064): cavL0 α=0° lower front at 0.554c in the committed JSON (pass-6/7
  flag, still unaddressed).
- ✗ Sweep text still describes the old L1-only study: "reusing the L1 grids"
  (l. 2081), "All eight solutions" (l. 2087), Table caption "the two L1 mesh
  families" (l. 2097) — under suite figures showing L0–L2.
- ✗ **The Re=10⁵ physics changed and the text didn't**: on every committed
  grid the upper-surface C_f now has a *single* upward zero-crossing at
  0.988–0.999c (strL2 0.999, cavL2 0.997, cav-L1 0.988; min C_f −0.004 to
  −0.005) — no mid-chord reattachment. Text (l. 2182–2184) and caption
  (l. 2222–2224) still say "a long bubble whose C_f regains zero near
  x/c≈0.71, with a shallow turbulent recovery."
- ✗ Recovery stations at the two highest Re: caption/text "0.52–0.54
  (3×10⁵), 0.48–0.49 (4.6×10⁵)" vs new-canon L2 0.567–0.569 and 0.496–0.499
  (L1 0.541–0.563 / 0.476–0.479). Pass 7 warned these had to be re-read, not
  just the figures swapped; they were not.
- ✗ "ΔC_d ≲ 6×10⁻⁴ at 3–4.6×10⁵" (l. 2164): actual L1 family spreads are
  5.3×10⁻³ and 5.4×10⁻³ (L2: 1.7×10⁻³ and 1.8×10⁻³).
- ✗ "C_l = 0.77 unstructured vs 0.65 structured" at 6×10⁴ (l. 2163): now
  0.735 vs 0.646.
- ✗ "~5% [drag excess] on the unstructured grid at Re=10⁵" (l. 2153–2154):
  now +38% (cav) / +54% (str) vs the measurement — consistent with the
  non-reattaching separation above, and a materially worse headline the
  paper has not absorbed.
- ✗ Turbulent-baseline percentages, both airfoils (major 3).
- ✗ NLF cross-family drag agreement claim (major 4).
- ✗ Conclusion recap (major 7).
- **l. 1362 "every coupled-RANS computation of Secs. III–V"**: now true of
  the *computations* (all campaign cases, sweeps, and baselines re-run at
  the committed canon — provenance verified in pass 7 and extended by
  727001c) and of every figure; still **not** true of the printed Eppler
  tables/prose, which quote runs that no longer exist. The sentence becomes
  honest exactly when majors 1–2 are fixed.

## Status of prior comments

- **P5-M1 / Secs. III–V canon refresh** — **~70% landed** (figures, flat
  plate, NLF table+prose ✓); remainder enumerated as majors 1–7 below.
- **P1-1** Daedalus Q1/Q2 jargon / undefined predecessor kernel — **open**
  (l. 2258–2263: "$Q_1$-only gate with its own three-anchor constants",
  $Q_2$ still never defined); the refresh *sharpens* it: Secs. III–V now
  actually run the whole-equation canon, so "the sole modeling difference …
  is the in-bubble amplification that $Q_2$ regulates" (l. 2259–2263)
  understates — the onset constants (three-anchor vs k=0.712-scaled) differ
  too, and the disclosure should say so.
- **P1-2** 0.02c predecessor-kernel cross-ref to Sec. V — **open** (l. 2263,
  2425; Sec. V still contains no kernel-vs-kernel comparison).
- **P1-7** RP² evenness/reversed-flow argument — **open** (unchanged).
- **P1-17 remainder** — **open**: "within ~30%" (l. 2422) vs "0.9–1.9×"
  (l. 2818) unreconciled; tie back-reaction "<1%"/"−2.6%" (l. 1177–1179)
  still measured at the low-branch anchor (acknowledged, still owed).
- **P1-18a** literature table — **open** (Conclusion still defers,
  l. 2504–2507).
- **P1-18c** flat-plate grid statement — **open** (single 320×80 grid,
  l. 1442–1450; note the refresh re-ran the plate on the same single grid).
- **P1-m13** Somers Fig. 11d/9d — **open**, but improved: the Fig. 11d
  digitization is now committed inline in `regen_nlf_polar.py` (pixel-
  calibrated) — source-PDF verification still owed.
- **P1-m17** eq-citation style — **open** (bare `(\eqref{...})` at l. 573,
  1705).
- **P7-m1** abstract missing "and" + "its freestream seed" antecedent —
  **open** (l. 48–51).
- **P7-m2** $u^{-1/2}$ substitution stated before the reversed-flow floor —
  **open** (l. 640–644).
- **P7-m3 / P6-m11** "All eight solutions" + sweep table re-verification —
  **open**, folded into major 2.
- **P8-m1** $Re_{\theta 0}$ vs $Re_{\theta c}$ split — **open and widened**:
  44be6c0 added two more $Re_{\theta c}$ uses (l. 816, 840) against six
  $Re_{\theta 0}$ sites; neither symbol is in the nomenclature.
- **P8-m2** deep-separated-row exclusion wording — **resolved** by the H≤5
  cap (rows deleted, reason stated in the Table-2 caption) — but see major 8
  for the residue.
- **P8-m3** constants-list c_ν,ai "across the transitional decade" —
  **open** (l. 1351, unchanged).
- **P8-m4** stale "text quotes … unstable-station means" printout in
  `tab_frozen_slope.py` — **open** (44be6c0 edited the file but only for the
  row drop; the stale provenance string still prints).
- **P8-m5** "just past the Blasius critical (Re_θ=400)" noun elision —
  **open** (l. 672).
- **P8-m6** deleted high-Re caveat (record only) — unchanged; note the new
  Green/tanh OS instrument is precisely the quantitative backfill for the
  companion deep-H story (minor 6).

## New major comments

1. **The Eppler section was left behind by the refresh: new-canon figures
   over old-canon tables and prose.** Every Eppler figure was regenerated
   from the re-run campaign (16:23–16:33), but `tab:eppxtr` (l. 2040–2043),
   `tab:eppresweep` (l. 2118–2122), the polar caption's "ΔC_d≲5×10⁻⁴"
   (l. 1913), "~7% near α=5°" (l. 1922), the reattachment prose "0.78/0.73/
   0.64c … closing to 0.002c" (l. 2056–2064), and "laminar to the trailing
   edge at every incidence" (l. 2032, 2064 — vs cavL0 α=0° lower front
   0.554c) all predate it. Evidence: `regen_epp_reattach.py` run today gives
   strL2 0.788/0.737/0.655/0.449 vs printed 0.778/0.726/0.643/0.449, and at
   α=7° the coarse cavity story changed qualitatively (cavL0 reattaches at
   0.087c, cavL1 not at all, vs printed 0.458/0.449 — the near-LE trip of
   the new canon); the sweep-table unstructured column is wrong by up to
   ΔC_d=+0.006 and ΔC_l=−0.073 (4.6×10⁵). A reader comparing Table 6
   against the figure directly above it will find different numbers. Fix:
   re-print both tables from `regen_epp_reattach.py` /
   `regen_resweep_table.py` (note the latter currently crashes — minor 5)
   and sweep the quoted prose (list in "Refresh verification").

2. **The Reynolds-sweep narrative is now qualitatively wrong at Re=10⁵, and
   the sweep text still describes the L1-only study.** At the whole-equation
   canon the 10⁵ upper surface never recovers mid-chord: single upward C_f
   crossing at 0.988–0.999c on cav-L1, cavL2, and strL2 (min C_f ≈ −0.005,
   transition χ-front mid-chord but the flow stays separated to the TE) —
   the committed 16:33 figure *shows* this, while the body (l. 2179–2184)
   and the caption **directly beneath the figure** (l. 2221–2224) still say
   "at 10⁵ the C_f regains zero near x/c≈0.71, with a shallow recovery."
   The bursting boundary has effectively moved from 6×10⁴ up to ~10⁵, the
   10⁵ drag excess is now +38–54% (printed: "~5% … on the unstructured"),
   and the family agreement at 3–4.6×10⁵ is 5.3–5.4×10⁻³ at L1 (printed:
   ≲6×10⁻⁴). Meanwhile "reusing the L1 grids … unchanged" (l. 2081), "All
   eight solutions" (l. 2087), and the table caption's "two L1 mesh
   families" (l. 2097) still describe the superseded study under 24-case
   suite figures, and the high-Re recovery stations (0.52–0.54 / 0.48–0.49)
   are the old L1 readings (new L2: 0.567–0.569 / 0.496–0.499). This is the
   biggest content item left: the section needs rewriting to the suite, and
   the model-assessment prose must own the worse 10⁵ result rather than
   describe the old one.

3. **Both fully-turbulent-baseline claims contradict the committed baselines
   they now sit above.** NLF polar caption: "drag sits 56–77% above the
   bucket" (l. 1683); body: "closes on SA-AI … (+26% at 9°, +11% at 15°)"
   (l. 1698). Eppler polar caption: "14–39% above the SA-AI drag" (l. 1915).
   Median extraction per the committed generator from the
   `strL2prop_*_turb_a*` cases (= `turb_baseline_results.json`, 16:10)
   gives NLF +142/+147/+64/+71% at α=0/4/9/15° and Eppler +87/+72/+66/+149%
   at α=0/2/5/7°. The old percentages appear to come from the pre-refresh
   baselines (or a different force-extraction of their unsteady histories —
   CD_std reaches 0.198 at NLF 15°, which is why 44be6c0 moved to medians);
   either way no committed data supports them. Since the figures now plot
   the new baselines, the captions actively disagree with their own curves.

4. **The NLF cross-family drag-agreement claim is ~3× too tight at the new
   canon.** l. 1657–1661: "agreeing in drag to within ΔC_d≈0.6–0.7×10⁻³
   through α=4° (rising to 1.0–1.4×10⁻³ at 9–15°)". Actual L2 medians:
   1.85/2.38/3.41/7.72 ×10⁻³ at α=0/4/9/15°. This is the sentence carrying
   the section's "adversarial-mesh robustness" headline, so it cannot stay
   as printed; either quote the new spreads (fronts agree to 0.01c while
   cavity drag carries a +1.9–7.7×10⁻³ discretization excess — a defensible,
   more informative statement) or re-scope the claim to fronts only.

5. **The working copy's new "coarser levels scattering by up to 0.03c" is
   contradicted by the committed campaign data.** l. 1665–1667. Counter-
   examples from `sphere_campaign_nlf_results.json` / `xtr_history.csv`
   (converged, solver-reported): cavL1 α=0° **lower front 0.356c** vs 0.566c
   at L2 (0.21c; strL1 reads 0.552 — the two L1 families differ by 0.20c on
   this front); cavL0 α=9° trips at 0.015c on *both* surfaces; strL0 α=15°
   lower 0.365 vs 0.662. If the intended scope is "the pinned fronts" or
   "L1 vs L2 on the structured family," say that; as written the sentence is
   falsified by the first column of its own cited table's source data. (The
   cavL1 α=0° lower-front outlier itself deserves a look — it may be the
   same marginal-onset pathology as the 9° cavity case, and is worth a
   footnote if kept.)

6. **XFOIL→FlexFoil attribution: the high-α NLF e^9 data are now FlexFoil's,
   but three captions and two body sentences still say XFOIL.** 44be6c0
   rebuilt the reference pickles; `xfoil_nlf0416_Re4M.pkl` now carries
   `source='flexfoil'` at *all four* incidences (the packaged xfoil SIGFPEs
   on NLF input per the commit message), and `regen_nlf_polar.py`'s legend
   says "FlexFoil (e^9, α≥9°)". Still saying XFOIL: the polar caption
   (l. 1680–1681 "the e^9 reference is XFOIL's (gray diamonds)"), the body
   (l. 1693–1695 "XFOIL converges at the identical conditions (C_l=1.52 at
   α=9°…)" — the number matches the FlexFoil pickle, 1.514), l. 1812–1814
   ("XFOIL, the reference e^9 solver, supplies the comparison there"), and
   the fig:nlfcfhigh caption (l. 1763–1764, "the e^9 overlay here is
   XFOIL's… and the N envelope is FlexFoil's" — both are now FlexFoil's).
   The working copy fixed only the Table-5 caption/footnote. Also
   reconcile: the new NLF text says "both e^9 implementations ignite at
   0.62c" (α=0° lower, mfoil+FlexFoil), while the `tab:eppresweep` footnote
   (l. 2136–2139) still tells the old story "mfoil (0.62c) and XFOIL (0.52c)
   separate by 0.10c" — one of these must go, and the footnote's "agree …
   to 0.005c in x_tr with one exception" needs re-verifying under FlexFoil.
   Finally, the ∂x_tr/∂N column still says "from XFOIL re-converged at
   N_crit=8 and 10" (l. 1846) — confirm that sweep is reproducible given
   the packaged xfoil's SIGFPE, or re-attribute.

7. **The Conclusion's quantitative recap was not swept.** (a) l. 2470–2474:
   "within ~17% of AGS … (exact at the lowest disturbance level, running
   early as Tu rises)" — the refreshed result is ±10%, **+10% late** at the
   lowest Tu (the sign of the headline error flipped; the new Sec.-IV text
   already says so). (b) l. 2487–2489: Eppler reattachment "0.78 vs 0.74,
   0.73 vs 0.67, 0.64 vs 0.59" and "0.04–0.06c aft" — new strL2 values give
   0.79/0.74/0.66 and 0.05–0.07c. (c) l. 2493–2496: "L1→L2 drift at or
   below 0.02c away from the marginal cases (… 0.03–0.04c)" — the new-canon
   marginal cases reach 0.24c (Eppler cav α=7° upper front 0.16→0.40) and
   0.21c (NLF cav α=0° lower, L1→L2). (d) l. 2496–2498: "agree to within
   0.02c … on every transition front and to 0.002–0.018c on the Eppler
   reattachment at L2" — new L2 reattachment spreads are 0.020/0.019/0.017/
   0.002c, so the upper bound is 0.020, and the α=0° front spread is 0.013c
   (fine). Sweep the paragraph against the refreshed tables once majors 1–2
   land.

8. **The H≤5 cap says "throughout Sec. II," but Sec. II's own Fig. 4 still
   runs to H≈10.6.** Table-2 caption (l. 601–604): "Shape factors past
   H≈5 are excluded throughout Sec. \ref{sec:model}…". Yet `fig:calibrate`
   (caption l. 962–965: "onto the reversed-flow (Stewartson lower) branch
   (H≈10.6; separated region shaded)") was not regenerated
   (`model_calibrate.pdf` mtime 10:48, generator 14:29 — both predate
   44be6c0) and the body still narrates the march "softening … onto the
   reversed-flow branch" (l. 980–984). Either scope the cap statement to
   the frozen-profile *tables* ("excluded from the comparisons of Tables
   2–3") or re-scope Fig. 4 to H≤5 — but note Sec. VI leans on the deep
   branch of that very figure ("within ~30% of the correlation
   (Fig. calibrate a)", l. 2422–2423; "1.3–1.8× across H=4.9–10.6",
   l. 2413) and App. B quotes "0.9–1.9×" — all now comparisons against a
   correlation the paper declares un-validated past H≈5. The Green/tanh OS
   instrument (minor 6) is the honest way out: it shows the kernel's deep-H
   reading matches OS (0.46× Drela vs OS's 0.45×) while Drela's
   linear-in-H extrapolation is ~2.2× high — i.e., beyond H≈5 the *kernel*
   is the better reference, which both justifies the cap and rehabilitates
   the Sec.-VI/App.-B deep-branch statements.

## New minor comments

1. Fig. `fig:nlfcflow` caption (l. 1740–1743): "upper x/c≈0.37–0.38, lower
   0.56–0.57 … marched forward to x/c≈0.24" — new L2 fronts are 0.38–0.39,
   0.57, and 0.25–0.26; the body and table already say 0.38/0.25. One-line
   caption sweep.
2. l. 1800–1801: "these offsets correspond to about one and a third of a
   unit of accumulated amplification, respectively" — as written it reads
   "1⅓ units". The arithmetic is 0.07c/0.067 ≈ 1 unit (e^9) and
   0.02c/0.067 ≈ ⅓ unit (SA-AI); write "about one unit and a third of a
   unit, respectively".
3. Constants list, l. 1349: `w=0.35` is the only SA-AI constant whose row
   carries no rationale; now that Sec. II justifies it, add the pointer
   ("onset ramp width scale; not fitted — marched stations insensitive
   (Sec. \ref{sec:calib_onset})").
4. Repro: `paper/repro/cfd/regen_flatplate_flow360.py` defaults to
   `PREFIX="flatplate_ags"`, whose case dirs no longer exist — it crashes
   out of the box and reproduces the paper figure only with
   `SAAI_CASE_PREFIX=flatplate_sphere`. Flip the default (the docstring
   already names the sphere cases as current).
5. Repro: `regen_resweep_table.py` crashes at the mfoil 460k point
   (`stagpoint_move: velocity error` uncaught in `mfoil_point`) before
   printing any rows — the documented divergence needs a try/except so the
   table generator that major 1 depends on actually runs.
6. The Green/tanh OS instrument (727001c, `explore_green_tanh_family.py`,
   "PREVIEW … not yet wired into the paper") is currently invisible to the
   reader. Beyond its role in major 8, it would also close pass-8 minor 6's
   disclosure gap (the deleted deep-H caveat) with one sentence plus a
   citation to Dini–Selig–Maughmer/Green. Recommend wiring it in at the
   Table-2 cap or in App. B.
7. The ramp-width sensitivity ("<1% at w/2, <2.5% at 2w — measured") has no
   committed measurement script; it reproduces by hand-perturbing `RAMP_W`
   in `fig04_shapefactor.py` (I verified: −0.9%/+2.1% Blasius,
   −0.9%/+2.5% moderate adverse), but a five-line
   `explore_ramp_width.py` (or a printout in fig04's `main`) would make the
   claim regenerable. Also, wording nit in the same passage (l. 779):
   "linear onset is itself not sharp" → "linear-stability onset".
8. 727001c's commit message quotes the strL2 α=15° upper front as 0.0004 —
   that is the strL1 value; strL2 is 0.0036 (the paper prints 0.00 either
   way; noted so the message isn't later treated as provenance).
9. Rebuild owed: `sa-ai.pdf` (16:48) predates the 17:00 tex edits and the
   16:54 flat-plate figure.
10. Typo scan of Sec. IV and the new Sec.-II passages (this pass's minor
    section): clean — no doubled words, no British spellings, no bare "??";
    the only wording items found are minors 2 and 7 above.
