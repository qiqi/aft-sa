# Response to review pass 20 (2026-07-24-2251)

Addressed in the commit accompanying this record. The deletion audit
was the right instrument for this round — both casualties were real.

## Majors
1. (cavL1 15° non-convergence lost) RESTORED in the fig:nlfaft caption:
   "the cavity-L1 15° lower front reached the iteration budget still
   wandering over 0.65–0.67c (the final batch's value is plotted; the
   one non-converged case of the twenty-four)." Chosen over restoring
   the old parenthetical since the figure is now where the case is
   visible.
2. (polar caption vs AFT curves) FIXED: the caption now describes both
   gray AFT (OVERFLOW) polars with provenance (same digitized source as
   fig:nlfaft) and the cd=0.025 source-axis truncation.
3. (XFOIL claim) SCOPED per your suggested wording: "FlexFoil converges
   at the identical conditions (Cl=1.51 at 9°, consistent with the
   present RANS), as does XFOIL in the N_crit sweep of Table 5." The
   annotation's intent (XFOIL also succeeds where mfoil fails) is
   preserved without implying an artifact we don't have; the packaged
   xfoil SIGFPE reproduction is noted.

## Minors
1. (adversarial-mesh payoff) → closing sentence added: "Figure 6 shows
   the result: at L1 and L2 the cavity markers land on the O-grid
   markers on every front."
2. (uncommented L0 outliers) → caption clause: "the isolated
   coarse-grid points at incidence are under-resolved-front artifacts
   (cf. the L0 discussion of Fig. 8)." The regen guard stays (it is
   future-proofing, not load-bearing).
3. (README row) → added for regen_nlf_aft_comparison.py.
4. (whitespace) → collapsed.
5. (surface-splitting note) → author-call acknowledged: it lives in the
   regen scripts by design; the paper no longer carries the repro
   detail.

Open ledger: P1-18c; invariant-kernel 2D verification still GPU-gated
behind the cavity-L2 campaign.
