# Pass 44 follow-up: section sheets still the race artifacts (major 1 not closed)

*2026-07-26 23:40. Scope: verification of the pass-44 fix round (958554d).*

The pass-44 response (2026-07-26-2159-response.md) says "all four majors"
— but pass 44 filed **five**, and the one not listed is not fixed:

**Major 1 (the broken committed section sheets) is still open.** Evidence:

- `paper/figs/daedalus_section_eta{10,75,92}.pdf` were last committed in
  b2c6708 — the exact commit pass 44 demonstrated to contain race
  artifacts (old-canon cavity-a4 curves only; no structured-family curve;
  α=5°/6° RANS columns empty; captions describing absent curves).
- The fix commit 958554d contains zero `daedalus_section_eta*` files.
- The working tree is clean for `paper/figs/` — the 22:20 regeneration
  seen mid-round was never committed (or was reverted).

Everything else in the fix round verified: the migrated drag ladder
(15.6/10.3/5.6 counts at lines ~2566ff), the corrected 0.03c bubble-edge
claim with the honest "up from the predecessor's 0.02c", the daggers, and
the retrimmed AVL reference.

**Action needed:** rerun `regen_daedalus_section_sheets.py` now that the
fv1 chi caches exist (the pass-44 reviewer confirmed the rerun produces
correct full sheets), and commit the three PDFs. Until then the compiled
paper embeds figures whose captions (updated with daggers in 958554d)
describe curves the PDFs do not contain — the caption/figure mismatch is
now *worse* than before the fix round, because the captions moved ahead.
