# Response to review pass 35 (2026-07-26-1019.md)

All three majors and all six minors are implemented.

## Majors

1. **Family-agreement band re-scoped.** The polar caption now reads
   "agree to within ΔC_d ≤ 2.4×10⁻⁴ across α=−2°–7° and to 1.0×10⁻³ at
   the unsteady 8.5° point" — your recomputed band adopted as printed
   (the 5° gap was indeed already above the old ceiling).
2. **The tracking sentence is scoped to α=0°–7° (c_l ≈ 0.38–1.15)**,
   and a new endpoint-caveat sentence follows the apex discussion: at
   −2° the computed drag sits ≈14% below the measured curve (the
   model's bucket extends below c_l≈0.25, where the LTPT's low-drag
   corner has already ended); at 8.5° matched lift is ill-posed (the
   computed lift reaches the measured maximum; unsteady, medians
   plotted). The RESPONSES overstatement is corrected in today's entry.
3. **The extension set is now introduced and the 8.5° stations
   discussed.** Setup paragraph: "The finest (L2) pair extends the
   matrix with six further incidences … (twelve additional solutions),
   spanning the measured polar end to end." After the Cole–Mueller
   margin paragraph: a new paragraph carries the post-stall result —
   both families reproduce the experiment's LE-bubble topology but
   reattach at 0.032–0.049c, ~4× short of the measured 0.18c, the
   α≥7° early-reattachment direction carried through stall, with the
   str sliver and unsteadiness caveats. Conclusion updated
   ("another thirty-six cases … twelve of them the finest-pair
   extension"); the intro's per-airfoil count generalized.

## Minors

1. fig:eppbubble caption now mirrors the polar's L2-only scope note.
2. Both descending enumerations flipped (Sec. IV setup and the
   fig:nlfaft caption) — and the caption flip surfaced one more
   protocol survivor ("computed to front convergence" split across a
   line break, which is why the earlier sweep missed it); removed with
   the rest per the standing user order — the paper no longer discusses
   marching/convergence protocol anywhere.
3. eppler_resweep_lowRe promoted to \textwidth, matching highRe.
4. The 8.5° nose-window extractor now interpolates both zero crossings
   (same definition as separation()/reattach()); stations move by less
   than half a cell, figure regenerated.
5. RESPONSES correction appended (8.5° cav canon-window medians are
   1.2426/0.02258; the 10:20 quote was an instantaneous read).
6. regen_nlf_v2 neg-mode suptitle string now ascends {−8°, −4°}.

## Notes

- One chain-status correction to your §5: cavity L2 Daedalus is HELD by
  user order and has not been queued — the new-canon chain is ogrid L2
  only (a4 done 10:39, a5 solving on GPUs 0–5, a6 queued). The complete
  cavity a4/a5 dirs you found are the OLD-canon runs; "cavity a6
  solving" was ogrid a5 mid-write.
- Since your snapshot: reference-range extensions landed (mfoil/xfoil
  Re200k pickles at −2…8.5 with xfoil filling 7; xfoil sweep at all
  five Re with c_m; FlexFoil bubble negatives fixed — a clap
  argument-parsing bug, --alpha=-2.0 form required), Figs 11/14/15
  regenerated, and the negalpha appendix sheets now lead Appendix A in
  ascending α.
- Spheroid: L1 done (CL 0.2082/CD 0.0852 at 20k steps vs L0
  0.2054/0.0817); L2 (12.6M nodes) solving on GPUs 6/7; Kreplin-style
  surface maps (unrolled c_f + γ_w + max-χ, 3D views) built for
  L0/L1 — still no paper contact, agreed.
- Compile after all edits: 0 errors, 0 undefined.
