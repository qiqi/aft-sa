# Response to review pass 13 (2026-07-24-1401)

All items addressed; commit 8ba3932 on paper-composite-snapshot.

## Major 1 (chi sheets missing from git)
FIXED at the root: the blanket `figs/` ignore in `paper/.gitignore` is
gone (it caused P11-M2, P12-M7, and this major — three passes in a
row). `figs/` is now tracked; only `figs_archive_*/` and
`figs_pre_recalib/` stay ignored. All eight
`chi_sheet_eppler387_Re{60k,100k,300k,460k}_{upper,lower}.pdf`
committed. Future new figures fail loudly (untracked in `git status`)
instead of silently vanishing from the repo.

## Minors
1. (shared-JSON clobber + missing cav entries) — Both
   `run_fork_extensions.py` and `run_dn_extensions.py` now
   read-merge-write `fork_extension_results.json` before each dump.
   All six extension entries re-emitted by recomputation from the case
   dirs, including the two missing cav entries:
   ext_fork_cavL2 CL 0.8598 / CD 0.03405 (drift 8.8e-5),
   ext_up_cavL2 CL 0.8592 / CD 0.03412 (drift 7.7e-5).
2. (slope ×10) — `history_diag` factor corrected `a*100*10` → `a*100`
   (rows log every 10 iterations; slope/row × 100 = per-1000-it). The
   JSON re-emit in minor 1 used the fixed function, so all stored
   slopes carry the corrected scale (e.g. ext_up_strL2 −3.7e-5/1000it).
3. (tab:eppfork cold cav 0.859 vs tab:eppresweep 0.860) — tab:eppfork
   cell corrected to 0.860 (the forces() median rounds to 0.8598).
4. (convergence-iterations claim) — "4–6×10⁴" → "3–4.5×10⁴ iterations
   (rolling-2000-iteration range below 10⁻³)", stating the criterion
   the number comes from.
5. (caption wording) — "returns to the cold (burst) state" →
   "collapses onto the burst branch" in the tab:eppfork caption
   (cold-start ≠ burst-branch identity is exactly the distinction the
   fork table exists to make).
6. (regen_resweep_table warm row) — the script no longer emits the
   "$100$k warm" row; a comment records why (20k-step transients,
   superseded by tab:eppfork's 60k-step histories). Script output and
   tab:eppresweep now agree row-for-row.
7. (tau-test repro chain) — `run_tau_test.py` parameterized over Re:
   it now constructs both `tautest_strL2_Re100k_a5_tau1` and
   `tautest_strL2_Re200k_a5_tau1`, with the falsification outcome
   recorded in the docstring.
8. (response-record nit) — 0930 response amended in place: the
   0.012–0.018 rms lives in the record and script output; Sec. V.A
   states the coincidence qualitatively and quotes only the 0.06 / 4×
   branch numbers.
9. (stale docstring framing) — `run_continuation_ladders.py` up-ladder
   docstring now carries the outcome line (monostable; the runs are the
   initialization-independence evidence, not a bistable band).
10. (typo scan) — clean per the review; no action.

PDF rebuilt: 0 errors, 0 undefined references.
