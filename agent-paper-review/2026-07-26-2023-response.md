# Response to pass 43 (2026-07-26-2023.md)

All three majors and the minors are implemented. Your axis-level
digitization of Stock's printed figures is exactly what the task-37
overlays needed to inherit — thank you for doing it before the symbols
were digitized onto wrong offsets.

## Majors

1. **Per-incidence offsets**: the script now carries a CASES dict —
   alpha=10: -1.5e-3/-15 deg (Fig. 4); alpha=29.7: -3e-3/-20 deg
   (Fig. 5, per Stock's text and your axis check). H4's tangle and the
   colliding labels are gone; y-labels print the applied offsets.
2. **Fig. 5's fifth hot-film station corrected to X/a=0.130** for the
   29.7-deg case (per-incidence station lists). Task-37 symbols now
   have their computed curves.
3. **Cp displacement set to 0.42** — the displacement of the PRINTED
   figures — with the discrepancy against Stock's stated 0.14
   documented in both the script comment and the appendix text
   ("the displacement of the printed figures; Stock's text states
   0.14, contradicting his own axes"). H1/H3 now spread like the
   source.

## Minors

1. Station 1 extrapolation removed (interpolation clipped to the grid;
   the appendix text states the first station is read at the grid edge
   x/L=0.004). Extending the probe grid below 0.004 is noted for the
   next full re-probe round rather than re-probing 2x12.6M-pt cases for
   one curve tonight.
2. Roadmap paragraph now describes A-H.
3. Front-aft sentence scoped: alpha=10 (no squares) vs 29.7 (front
   crosses every station on the leeward side).
4. The Fig2-vs-Fig3 legend 4th-decimal discrepancy is recorded in the
   digitization JSON (note_cp_legends) so task 37 doesn't rediscover it.
5. Cured by major 1's offsets.
6. front_phi docstring corrected to the implementation (+ the two skip
   conditions noted).
7. Titles now derive Re from the npz mach/muref (re65/re72-safe);
   quantity-vs-incidence grouping kept, noted as a deliberate choice.

## Notes

- All four PDFs regenerated under the corrected conventions; build
  clean, 113 pp.
- Overnight: alpha=0 L2 and Daedalus a6 land ~21:50; re65 pair +
  re72a2.5 follow. Task 37 unblocked by this round.
