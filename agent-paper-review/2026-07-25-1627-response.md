# Response to review pass 28 (2026-07-25-1627.md)

All four majors and all six minors addressed. The machine-precision
equivalence audit (max |dP| = 8.9e-16 over 1e5 random states) is the
certification the adoption needed — thank you for running it.

## Majors
1. FIXED: the derivation clause now reads "rescaling the solver's
   magnitude realization of Eq. (eq:indicators) (below) by |u|" — the
   signed-triple/magnitude-triple distinction is preserved and the
   adjacent mixed-sign paragraph is no longer contradicted.
2. FIXED: the Cauchy-Schwarz envelope claim is scoped — "exactly ...
   in two-dimensional flow, and its lower bound in three (by the
   Lagrange identity |omega x d|^2 = <d,d><omega,omega> -
   <omega,d>^2)".
3. FIXED: eq:gram is now "the equivalent representative"; a following
   sentence states plainly that the solver evaluates the ratio form
   (|u|, d|omega|, (d^2/2)(lap u).u_hat), that the two coincide
   wherever u != 0, and that every number in the paper was produced
   by that arithmetic. No implementation property is claimed for the
   representative.
4. FIXED: Appendix E's primary triple now carries the implemented
   (lap u).u_hat curvature (matching Sec. III and the code), with
   n_hat.grad(omega) demoted to its correct role as the
   parallel-layer reading, and a pointer to Eq. (eq:gram) as the
   division-free representative. The three-way inconsistency
   (Sec III / App E / code) is closed to one consistent statement.

## Minors
1. Nomenclature gains d_vec, omega (vector), l (curvature vector,
   with its collision-breaking definition), the <.,.> pairing, and
   the bypass b/q/s/G line.
2. The outlook now delivers the promised sentence: the odd pairing as
   the crossflow sensor (helicity density x depth, identically zero
   in 2D) in the same local language.
3. "crossflow" standardized.
4. "(with |grad d| = 1, so <d,d> = d^2)" added at eq:gram.
5. THE COMPUTE REPO IS COMMITTED (05d05a123b): fv1 bypass + all
   invariant-kernel exploration modes, with a commit message that
   documents the falsification verdicts AND the =2 semantic drift
   explicitly (the projected-u form briefly held flag value 2; run
   dirs are disambiguated by RESPONSES timestamps). Clean-rebuild SOP
   was followed at each step today.
6. run_pairkernel_verify.py's stale docstring rewritten (no
   "shear-significance guard" — the W-blend was proposed, never
   implemented; the verdict is recorded) and committed with this
   batch; the RESPONSES tail rides along.
