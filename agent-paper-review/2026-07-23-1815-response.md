# Response to review passes 8 (`2026-07-23-1600.md`) and 9 (`2026-07-23-1711.md`)
*2026-07-23, ~18:15. CONTEXT FOR THE OPEN ITEMS: pass 9 correctly found the
Eppler half of the refresh missing — and its cavity-side numbers were about
to move again anyway. We discovered this afternoon that the case tree's
cavity meshes, rebuilt this morning with a freshly compiled mesh2d, are a
DIFFERENT (7–8% coarser) triangulation than the paper's Table 4/7 meshes
(counts 27.8k/98k/354k elements vs the printed 29.9k/105k/386k), which is
what degraded the cross-family drag agreement pass 9 measured (NLF L2
1.9–7.7×10⁻³). The author chose to restore the archived original meshes
(rsync from the archive machine; the restored L2 mesh matches Table 4's
counts exactly, 386,952 3D nodes = 2×193,476), and all 36 cavity cases are
re-running on them now, Eppler first. Everything cavity-dependent (majors
1–2, 4–5, 7 and the cross-family caption claims) will be finished from
those runs; everything mesh-independent is addressed below.*

## Pass 9 majors

1. (Eppler tables/prose behind the refresh) — IN FLIGHT: gated on the
   cavity restore above; `tab:eppxtr`, `tab:eppresweep`, the polar
   ΔC_d caption, the reattachment prose, and "laminar to the trailing
   edge" will be re-printed from the restored-mesh runs. Already fixed
   now (structured-side, final): the bucket-tracking sentence
   (−1/+1/−3% at the edges, +11% at α=5°, computed at matched c_l
   against the digitized LTPT polar).
2. (Re-sweep narrative, 10⁵ physics) — IN FLIGHT for the cavity rows;
   ACKNOWLEDGED that the 10⁵ non-reattachment shows on the structured
   grid too (strL2 single crossing at 0.999c), so the section rewrite
   will own the changed physics regardless of the mesh restore. Full
   sweep-text rewrite (L0–L2 suite, "All eight" → 24, recovery
   stations re-read) comes with the restored-mesh table pass.
3. (turbulent-baseline percentages) — ADDRESSED: both polar captions and
   the NLF body now quote the committed median-extracted baselines
   (NLF +142/+147% in the bucket, +64/+71% at 9–15°; Eppler +66–87% in
   the bucket, +149% at 7°).
4. (NLF cross-family drag claim ~3× too tight) — DEFERRED to the
   restored-mesh numbers: the 1.9–7.7×10⁻³ spreads pass 9 measured were
   the coarse rebuilt-cavity artifact; re-quoted after the re-runs.
5. ("coarser levels scattering by up to 0.03c" falsified) — DEFERRED
   likewise (the cavL1 α=0° lower outlier at 0.356c was measured on the
   rebuilt mesh; re-checked after the re-runs, kept-or-footnoted then).
6. (XFOIL→FlexFoil attribution) — ADDRESSED, all five sites: NLF polar
   caption (diamonds now "FlexFoil's"), body C_l=1.51 sentence, the
   fig:nlfcfhigh caption (both overlays FlexFoil's), the "reference e^9
   solver" sentence, and the Eppler α=7° overlay caption (FlexFoil
   surfaces; XFOIL's forces agree, C_l=1.17). The ∂x_tr/∂N column is
   re-attributed as "the e^9 reference re-converged … computed with
   XFOIL at the original study's conditions" (historical provenance
   stated rather than implied reproducible). The tab:eppresweep
   footnote's 0.62c/0.52c story is reconciled: mfoil and FlexFoil both
   read 0.62c; the 0.52c is identified as an XFOIL reading from the
   original study.
7. (Conclusion recap) — DEFERRED to the restored-mesh table pass (same
   sweep, one commit).
8. (H≤5 cap vs Fig. 4's H≈10.6) — ADDRESSED as recommended: the cap is
   re-scoped to "the frozen-profile comparisons of this and the
   following table", and the Green/tanh OS instrument is now wired in
   (with new refs Dini–Selig–Maughmer 1992 and Green 1966): where the
   deeper branch appears (Fig. 4's march, Sec. VI), the OS rates on the
   measurement-supported tanh family saturate with depth and side with
   the kernel — at H≈13 the kernel's inviscid limit matches OS while
   the extrapolated correlation is ~2× above both. This also closes
   pass-8 minor 6's disclosure residue.

## Pass 9 minors

1. (fig:nlfcflow caption fronts) — ADDRESSED: 0.38–0.39 upper, 0.25 at 4°.
2. ("one and a third of a unit") — ADDRESSED: "one unit and a third of a
   unit, respectively".
3. (constants w rationale) — ADDRESSED: "not fitted---the marched
   stations are insensitive (Sec. II.D)".
4. (flatplate PREFIX default) — ADDRESSED: defaults to flatplate_sphere.
5. (regen_resweep_table mfoil crash) — ADDRESSED: guarded (NaN row +
   note; the 460k divergence is documented behavior).
6. (wire the Green/tanh instrument) — ADDRESSED with major 8.
7. (ramp-width measurement script) — ADDRESSED:
   repro/analytic/explore_ramp_width.py; "linear-stability onset" fixed.
8. (commit-message strL2/strL1 provenance) — noted for the record here:
   the 0.0004 was indeed strL1; strL2 is 0.0036.
9. (rebuild owed) — done with this round (bibtex included for the two
   new references): 0 errors, 0 undefined refs.
10. (typo scan clean) — no action.

## Pass 8 minors

1. (Re_θ0 vs Re_θc split) — ADDRESSED: all four Re_θc sites unified to
   Re_θ0 (including the graze figure's y-label, regenerated), and
   Re_θ0(H) now has a nomenclature row.
2. (deep-row exclusion wording) — resolved by the H≤5 cap (per pass 9).
3. (constants c_ν,ai criterion) — ADDRESSED: "at the just-past-critical
   stations".
4. (stale "text quotes" printout) — ADDRESSED.
5. (noun elision) — ADDRESSED: "just past the Blasius critical station".
6. (deleted high-Re caveat, record) — the Green/tanh wiring of major 8
   restores a quantitative deep-H disclosure at the Table-2 cap.

## Pass 7 minors (as carried in pass 8's list)

- P7-m1 (abstract "and" + antecedent) — ADDRESSED: "…and the Blasius
  $e^N$ envelope; the freestream seed is set by Mack's receptivity
  correlation."
- P7-m2 (u^{-1/2} before the floor) — ADDRESSED: BCs and floor now
  stated first, the substitution last.
- P7-m3 ("All eight solutions") — with majors 1–2, post-restore.

## Long-standing

Unchanged: P1-1/P1-2 (Daedalus predecessor kernel; pass 9's sharpening
of P1-1 — the disclosure must also name the onset-constant difference —
accepted and queued with that rewrite), P1-7, P1-17 remainder + tie
back-reaction, P1-18a, P1-18c, P1-m13 (Fig. 11d digitization now
committed in-repo; source-PDF check still owed), P1-m17.

## Addendum (2026-07-24): the sweep narrative became its own section
The Reynolds sweep (with the L0-L2 suite, the restored-mesh numbers, and
the 1e5 bursting result) is now a standalone section before the Daedalus
wing, expanded with: a forces-vs-Re figure (fig:eppresweepforces, the
refinement fan closing at 2-4.6e5 and opening at 1e5), an initialization-
fork study demonstrating the model's bistable band sits one Reynolds step
above the experimentally bistable 6e4 (tab:eppfork, TM-4062 Fig. 11a), and
a quantitative handover-length analysis (tab:epphandover) attributing the
raised bursting boundary to the deliberately unmodeled transition zone --
closing the loop with Sec. II.E's own caveat sentence, which now carries a
forward pointer. Conclusion updated accordingly (finding + the
transition-zone closure as a named future-work item).
