# Response to review pass 18 (2026-07-24-2110)

Addressed in commit cbec5f7. All four majors were right, and major 1
was the important catch of the pass — thank you for measuring rather
than reading.

## Majors
1. (cancellation clause falsified by the data) FIXED as you prescribed:
   the sentence now states the measured head start — "the re-seeded
   climb fires the χ=1 front ~0.16c ahead of the physical envelope's
   equivalent station (0.41 vs 0.57c at 1e5, 0.44 vs 0.60c at 6e4,
   against the mfoil envelope at N = ln(1/χ∞)), a head start that the
   over-long handover then spends — and, below the boundary,
   overspends." You are right that the old clause undermined the
   paragraph's own logic: the cancellation exists precisely because the
   front is early.
2. (production bound exceeded 40× at 1e5) FIXED: "median rate below
   10⁻⁷ a_max at every swept Re, and four orders below the free-shear
   ceiling even at the 95th percentile"; the script docstring's stale
   1e-10 is now 1e-8 (absolute rate units).
3. ("predict the bubble" vs "never closes") FIXED: "the model still
   places separation and the transition front, and lands within 2–12%
   (family-dependently) on lift — but by compensation..." — the
   load-bearing sentence no longer claims the bubble.
4. (Daedalus re-badge) FIXED: the early wing front is re-attributed to
   the seed convention with the restored 11.7-vs-13.6 e-fold
   arithmetic, explicitly noting the re-seeding measurement is quiet at
   these Re; only the commit half (eddy viscosity taking the remainder
   of the bubble vs e^N's instant commit) is kept as shared with
   Sec. epphandover.

## Minors
1. (stale "dotted reference") → row-2 description now says log axis
   spanning the amplifying decades, suppressed/favorable stations
   falling below the plotted range.
2. (missing 2e5 point) → added to `measure_lsb_reseeding.py` with the
   non-sweep directory special-cased; the committed table now prints
   front 0.09× / rear 58.10× / rate 0 at 200k — matching your
   independent run digit-for-digit and bracketing the flip at the
   bursting boundary.
3. (monotonicity inverted by 6e4) → "below the bursting boundary the
   amplification the model books inside the bubble outpaces the e^N
   envelope" (monotonicity dropped).
4. (0.08 vs 0.09 Re_τ; Re_τ undefined) → 0.09 per the committed
   script, with "(the friction-velocity Reynolds number)" defined in
   place. The Dirichlet far-field variant stays uncommitted session
   work.
5. (Re_θ scope) → "at the 6×10⁴ bubble shear layer's Re_θ ≈ 220–330",
   with "would equilibrate" since the 6e4 bubble in fact never
   reattaches.
6. (median-vs-peak asymmetry) → convention now stated in the text
   ("compares the reversed region's median χ against the incoming
   envelope's peak, conservative for the low-Re claims").
7. (×/\!\times\! house style) → all four sites fixed.
8. (README rows) → added for `measure_lsb_reseeding.py` and
   `sa_sustain.py`.
9. (working-tree hygiene) → the 28 side-effect figure regenerations
   committed (they carry the new row-2/e^N conventions consistently);
   `lsb_zoom_sheets.py` and the two `figs_explore/lsb_zoom_*.png`
   tracked.
10. (typo scan clean) — no action.

On the abstract note (not counted): left as is for now — the bursting
boundary is the handover's doing, as you say — flagged to the author.

Open ledger: P1-17, P1-18c.
