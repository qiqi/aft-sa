# Response to review pass 15 (2026-07-24-1833)

Addressed in commits 53d0a40 (which crossed the review in flight) and
a2f5b44. One superseding event the review predates: **the AVL+XFOIL
reference itself carried a bug** — `run_avl` grepped the near-field
`CDind` (0.0121 at α=4°, an implied e=0.66) instead of the Trefftz
`CDff` (0.0092, e=0.87) that Appendix D always claimed. Fixed in
53d0a40: the reference CDs are now 0.02028/0.02212/0.02414, RANS drag
lands within ±3 counts (1.5%) at L2, and the drag narrative the pass
verified ("30–36 below", "40–50 predecessor") is rewritten
("on the reference"; predecessor 11–17 below). The near-field value is
confirmed the fragile one: the cached 24×80 lattice run moves CDff by
0.2% and CLff by 0.04% while its near-field totals NaN — now stated in
Appendix D.

## Majors
1. (Appendix D predecessor clause) FIXED: the ledger is scoped to "the
   predecessor-kernel campaign (the final-kernel recomputation of
   Sec. daedalus supersedes those solutions but not this diagnostic,
   which is a property of the ungated rate)"; the profile extraction is
   labeled "the predecessor-kernel campaign's structured L2 ... that
   kernel's whole bubble; the final-kernel bubble at the same station
   spans 0.47–0.65c". The anchor-condition sentence at l. 3332 already
   reads historically ("the anchor condition of the *predecessor*
   kernel").
2. (untracked repro chain) FIXED: force-added `avl_compare.py`,
   `run_solution.py`, `section_chi_diag.py`, the three `dae*_selig.dat`
   sections, and the full `daedalus/avl/` text cache (0.4 MB; the AVL
   binary lived in a dead session scratchpad, so the cached outputs are
   the reference — `run_avl` reuses them and only invokes the binary
   when they are absent).
3. (availability-filter race) FIXED before the review landed, in
   53d0a40: `complete()` now requires a full 20k-step force history
   (≥2001 rows), which is exactly the condition the running a6-L2 case
   violated when it crashed my own regen. The committed figure was
   regenerated under the tightened gate (with the corrected AVL
   reference).

## Minors
1. (jargon) "Q1-only gate" → "a shaped-rate variant whose onset
   threshold lacked the in-bubble B/⟨Ŝg⟩² arm of Eq. (reomc)"; "second
   gate pinch" → "the in-bubble arm of the final onset threshold (the
   B/⟨Ŝg⟩² branch ... which suppresses the rate while the separated
   layer is still wall-damped)".
2. (0.01c over-claim) → "0.02c on both edges".
3. (y+ caption) → all y+ rows attributed to the predecessor-kernel
   campaign's α=5° solutions, noted as a mesh-resolution property
   insensitive to the kernel revision. (No recompute; no false match
   claim.)
4. (Daedalus provenance) FIXED: `ai_constants.log` extracted from the
   solver echo for all 8 completed cases and force-added, plus
   `daedalus/canon_results.json` (totals + η=0.31 front/bubble per case
   + the e^N strip references) — same convention as 2d4c7cd.
5. (same-fronts evidence) → the sentence now carries it: "its χ=1
   contour sat the same ~0.1c ahead of the strips' N_crit contour".
6. (surface-maps docstring) → rewritten to match the availability-
   dependent Nx3 layout and the three actual columns.

## Also new since the pass (author-driven, for pass 16's scope)
- Blockage quantified in-text: classical wall-interference for the
  O-grid's 2.7-span cylinder gives ΔCL/CL ~ 0.06% — the 0.7% family
  gap is discretization, not confinement.
- Annotated-pdf round (53d0a40): Sec. II.E restructured — calibration
  contact split into its own subsection (high-Re untouched → inner-layer
  linearity argument with round-off scheme-independence verified on two
  discretizations → c_ν,ai QED), τ discussion folded into the blend
  paragraph, coarse-grid table dropped, assembled model moved to
  Appendix E as bare reference. Fresh surface for review.
