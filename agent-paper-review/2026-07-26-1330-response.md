# Response to pass 38 (2026-07-26-1330.md) — repro-audit round 1

The actionable core is done this round; the deeper refactors are queued
with owners below.

## Correctness

1. **resweep e^9 column fixed and verified**: the branch now requires a
   finite converged mfoil entry AND excludes {60,460} (the table's own
   footnote convention); xfoil rows hard-fail rather than NaN (also:
   xvfb-run dependency dropped — plain xfoil with PLOP G F, the same
   invocation the pickles script uses). Rerun reproduces the tex rows
   digit-for-digit at all five Re, including 60k (0.869/0.0411/-0.102)
   and 460k (0.966/0.0086/-0.082). Docstring rewritten (L2 default,
   dissolved-table note).
2. **constants_report.py rewritten around the sphere-kernel canon**:
   imports driver/saai_env._SPHERE, asserts the paper's Appendix-E block
   (a_max, k*{2600,175,2}, w, tau, c_nu_ai=1/6, Mack map, c_v1); the v2
   report is git history. verify_three_anchors is retired from the
   analytic suite with the other v2-record scripts (see Consistency 2).
3. **c_nu_ai = 1/6 in the three II.E scripts** (local override with a
   comment; lib default untouched pending its migration). Reruns: the
   tie stays exact (dB = +0.0e+00, linearity 3e-16) and no printed
   II.E digit moves (the round-off claim is c_nu_ai-independent, as you
   predicted; yplus table digits shift only in retired-float columns).
   floor_backreaction_table no longer imports K_R.
4. **Crossing guards added** in onset_vs_ags (assert crossing exists +
   interior; C_V1 import used instead of the 7.1 literal).
5. **regen_nlf_aft_comparison hardened**: neg-results file asserted
   (part of the committed canon), and cl_of_alpha asserts the requested
   alpha lies inside the computed lift-curve nodes instead of clamping.

## Consistency

1. **regenerate_cfd.SCRIPTS rebuilt to the current figure inventory**
   (15 entries): adds nlf_aft_comparison, epp_bubble_figure,
   epp_resweep_forces, negalpha_sheets, epp_ext_sheets,
   daedalus_section_sheets; L1compare -> resweep_suite; l0_artifact
   dropped (diagnostic); float notes updated to the dissolved-table
   reality.
2. **regenerate_all.SCRIPTS rebuilt**: fig01_indicator_sphere,
   fig02_onset_graze, fig02_model_calibrate in; indicator_plane,
   kernel_maps, klambda, fit_fpg_rate_slope, verify_three_anchors out
   (annotated as historical v2 records). tab02_yplus kept as a prose
   verifier with its float noted as dropped.
3. **The six stale paper/-level duplicates are deleted** (git rm:
   regen_eppler_v2, regen_epp_reattach, regen_nlf_polar,
   regen_resweep_table, regen_nlf_transition, regen_l0_artifact) and
   ONBOARDING §5's generator map now points at repro/cfd.
4. README: the wrongest lines fixed (root default, deleted script name,
   the two count claims). The full reading-order float-table regeneration
   against the current tex is QUEUED (it deserves its own careful pass).
5. cases.py header: flow360_g4 -> flow360_fv1 (7 refs) + script path.
6. Table-generator docstrings updated (resweep done; nlf_transition and
   epp_reattach queued with the README pass).
7. Flat-plate canon note replaced ("default root is the fv1 canon").
8. Test SKIP re-scope: queued with the lib migration.

## Style

- Committed junk removed (pycache, .bak, texput.log, figs_explore,
  :00.bl, lambda_v scan outputs) + .gitignore extended.
- The bigger refactors — panel-count assertions (your shared
  convention), quarantining the v2 landscape machinery out of
  regen_eppler_v2, lib.forces.median_tail deduplication, single
  __main__ in the flat-plate script, _saai os.chdir removal,
  fig04 0.708 comment digit — are ACCEPTED and queued as a follow-up
  batch; they touch many files and deserve their own verified round
  rather than a tail-end of this one.

## Note

regenerate_all's full-suite run exceeded this round's time budget
mid-audit (fig03 is slow); constants_report, wall-layer tie, tab02, and
the resweep table were run individually and pass. New user orders landed
mid-round (OpenFOAM figure integration + appendix data tables), which
take the next work slots; the queued items above follow.
