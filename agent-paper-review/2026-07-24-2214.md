# Review pass 19 of `paper/sa-ai.tex` — P1-17 closure check, Daedalus a6-L2 completion, Sec. III trims

*Reviewed 2026-07-24, ~22:14–23:10, against HEAD 269a19b. Scope as
tasked: f5142bf (floor back-reaction on the march instrument, the
P1-17 candidate), 269a19b (Daedalus a6-L2 landed), 3890a00 (Sec. III
trims + Re_θ by integration) — plus two small structural commits in
the same window folded in and scanned: 04a75c4 (II.F retitle) and
e4dd7cf (back-reaction paragraph II.F→II.E; it fixes l. 899's "same
section" pointer, which now correctly lands in sec:blend). The
pass-18 fix round (cbec5f7) treated as closed per task. Verification:
`floor_backreaction_table.py` **run in full** (all 9 wedge×seed rows,
~23 s); `ONSET_DIAG=1 regen_flatplate_flow360.py` **run** (all five
Tu rows, both crossings, plus the retired 0.664√Re_x conversion for
comparison); `canon_front_metrics.py` **run** (all 12 rows incl. the
new a6 set); Daedalus totals recomputed from the five most-relevant
`total_forces_v2.csv` (final-500-step window) incl. both new a6-L2
cells; `avl_fixed_cl_reference.py` **run** (all three matched-lift
rows reproduce from the committed AVL cache, trims 4.527/5.547/6.555°);
`regen_daedalus_polar_sectional.py` **rerun with the in-progress
67-row cavity-L2-a4 case present** — no crash, no partial point, and
the output PDF is content-stream-identical to the committed one;
tex test-compiled in the scratchpad (0 errors, no undefined or
multiply-defined references); doubled-word and aspell scans of every
edited region plus Appendix D (clean). Overwritten figures restored
via `git checkout --` (flat-plate batch, polar); no paper files
edited.*

## Summary

Three genuinely good closures in one evening. f5142bf finally puts
committed, canon-current numbers under the destruction-floor
back-reaction — the review thread's oldest open measurement debt —
and every printed range reproduces digit-for-digit from the new
script; the one thing the commit does *not* do is re-derive the
<1%/−2.6% station-shift figures themselves, which the response
memo explicitly elects to keep (they "measure a different thing"),
so P1-17 closes in substance with a one-line provenance residue
carried below as minor 1. 3890a00's Re_θ-by-integration change is
the substantive honesty fix of the round: the old laminar-conversion
reading at the χ=c_v1 crossing (+14–36% vs AGS) badly understated
how far the layer had left the laminar branch, and the new statement
— integrated Re_θ ≈ 2500 nearly uniform, 2.2–3.5× AGS, transitional
growth rather than onset placement — is what the rerun instrument
actually prints, with the conclusion and caption updated in step.
269a19b lands the exact case pass 15 flagged as the availability-gate
race: the a6-L2 run passes the tightened ≥2001-row gate cleanly (2001
force rows), both new table cells and every drag/family/bubble number
recompute exactly, and the generator now demonstrably does the right
thing with a mid-solve case sitting in the directory (67 rows →
excluded, sectional falls back to cavity L1). What keeps this from
being a clean sweep is that 269a19b's refresh missed three sites that
now contradict the updated numbers: Appendix D still enumerates the
completed set as "structured family at L2, α=4°,5°", the polar
figure's caption still scopes panel (b) to "structured L2 at
α=4°,5°, otherwise L1" while the committed panel plots structured L2
at all three incidences, and the ledger paragraph still says the
matched-lift residual is "6–13 counts" three sentences after quoting
13/7/2. All three are one-line fixes. Typos: none found. Ledger
after this pass: P1-18c plus the minors below.

## Verification

### 1. P1-17 closure (f5142bf)

- ✓ **The new numbers come from a committed script at the current
  canon and reproduce exactly.**
  `repro/analytic/floor_backreaction_table.py` (committed in
  f5142bf) marches the frozen-profile instrument with the canon
  `sphere_rate` (via `fig04_shapefactor`/`_saai`) and evaluates
  D_floor/P_AI at the marched ν̃ peak. My rerun: χ=0.1 →
  2.88e-6–1.17e-5 (tex "3×10⁻⁶–1.2×10⁻⁵" ✓); χ=1 →
  2.07e-4–7.61e-4 (tex "(2–8)×10⁻⁴" ✓, "below 10⁻³ for every wedge
  and seed" ✓ — max 7.61e-4 at sep/N=7); χ=c_v1 → 8.62e-3–2.29e-2
  (tex "(0.9–2.3)×10⁻²" ✓); D/(σ_P·P_SA) at c_v1 → max 1.42e-2
  (tex "at most 1.4%" ✓, largest at the separation limit ✓).
  "Growing toward lower seeds" ✓ — monotone in N_crit at every β
  and level.
- ✓ **The floor algebra in the script matches the paper's tie**:
  floor (1+c_b2)/(σc_w1)=0.751, D = 0.751·c_w1·f_w·(χ/d)², standard
  SA r with the f_v2 correction; f_w≈0.7r small-r asymptote checks.
- ✓ **No stale <1%/−2.6% elsewhere.** The pair appears at exactly one
  site (l. 1096–1100); the only other floor-inertness quotes are
  l. 899 ("<10⁻³ of the amplification, same section") and l. 1068
  ("stays below 10⁻³ up to χ=1, quantified below") — both now
  *supported* by the committed table rather than by an unstated
  measurement, and both consistent with it.
- ✓ **Constants-list/blend-subsection story consistent.** The
  paragraph now lives in II.E (sec:blend) after e4dd7cf, so l. 899's
  "same section" pointer resolves correctly; the tie's slope/floor
  arithmetic (0.249/0.751 summing to one via c_w1) is untouched; II.F
  retitle (04a75c4) is cosmetic and its one cross-ref updated.
- △ **The <1%/−2.6% figures themselves are still the pre-rework
  march** — the new script marches *without* the floor and evaluates
  ratios post hoc; no with-floor re-march was run. The coupled-bounds
  sentence (+0.8% Re_θ, |ΔC_f| 3×10⁻⁵, forces to four digits) was
  deleted per the annotation, so those two numbers are now the
  paragraph's only claims not backed by a committed canon
  measurement. Minor 1.

### 2. Daedalus a6-L2 (269a19b)

- ✓ **The landed case passes the tightened gate**: `complete()` in
  `paper/regen_daedalus_polar_sectional.py` requires ≥2001 lines of
  `total_forces_v2.csv`; a6-L2 has 2001 force rows (2002 lines).
  The in-progress cavity-L2-a4 (67 rows at review time) is excluded,
  and my rerun of the generator with it present completed without
  the pass-15 crash and reproduced the committed PDF
  content-identically (decompressed streams byte-equal) — the race
  pass 15 demonstrated is closed in practice, not just by code
  inspection. (Trivial margin note: the line count passes at 2000
  data rows + header, ten steps short of 20k; immaterial.)
- ✓ **Tables/figures filled in: 16 → 18 CFD cells.** New cells
  recomputed from the CSVs under the final-500-step window:
  a6-L2 CL 1.2279 / CD 0.02509 ✓ exactly as printed; spot-recheck of
  a4-L2 (1.0282/0.01997), a5-L2 (1.1289/0.02243), a6-L1
  (1.2317/0.02479), cavity-a6-L1 (1.2227/0.02523) all match. Dashes
  now exactly the three pending unstructured-L2 runs; a6 surface
  figure re-added (`fig:daesurf6`, caption "As Fig. 4"), Figs. 4–6
  range refs restored at all three sites, compile has no
  undefined/multiply-defined labels.
- ✓ **Drag/family numbers at 6° recompute.** Matched-lift reference
  reproduces from the committed AVL cache: CD_ref 0.02530 at trim
  6.555° (and the "~0.5° more incidence" clause: +0.53/+0.55/+0.56°).
  Ladder: 0.01997 vs 0.02125 → −12.8 counts (−6.02%); 0.02243 vs
  0.02315 → −7.2 (−3.11%); 0.02509 vs 0.02530 → −2.1 (−0.83%) —
  the text's "13 (6.0%) / 7 (3.1%) / 2 (0.8%) on the finest grids" ✓,
  deficit shrinking with incidence ✓. Family agreement at L1 still
  holds at a6 (CL 0.73%, CD 1.77% — inside the quoted 0.6–0.8% /
  0.9–1.8%).
- ✓ **Bubble and front at 6°**: `canon_front_metrics.py` rerun —
  a6 str-L2 bubble 0.458–0.630 vs strips 0.458–0.631 (separation
  exact, reattachment 0.001c; text "0.46–0.63 against 0.46–0.63" ✓,
  and "separation to 0.02c, reattachment under 0.01c **at every
  incidence**" now true: sep Δ = 0.017/0.007/0.000, reatt Δ = 0.001
  at all three ✓). Front offset 0.617−0.522 = 0.095c, consistent
  with the standing ~0.09c convention arithmetic. Families at L1
  within 0.02c at a6 (sep 0.015, reatt 0.015) ✓.
- ✓ **Text scope updated at the main sites**: "Nine converged RANS
  solutions… both families at L1 and the structured family at L2, at
  all three incidences" (l. 2574–2576); pending set correctly reduced
  to the three unstructured-L2 runs. Provenance kept to convention:
  `case_ogrid_L2_saai_a6/ai_constants.log` committed (all nine
  completed cases now have one) and `canon_results.json` extended.
  The `.gitignore` `!paper/figs` exception is a real fix (second
  path to the pass-13 silent-figure-loss class) — with one side
  effect, minor 7.
- ✗ **Three sites missed by the refresh** — majors 1–3.

### 3. Sec. III trims + Re_θ by integration (3890a00)

- ✓ **The integration change matches what the instrument prints.**
  `ONSET_DIAG=1` rerun: χ=1 crossing integrated Re_θ vs AGS =
  +6.1 / −3.3 / −10.5 / −13.7 / −11.2% at Tu = 0.04–0.60% — the
  text's "within 14%: +6% late at the lowest, −3% at 0.08%, 10–14%
  early over 0.16–0.60%" ✓ (−11.2 inside the quoted 10–14 ✓);
  caption and Conclusion carry the same numbers ✓. χ=c_v1 crossing:
  integrated Re_θ 2486–2508 ("≈2500, nearly uniform" ✓), 2.21–3.51×
  AGS ("2.2–3.5×" ✓) — and the reframing as transitional growth
  rather than onset placement is the honest reading (the old
  +14–36% was a laminar-relation artifact). The old ±10% and
  +14/+23/+36% are purged from tex, caption, and Conclusion; no
  other site quotes them. The "+6% at lowest Tu agrees better with
  the 7% drain footprint" story is internally consistent (the drain
  clause at sec:calib_onset is a march number, untouched by this
  CFD-side change).
- ✓ **No knock-on to the N=9 / anchor-station numbers**: the 7%
  drain footprint, Drela-station arithmetic, and the airfoil
  N_crit=9 seed value (χ∞ = 8.76×10⁻⁴, quoted at l. 1437 and 1784)
  are all march- or map-side and unaffected. One cross-reference
  *is* orphaned by the trim — minor 4.
- ✓ **The trims are content-safe**: the deleted "assembled forms in
  Appendix E" clause is redundant (Appendix E still referenced at
  l. 274 and 1210); the deleted discretization paragraph and
  kernel-exactness sentence were author-directed removals with no
  other tex site depending on them (grep: no surviving reference to
  the ring-average/Laplacian discussion or "factor of three"
  spread); the new vector-calculus statement of the triple
  (∇²u = −∇×ω, sign(u)u″ on a parallel layer) is correct and
  consistent with the signed-algebra paragraph that follows;
  "χ=1→O(c_v1), several e-folds" coexists tolerably with Sec. VI's
  precise ln c_v1 ≈ 2.0 + 1.4 accounting. Figure moved to the top
  of Sec. III renders correctly (90-page compile, figure before
  sec:calib_tu's paragraph).
- △ The figure's AGS markers are still conversion-placed while the
  quoted bracket is integration-based — minor 5.

### 4. Light scan / typos

- ✓ Doubled-word scan of the full tex: clean. aspell over all four
  edited regions plus Appendix D: only proper nouns and established
  jargon. No bare refs; compile clean.

## Status of prior comments

- **P1-17** (floor/tie back-reaction re-derivation) — **closed in
  substance by f5142bf** (canon quantification committed, verified,
  and correctly cross-referenced); residual <1%/−2.6% provenance
  carried as minor 1. Off the major ledger.
- **P1-18c** (320×80 flat-plate single grid) — open, GPU-gated,
  untouched this round.
- **Pass-18 round (cbec5f7)** — closed as tasked.
- **Pass-15 major 3** (availability-gate race) — now verified closed
  in practice: gate ≥2001 rows, live in-progress case correctly
  excluded, no crash, committed figure reproduces.
- **Pass-15 minor 4** (per-case provenance) — all nine completed
  Daedalus cases now carry committed `ai_constants.log` ✓.

## New major comments

1. **Appendix D still enumerates the pre-a6-L2 completed set
   (l. 3204–3206).** "the completed set is both families at L1 and
   the structured family at L2, $\alpha\!=\!4^\circ,5^\circ$" —
   untouched by 269a19b and now contradicting the section's "Nine
   converged RANS solutions … the structured family at L2, at all
   three incidences" and the table it introduces (whose a6-L2 row is
   filled). This is the exact staleness class pass 15's major 1
   caught in the same appendix. One line: "…and the structured
   family at L2, at all three incidences."

2. **The polar figure's caption misdescribes the committed panel
   (fig:daepolar, l. 2632–2633).** Caption (b): "Sectional lift on
   the finest completed grids (structured L2 at
   $\alpha\!=\!4^\circ,5^\circ$, otherwise L1)" — but the figure
   committed in 269a19b plots structured L2 at all three incidences
   (verified by rerunning the generator: the sectional panel selects
   `case_ogrid_L2_saai_a6` at $6^\circ$, and the regenerated PDF is
   content-identical to the committed one). A reader checking the
   caption against tab:daetotals will conclude the a6 sectional
   trace is L1 when it is L2. "structured L2, unstructured L1" is
   both correct and shorter.

3. **"leaving the $6$--$13$-count residual above" (l. 2685) is stale
   against the numbers quoted three sentences earlier.** The
   matched-lift residuals "above" are now 13/7/2 counts (l. 2650–
   2652, recomputed and confirmed); the 2-count a6 point sits well
   outside "6–13". Either update to "2–13" or — if the like-for-like
   predecessor comparison underlying the "returned 6–14 counts"
   clause has no a6-L2 counterpart — scope the parenthetical to the
   cases it was measured on. As written the paragraph contradicts
   itself at the digit level.

## New minor comments

1. **P1-17 residue: the <1% anchor-shift and −2.6% favorable-family
   figures (l. 1097–1100) are still the pre-rework march**, now
   juxtaposed without provenance distinction against a table measured
   at the current canon — and the coupled-computation bound that
   previously backstopped them was deleted in the same commit. The
   response memo's "measures a different thing" is fair, but pass 5's
   original ask (re-derive, or state which anchor variant produced
   them) is now a ~10-line flag on the new committed instrument:
   add a with-floor march option to `floor_backreaction_table.py`
   and print the station shifts, or add "(measured on an earlier
   anchor variant; the ratios above bound the effect at the current
   canon)".

2. **"(the largest values at the separation limit)" (l. 1095) is
   only cleanly true of the $\sigma_P$-weighted comparison.** For
   D_floor/P_AI at $\chi\!=\!c_{v1}$ the largest printed value is
   Blasius/N=7 (2.29e-2), a hair above the separation limit's
   2.28e-2 (tied at the quoted precision). Scope the parenthetical
   to the tie comparison — "(largest at the separation limit, where
   the tied SA production is weakest)" — where sep exceeds the next
   family by 6× and the claim is unambiguous.

3. **README row missing for the new script**:
   `floor_backreaction_table.py` appears in neither
   `paper/repro/README.md` nor `repro/README.md` — the convention
   the pass-16/17/18 rounds established and that the two pass-18
   scripts received one commit earlier. One row.

4. **Orphaned cross-reference (l. 1437).** "(the
   $N_\mathrm{crit}\!=\!9$ anchor of Sec.~\ref{sec:calib_tu}; …)" —
   3890a00 deleted the calib_tu sentence that declared the single
   $N_\mathrm{crit}\!=\!9$ anchor ("redundant" per the response
   memo), so the reference now points to a section that never states
   it. Cheapest fix at l. 1437: "(the $N_\mathrm{crit}\!=\!9$
   anchor, via the map of Sec.~\ref{sec:calib_tu}; …)"; l. 1784's
   "same … anchor as the NLF" then chains correctly.

5. **fig:flatplate_batch caption: the AGS markers and the quoted
   bracket now use different conventions.** The gray verticals and
   bottom-edge symbols are still placed through the laminar
   $0.664\sqrt{Re_x}$ relation (the regen script says so explicitly:
   "the AGS markers on the figure remain placed by it"), while the
   caption's "brackets AGS within 14%" is the integrated-Re_θ
   reading; a reader measuring the panel recovers
   $+9.8/+0.6/-6.6/-9.6/-7.2\%$ instead. One clause — "markers
   placed via the laminar $Re_\theta$–$Re_x$ relation; the quoted
   residuals use the integrated $Re_\theta$ of the text" — closes
   the gap.

6. **Stale comment in `paper/regen_daedalus_polar_sectional.py`
   (l. 29–31)**: "a6 entry uses the L1 lift target until the L2 run
   lands --- rerun avl_fixed_cl_reference.py and refresh these three
   tuples then" — 269a19b refreshed the tuple but left the
   instruction that says it hasn't been. Delete the two lines (or
   re-scope them to the unstructured-L2 landing, which will change
   nothing since the targets follow the structured family).

7. **Hygiene**: (a) `paper/figs/nlf0416_cp_cf_a4.pdf` (Jun 18,
   referenced nowhere in the tex) is newly *visible* as untracked —
   surfaced by 269a19b's `!paper/figs` gitignore exception — and
   will ride into the next `git add paper/figs` unless removed or
   deliberately tracked. (b) Unchanged from prior passes: the small
   untracked `daedalus/` helpers (`*_pool.sh`, `run_canon_resume.sh`,
   `bl_height.py`, `grid_summary.py`, `make_gallery.py`,
   `ogrid_wing.py`, `plot_meshes.py`, `size_vs_dist.py`,
   `viz_*.py`) and `daedalus/avl/ft_dp_a0.txt`/`ft_dp_a2.txt`
   (extras beyond the three committed DP-evidence files).
