# Review pass 15 of `paper/sa-ai.tex` — the Daedalus final-kernel recomputation (5509ab0)

*Reviewed 2026-07-24, ~18:33. Scope: commit 5509ab0 (Sec.~daedalus +
Appendix~D recompiled onto the canon campaign) plus the pass-14-response
minor-1 fix (2d4c7cd); HEAD is 078ee96 (response record). Working copy
clean for all paper files; `sa-ai.pdf`/`.log` (18:25) current with the
tex, 0 errors, 0 undefined references. Verification: the full tex delta
5509ab0^..5509ab0 diffed hunk-by-hunk; `daedalus/canon_front_metrics.py`
**run** (all 11 completed-case/strip rows) and extended to the outboard
stations; `tab:daetotals` recomputed cell-for-cell from the case
`total_forces_v2.csv` under the generator's final-500-pseudo-step
window; every drag/lift ratio in the text recomputed from the new and
old tables; the $N_\mathrm{crit}\!\approx\!13.6$ seed map recomputed
through `repro/driver/saai_env.py`; per-case kernel provenance read from
the solver-stdout resolved-constants echoes; both regen generators read
and `regen_daedalus_polar_sectional.py` **rerun** (it crashes — below);
figure-PDF creation stamps vs. the in-progress a6-L2 run's file birth
times compared. No paper files edited.*

## Summary

**This commit resolves the review thread's oldest open item.** Since
pass 1, P1-1/P1-2 have stood against the 3D section: results from an
undefined predecessor kernel ($Q_1$/$Q_2$ jargon), an unsupported
$0.02\,c$ cross-reference, and later a "sole modeling difference"
disclosure that aged badly as the 2D model evolved. The section and
Appendix~D are now compiled onto the final-kernel canon campaign, and
the recomputation is the right kind of vindication: the compressed
bubble the predecessor produced ($0.04$–$0.06\,c$) opens to the
strips' $\sim\!0.2\,c$, separation/reattachment land within
$0.02\,c$/$0.01\,c$ of the $e^N$ reference, and $6$–$14$ counts of
missing bubble drag return to the polar. **Every headline number in
the new text checks against the committed data and scripts** — I
reran the front-metrics script and reproduced the totals table
cell-for-cell — and the partial-campaign honesty is genuine: the five
pending runs appear in no figure, table, or claim, and the scope is
disclosed in text, captions, and the appendix. The old jargon and the
stale cross-reference are gone from the section. **P1-1/P1-2: closed**,
with three pieces of residue that keep this from being a fully clean
close: a stale Appendix-D clause that still says the wing computations
"carry" the predecessor kernel (major 1), an untracked repro chain
under `daedalus/` that makes the committed generators un-runnable from
a clean checkout (major 2), and an availability filter that a
still-running case passes — the committed polar figure beat the a6-L2
forces file into existence by one second (major 3). Pass-14 minor 1 is
verified fixed. With this, the open ledger is down to P1-17, P1-18c,
and the deliberately-outside-the-paper explorations.

## Daedalus verification

✓ = verified against committed data / scripts run during this review.

**Predecessor residue purged from the section:**

- ✓ The "computations … predate the second gate pinch and carry the
  predecessor kernel" framing is gone; the section now states "the
  solver and kernel are exactly those of the airfoil studies" with the
  predecessor demoted to an "earlier campaign" that "exposed the
  compressed-bubble deficiency" (l. 2691–2696).
- ✓ The "sole modeling difference from Secs. III–V" understatement is
  deleted outright.
- ✓ The unsupported "$0.02\,c$ (Sec. V)" kernel-difference
  cross-reference (P1-2) is deleted; every remaining $0.02\,c$ near the
  section is a RANS-vs-strips agreement statement supported by the
  recomputation.
- Partial residue remains: "$Q_1$-only gate" (l. 2693) and "the second
  gate pinch" (l. 2856) are still undefined anywhere in the paper, and
  Appendix~D still carries a present-tense predecessor clause — see
  major 1 and minor 1.

**Numbers vs. the campaign data (`canon_front_metrics.py` run, output
reproduced):**

- ✓ η=0.31, α=4°: bubble 0.489–0.661 (str L2) vs strips 0.472–0.660 —
  the text's "0.49–0.66 vs 0.47–0.66" and the response headline,
  exactly. Separation Δ=0.017 ("agrees to 0.02c" ✓), reattachment
  Δ=0.001 ("under 0.01c" ✓).
- ✓ α=5°: 0.465–0.645 vs 0.472–0.646 — "0.47–0.65 against 0.47–0.65" ✓.
- ✓ Refinement: reattachment 0.630 (L1) → 0.661 (L2) vs strips 0.660 —
  "0.63 at L1 to 0.66 at L2 against the strips' 0.66" ✓; separation
  and front level-converged (0.489/0.489; front 0.549/0.551) ✓.
- ✓ χ=1 front offset: 0.095/0.091/0.093c at α=4/5/6° — "~0.09c" ✓;
  ln(1/8.76e-6)=11.65 → "11.7 e-folds" ✓; 13.6−11.7=1.9 → "~2 e-folds
  per 0.09c" ✓.
- ✓ Outboard stretch (α=4°, str L2, stations rerun): reattachment
  0.661→0.786→0.950 at η=0.31/0.92/0.97 vs strips 0.660→0.740→0.837 —
  the text's 0.66→0.79→0.95 vs 0.66→0.74→0.84 ✓.
- ✓ `tab:daetotals` recomputed from `total_forces_v2.csv` with the
  generator's window (pseudo_step ≥ last−500, 51 rows): all 16 CFD
  cells match to the printed digit; dashes exactly where runs are
  pending. The in-progress a6-L2 partial (CL 1.1811, CD 0.0403 —
  mid-transient) appears nowhere ✓.
- ✓ Drag narrative: final kernel 31.4–36.1 counts below AVL+XFOIL
  ("30–36" ✓); predecessor table (5509ab0^) 40.3–45.9 below ("40–50"
  ✓); like-for-like return +5.9 to +13.8 counts ("6–14" ✓); L1→L2 adds
  4.0/3.1 counts ("3–4" ✓). Lift: 5.0–5.3% above ("~5%" ✓); family
  agreement at L1: CL 0.66/0.67/0.74% ("0.6–0.8%" ✓), CD
  0.92/1.04/1.77% ("0.9–1.8%" ✓).

**Partial-campaign honesty:**

- ✓ "Eight converged RANS solutions underlie the comparison" — exactly
  the eight case dirs with completed artifacts (6×L1 + str-L2 a4/a5);
  cavity-L2 dirs hold only meshes, ogrid-L2-a6 is mid-solve (started
  18:16, ~2000 pseudo-steps at review time). Pending set enumerated in
  text and excluded from every figure/table/claim I could trace.
- ✓ The a6 surface figure and its `\label{fig:daesurf6}` are deleted
  with all references retargeted (build log: 0 undefined refs); the
  fig:daesurf4 caption discloses "the unstructured-L2 row will be
  added when that run completes"; `tab:daetotals` caption defines the
  dashes.
- ✓ No sentence still quotes a predecessor number *as a result* — the
  0.04–0.06c and 40–50-count figures appear only as the labeled
  before-state of the correction story.

**Provenance (no ai_constants.log in the daedalus case dirs — but the
echo exists):** the resolved-constants block is present in each
completed case's (untracked) `solver_stdout.log`; I extracted
ogrid-L2-a4/L1-a4/cavity-L1-a6 and all match the canon `_SPHERE` dict
of `saai_env.py` (a_max 0.19, reOmCeil 1851.2, reOmA 124.6, reOmB
1.424, rampWidth 0.35; sigmaDTie 1). Seed: `Flow360.json`
modifiedTurbulentViscosityRatio = 8.76e-6 with ai_laminarSlowdown =
1.0 — the *uncompensated* convention, correct for these cold starts
(no campaign-JSON pairing hazard). ✓ N_crit map: 8.76e-6 → Tu 0.0103%
→ N = 13.61 via `saai_env.Tu_pct_from_chi_inf` — "≈13.6 by the map of
Sec. II.F" ✓. But none of this is in git — see minor 4.

**Appendix D:** opening, totals table, and figure references all
recompiled consistently (✓, diff read in full); the temporal-units
ledger arithmetic checks (0.361×0.585=0.211→0.212; 0.066/0.19=0.35;
1.45×2.05=2.97 → "factor of three") — but the ledger's provenance
clause is now self-contradictory (major 1).

**2D coherence:** the wing story (χ=1 front ~0.09c ahead, eddy
viscosity taking the remaining bubble length to reach closing
strength) is cited to and consistent with Sec. VI's handover anatomy;
the wing bubble matching the strips does not contradict Sec. V's
bubble-too-long-vs-*experiment* finding (different references). The
conclusion's new Daedalus sentence matches the section's numbers ✓.

**Pass-14 minor 1 (2d4c7cd):** ✓ verified — 98 `ai_constants.log` + 14
results JSONs added in that commit; with the 4 previously-tracked
sphere-campaign JSONs, all 18 top-level `flow360_fr/*.json` are now in
`git ls-files`, exactly as the response claims.

## Status of prior comments

- **P1-1 (predecessor kernel / Q1-Q2 jargon)** — **closed by
  recomputation** (fix (a) of the original review); residue: two
  undefined jargon phrases (minor 1) and the Appendix-D clause (major 1).
- **P1-2 (unsupported 0.02c cross-ref)** — **closed** (deleted; claims
  now rest on the recomputation itself).
- **P1-17 (tie back-reaction re-march)** — open, untouched.
- **P1-18c (320×80 flat-plate single grid)** — open, GPU-gated.
- **fv1-bypass exploration** — outside the paper per the response;
  `run_fv1bypass_test.py` still untracked, as are
  `lsb_zoom_sheets.py` and the two `figs_explore/lsb_zoom_Re*.png`.
- **Pass-13 round + pass-14 minor 1** — all verified (pass 14 and above).

## New major comments

1. **Appendix D still says the wing computations carry the predecessor
   kernel (l. 3348).** "This ledger, measured on the predecessor kernel
   these wing computations carry, is the in-place excess…" — untouched
   by 5509ab0 and now contradicting the same appendix's opening ("All
   solutions use the airfoil-study solver settings with the final
   kernel"). A referee reading Appendix D would conclude the 3D results
   are still predecessor-kernel. Relatedly, the ledger's profile
   extraction "(structured L2, η=0.31, α=5°, x/c=0.48–0.53)" is now
   ambiguous about *which* campaign's L2 — the ν̃-peak-riding
   diagnostics are predecessor-kernel phenomena and x/c 0.48–0.53 was
   that kernel's whole bubble, whereas the canon L2 bubble spans
   0.465–0.645. Fix: "measured on the predecessor-kernel campaign"
   (twice: also check l. 3332's anchor-condition sentence reads clearly
   as historical), and label the extraction's campaign explicitly.

2. **The committed Daedalus generators cannot run from a clean
   checkout.** 5509ab0 tracks `polar_compare.py`, `wing_geometry.py`,
   `sectional_compare.py`, `chi_surface_map.py`,
   `canon_front_metrics.py`, `run_all_canon.sh` — but not their
   dependencies: `daedalus/avl_compare.py` (imported at module load by
   `polar_compare.py`, hence by `regen_daedalus_polar_sectional.py` →
   ImportError), `dae{11,21,31}_selig.dat` (read by tracked
   `wing_geometry.py`), the `daedalus/avl/` cached AVL outputs (the
   AVL binary path points to a dead session scratchpad, so the cache
   *is* the reference), and `run_solution.py` (invoked by tracked
   `run_all_canon.sh`). Same loud-failure class as the three-pass
   figs/ saga and pass-14 minor 1: force-add the small scripts, the
   three .dat files, and the `avl/` text outputs.

3. **The polar generator's availability filter passes a still-running
   case — demonstrated.** `complete()` is
   `os.path.exists(total_forces_v2.csv)`; the in-progress ogrid-L2-a6
   run's forces file was born 18:16:41 — one second *after* the
   committed `daedalus_polar_sectional.pdf` was created (18:16:40 per
   its /CreationDate). The committed figure is clean only by that
   race. Rerunning `regen_daedalus_polar_sectional.py` now crashes
   (FileNotFoundError on a6-L2's `Y_slicing_forceDistribution.csv` in
   the sectional panel — I reran it; the committed PDF was not
   modified), and once the run writes its slicing CSV the figure would
   *silently* plot the unconverged point (currently CL 1.18, CD
   0.0403). Gate on run completion (e.g., final pseudo-step reached, or
   the forces+pvtu+npz triple that `regen_daedalus_surface_maps.py`
   already requires) rather than file existence.

## New minor comments

1. **Undefined jargon residue (P1-1's letter, not its spirit):**
   "the $Q_1$-only gate with its own three-anchor constants" (l. 2693)
   and "The second gate pinch was built to close exactly this blind
   spot" (l. 2856). Neither $Q_1$ nor "gate pinch" (first or second)
   is defined anywhere; Sec. II describes the final onset as a softmin
   of two limits. Rephrase in Sec.-II language — e.g. "a shaped-rate
   predecessor without the in-bubble branch of the onset threshold
   (the $B/(\hat S g)^2$ arm of Eq. (reomc))" — or define the two
   terms in one parenthetical each.
2. **"the two families agree at L1 to 0.01c on both edges" (l. 2833)
   over-claims:** true at α=4° (Δsep 0.008, Δreatt 0.000) but at α=5°
   separation differs by 0.016c (0.465 vs 0.481) and at α=6° by 0.015c.
   Say 0.02c, or scope the sentence to α=4° where the preceding
   numbers live.
3. **`tab:daemesh` y⁺ caption vs. unchanged numbers:** the caption now
   says the y⁺ percentiles come from the α=5° solutions "(for levels
   whose final-kernel run is pending, from the predecessor-kernel
   campaign)", but the commit changed no y⁺ digit — either the
   completed levels' values were re-derived and happen to match, or
   they are all still predecessor numbers. Also L0's final-kernel run
   is not "pending" — `run_all_canon.sh` only runs L1/L2. Recompute the
   completed levels' y⁺ (or state that all y⁺ rows are from the
   predecessor campaign, which is harmless for a mesh-quality table).
4. **Daedalus per-case provenance is not in git:** the sphere-kernel
   constants echoes live only in the untracked multi-MB
   `solver_stdout.log`s. Extract `ai_constants.log` per completed case
   (the `write_ai_constants` helper exists) plus a small canon results
   JSON (totals + front/bubble metrics), and force-add — the exact
   convention 2d4c7cd just established for the 2D campaign.
5. **"the predecessor-kernel campaign on the same grids returned the
   same fronts" (l. 2846)** — the predecessor case dirs are gone from
   `daedalus/` (rebuilt for the canon campaign), so the only evidence
   is the pre-5509ab0 figures in git history. Fine as history, but
   consider quoting the predecessor front number (the old text's
   0.613 vs 0.628 at α=4° L2) so the "same fronts" claim carries its
   own evidence.
6. **`regen_daedalus_surface_maps.py` docstring drift:** promises "3x3
   panels" with "columns = |C_f| (left), -Cp (left), streamwise C_fx
   (middle)" — four names for three columns, |C_f| is not plotted, and
   the row count is now availability-dependent; the variable `cfm` in
   `make_fig` actually holds −Cp. Cosmetic, but it is the repro
   documentation for the paper's two largest figures.
