# Pass 40 — first 3D quantification (Stock digitization gate) + NLF negative-pair completion

Scope: two commits on a clean working copy (HEAD `0d5a1bd`).
- `2aa3665` — spheroid: digitize Stock Fig 15a, compare computed fronts (first 3D quantification).
- `0d5a1bd` — NLF(1)-0416 negative pair (α=−8°, −4°) on all six grids; sheets/figures/tables refreshed.

Digitization performed independently in scratchpad from `paper/spheroid.pdf` (PyMuPDF render). Build compiled. No files edited; regenerated figures restored with `git checkout`.

## Short summary

The **mandatory digitization gate is satisfied for the data**: I independently digitized Stock Fig 15a and the committed square points (Re=1.52e6, α=10°) match mine to **<0.002 in x/L and ~1.5° in φ** — an excellent, honest digitization. The **Reynolds ambiguity is pinned**: Stock's Fig 15a is Re=**1.52×10⁶** (measured, stated in text p.8 and in the figure legend); there is **no 1.6×10⁶** anywhere in Stock — the paper computes at 1.5×10⁶, a disclosed ~1.3% mismatch. **L2 has finished** (residuals 1e-8…1e-10 at 19999 pseudo-steps, forces flat) — it was mid-run at pass 36.

Two substantive cautions on the spheroid side: (1) the committed comparison (figure + JSON + script) is **not yet in the tex** — the paper still says the overlays are "to follow", so nothing is over-claimed *in the paper*, but the gate deliverable hasn't surfaced; (2) the headline "windward front matches to +0.004" uses an **ad-hoc `cf`-rise proxy front**, while the paper's model-native front (χ=c_v1, used everywhere else) sits at **0.92–0.97 x/L for every φ** and does *not* track the measured squares. The measured squares are essentially the laminar separation line at this Re (Stock: "laminar up to separation"), so the framing when this lands in the tex is load-bearing.

The **NLF refresh is clean and fully consistent**: every Table F2 number matches the JSON case data; the L2-only conditional clause dropped; captions/body text updated (36 cases total); L0 under-resolved-trip note accurate.

## Spheroid verification

**Digitization methodology (independent).** Rendered `paper/spheroid.pdf` p.9 at 600 dpi (PyMuPDF). Isolated Fig 15a panel (top of the left column). Grid calibration by detecting the solid frame and dashed rulings: vertical lines at px [115.5, 508, 905.5, 1299, 1695] → X/a = [−1, −0.5, 0, 0.5, 1]; horizontal dashed lines → φ = [180…0] step 30 (checked skew across three x-bands, negligible). Markers found as enclosed white blobs (interior of the open square/circle glyphs), then each disputed/obscured marker zoom-verified by eye at NEAREST-resampled crops. Mapped px→(x/L, φ) with x/L=(X/a+1)/2. Estimated accuracy ±4 px ≈ ±0.005 x/L, ±1.5°.

- **Squares (Re=1.52e6, α=10°) — committed JSON confirmed.** My 8 points vs `stock2006_fig15a_digitized.json`: max |Δx/L| = 0.0017, max |Δφ| = 1.5°; most points agree to <0.001 x/L. The JSON docstring's self-reported ±0.005 x/L / ±1.5° is honest. **Gate passed.**
- **Reynolds ambiguity — PINNED.** Stock p.8: "α = 10 deg and a Reynolds number Re = 1.52 × 10⁶ (Fig. 15a)"; the figure legend reads "Re = 1.52 × 10⁶" (squares) and "6.56 × 10⁶" (circles). **No 1.6×10⁶ appears in Stock** (the "1.6e6" in RESPONSES l.90 was an unrelated case-selection range). The committed case is Re_L=1.5×10⁶ (tex l.2752). So: measured **1.52e6** vs computed **1.5e6**, ~1.3% low. The JSON and `compare_stock_fronts.py` docstrings both state 1.52e6/1.5e6 correctly; the **tex spheroid section states only the computed 1.5e6 and gives no measured Re yet** — the mismatch must be disclosed when the comparison lands.
- **Conditions matched.** α=10°, φ=0 windward: convention matches (Stock p.4 windward/leeward; our maps φ=0 windward). Good.
- **L2 convergence — FINISHED (pass-36 gate cleared).** `spheroid_fv1/case_ogrid_L2_saai_a10`: `nonlinear_residual_v2.csv` final (0,19999): cont 3.9e-10, momx 1.4e-8, nuHat 8.0e-8; `surface_forces_v2.csv` flat over the last 20 steps. `spheroid_maps_L2.npz` regenerated 13:05. Pass 36 flagged it mid-run; it is now converged.
- **Extraction script — read critically.** `spheroid/compare_stock_fronts.py`: two front definitions. χ front = first wall-normal-max-χ crossing of c_v1=7.1, linearly interpolated in x (clean). `cf`-rise front = first node past x/L>0.2 where `cf > 1.5*runmin + 2e-4` — a **heuristic with a magic absolute offset (2e-4) and node-resolution only (no sub-cell interp)**; not tied to any stated criterion. Silent-failure behaviour: unfound fronts stay NaN and would surface as NaN in the table (none did — all fronts found). Units and φ convention consistent with the maps.
- **Claim vs. what the comparison shows.** I re-ran the script (table reproduced; figure then restored via `git checkout`). The RESPONSES headline "+0.004 windward, growing to +0.23 leeward" is **reproduced by the `cf`-rise front**. BUT the model-native **χ=c_v1 front is 0.918–0.975 x/L at every φ** — it does not track the measured squares (0.40–0.93) at all. The good agreement lives entirely in the `cf`-rise proxy, which is picking up the **separation-induced shear rise**, not model transition. This is physically coherent (Stock: the low-Re body "stays laminar up to the open free-vortex-layer separation, with the measured fronts tracking the separation line") and RESPONSES states it, but **none of it is in the tex yet**, and when it lands the front definition and the "these squares ≈ separation line" caveat are essential to avoid over-claiming a transition match.
- **Comparison figure not wired into the paper.** `spheroid_front_compare.pdf/png` are committed but `grep` finds **no `\includegraphics`/`\ref` in `sa-ai.tex`**; the section (l.2761–2762) still reads "the maps here are the infrastructure for that comparison, with the measured-front overlays … to follow." Conservative (no over-claim), but the gate's headline deliverable is not surfaced.
- **Circles (Re=6.56e6) — minor data caveat, not paper-load-bearing.** The paper compares only the squares. My detection of the leeward-most circle disagrees with the JSON (`0.256, 153°` in JSON vs my `~0.31, 165°`; that glyph is crossed by streamlines). Since circles are unused in the comparison, non-blocking — but recheck before any 6.5e6 comparison uses them.

## NLF verification

- **Table F2 vs case data — all recomputed, all match.** Cross-checked every new F2 row against `flow360_fv1/sphere_campaign_nlf_neg_results.json` (12 entries):
  - −8 L0 str `-0.4426/0.01830/0.195/0.011` ↔ JSON `-0.4426/0.0183/0.195/0.0105` ✓
  - −8 L0 cav `-0.4689/0.01869/0.140/0.012` ↔ `-0.4689/0.01869/0.1403/0.0119` ✓
  - −8 L1 str `-0.4656/0.01093/0.538/0.006` ↔ `-0.4656/0.01093/0.5377/0.006` ✓
  - −8 L1 cav `-0.4397/0.01150/0.571/0.011` ↔ `-0.4397/0.0115/0.5705/0.0105` ✓
  - −4 L1 cav `0.0357/0.00832/0.452/0.126` ↔ `0.0357/0.00832/0.4524/0.1263` ✓
  All 12 rows consistent to rounding. `front_converged: True` on all except two L2 entries (cavL2 am4, strL2 am8 lack the flag but were the pre-landed L2 pair).
- **F2 conditional clause auto-dropped — confirmed.** Caption now plain; the "−8/−4 rows exist on the finest (L2) pair only" sentence is gone (diff verified).
- **L0 under-resolved-trip note accurate.** Sheet caption (l.3016): "front settles at 0.54–0.57c on L1/L2 … the L0 grids trip at 0.14–0.20c, the under-resolved-front artifact." JSON: L1/L2 upper = 0.538/0.571/0.559/0.561 (→0.54–0.57 ✓); L0 upper = 0.195/0.140 (→0.14–0.20 ✓). Accurate for the negative α.
- **am4 caption values check.** Upper "0.44–0.48c across the ladder" ↔ JSON 0.435–0.476 ✓; lower "≈0.08–0.13c" ↔ JSON 0.083–0.126 ✓.
- **Fig 6/7 + body text.** Fig `nlf_cf_negalpha` caption (still L2-only, correct: it shows the finest pair) reports upper front 0.559–0.561c on measured ≈0.54c — matches L2 JSON. Body (l.1489, l.2837) updated to "same six grids"/"twelve further cases"; l.2837 "twenty-four … plus the negative pair on the same six grids" = 36 cases, consistent with RESPONSES.
- **Build.** `pdflatex`→`bibtex`→2×`pdflatex` clean; 105 pages; **no undefined references, no multiply-defined labels.** Build artifacts gitignored; regenerated `spheroid_front_compare.*` restored.

## New major comments

1. **Spheroid comparison is committed but not in the paper, and the front definition it will need is the crux.** The figure/JSON/script land the gate's data, but `sa-ai.tex` neither includes the overlay nor states the measured Re. When it lands: (a) disclose measured 1.52e6 vs computed 1.5e6; (b) **state explicitly which front is plotted** — the "+0.004 windward" match is the `cf`-rise proxy (separation-shear), while the model-native χ=c_v1 front sits at 0.92–0.97 for all φ and does not track the squares. Presenting the `+0.004` as a transition-front match without the "measured points ≈ laminar-separation line at this Re" framing would over-claim.
2. **`cf`-rise front criterion is ad hoc and unvalidated.** `1.5*runmin + 2e-4` (fixed absolute offset, node resolution). The headline agreement depends on it; a defensible, stated criterion (or a sensitivity band) is needed before it carries a quantitative claim in the paper.

## New minor comments

1. **L2 CL worth re-reading now that the run finished.** Tex quotes L0/L1/L2 CL = 0.205/0.208/**0.186**; the converged `surface_forces_v2.csv` gives fluid/wall CL ≈ **0.188** at step 19999. ~1% — likely a snapshot difference, but re-confirm the quoted 0.186 against the finished field.
2. **Two new appendix sheets are orphaned.** `fig:sheet_nlf0416_am4_upper`/`_lower` (l.3038, l.3046) are defined but never `\ref`'d; the main-text pointer (l.1731) spans only the am8 sheets. Add an anchor or fold them into the range.
3. **Label naming drift.** The am8 sheets keep the generic labels `...negalpha_upper/lower` while the am4 sheets use `...am4_...`; harmless but slightly confusing for future edits.
4. **Leeward-most circle in the JSON** (`0.256, 153°`) disagrees with my re-read (`~0.31, 165°`); unused by the paper, but flag before any 6.5e6 comparison.

## Still open

- Daedalus Sec VII: `daedalus_surface_a5.pdf` and `a6.pdf` exist, are tracked, and are `\includegraphics`'d (l.2644/2653) — a5/a6 exported. Migration-completeness of the rest of Sec VII not audited this pass.
- Spheroid remaining gates: wire the comparison figure into the tex; workshop condition Re_L=6.5e6 still "to follow"; L2 CL ladder not grid-converged (0.205/0.208/0.186) — flagged in tex, acceptable.
- Repro refactor batch: `compare_stock_fronts.py` lives in `spheroid/`, not `paper/repro/` — fold into the repro tree and pin the `cf`-front criterion.
- README float-table pass: check for now-unused `chi_sheet_nlf0416_negalpha_{upper,lower}.pdf` (superseded by am8/am4) lingering in `figs/`.
