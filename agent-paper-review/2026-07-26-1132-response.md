# Response to review pass 36 (2026-07-26-1132.md)

## Majors

1. **L2 landed since your snapshot** (rc=0 at 20k steps, 12:35): final-window
   CL 0.1861 / CD 0.08175. The section now states all three levels are
   computed and quotes the non-monotone ladder (0.205/0.208/0.186) with an
   explicit not-yet-grid-converged flag; the figure remains L1 (stated), and
   the finest-level comparison is tied to the front-overlay step. The
   CL drop at L2 is exactly why the maps are being regenerated at L2 —
   suspicion: the leeward front/vortex structure shifts with resolution.
2. **"to follow" tracked, agreed not submission-safe.** It stays as a draft
   marker per the user's "don't add much text other than the plots yet"
   order; your item guarantees it can't ship silently. The overlays are the
   campaign's next milestone (Stock Fig. 15a squares + separation line at
   our exact condition; K-K 2010 Fig. 5a as the second source).
3. Digitization standing-instruction noted and accepted — independent
   re-digitization + Re-condition pinning (your minor 7: 1.5 vs 1.6e6)
   happens when the overlays go in.

## Minors

1. "now spanning" → "spanning" (changelog language dropped).
2. Unreferenced binaries untracked (spheroid_maps_L0.pdf/.png/_3d.png,
   spheroid_maps_L1.png); the tex's L1.pdf + L1_3d.png remain.
3. surface_map.py Re label now computed from the case's Mach/muRef
   (renders 1.5e6 and 6.5e6 correctly); the --paper flag edit is committed
   with this round.
4. regen_epp_resweep_forces.py docstring updated to the 0.6-3e5 / all-five /
   c_m-from-both behavior.
5. conv=False remark: kept as caption wording; a repro-notes line added in
   the docstring covers the 3e5 mfoil point (XFOIL-corroborated to 4 digits).
6. Stale pre-fix FlexFoil pickles: the only copy outside /local_data is the
   committed bubble JSON, which was regenerated post-fix; noted.
7. Folded into major 3.
8. "half model" → "half-model".

## Note

Since your snapshot the flat-plate chi normalization bug reported by the
OpenFOAM replication (2026-07-26-1253) was verified and fixed — the
flat-plate onset text/caption/conclusion now quote corrected crossings
(c_v1 within 10%; chi=1 drifts early with seed). Expect that in your next
diff. Daedalus is PAUSED after ogrid-L2 a5 (day shift belongs to the
spheroid per user order); a6 runs tonight.
