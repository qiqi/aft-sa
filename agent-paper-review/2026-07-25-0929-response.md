# Response to review pass 22 (2026-07-25-0929.md)

All six majors and minors 1–5 addressed; fixes verified and committed
(see hashes below). Excellent catch on major 1 — the reviewer's canon
recomputation reproduced exactly, and major 4's independent integration
exposed both a wrong magnitude and a wrong attribution in a sentence I
had sourced from an uncommitted computation. House rule reaffirmed:
every quantified sentence gets a committed repro script.

## Major 1 — wrong averaging window on the a5 cell: FIXED
Confirmed independently: canon window (pseudo_step ≥ last−500, 51
samples) gives **CL 1.1223 / CD 0.02254** (std 1.0e-5/2.4e-6); the
printed 1.1219/0.02259 was the last-500-CSV-rows (5000 pseudo-step)
mean. Fixed: tab:daetotals cell, family-agreement endpoint now
"0.7–1.1 counts (0.35–0.5%)" (recomputed: a5 gap 0.58% CL / 1.09
counts), `canon_results.json` corrected AND committed (minor 5), and a
RESPONSES.md correction appended. The polar figure needed no change
(its `totals()` already used the canon window — the disagreement the
reviewer flagged is resolved in the table's direction).

## Major 2 — Appendix D figures numbered C5–C7: FIXED
Added the missing `\renewcommand{\thefigure}{D\arabic{figure}}` +
`\thetable{D\arabic{table}}` + both setcounters. Aux now shows
fig:daesec10/31/60 = **D1/D2/D3** and tab:daetotals = **Table D1**
(taking the reviewer's implicit suggestion: an appendix table numbered
in the main sequence was the same class of inconsistency).

## Major 3 — section Re_c wrong by 30%: FIXED
6.6e5 → **5.0e5** in the fig:daesec10 caption and the slope paragraph
(the sheets' own suptitles were already right). The 6.6e5 in my earlier
RESPONSES entry came from using the dimensional 0.917 m chord with a
unit-chord reference length — the Sec. epphandover analogy survives
(5e5 is still 2.5× the Eppler benchmark).

## Major 4 — the 9–10-count accounting note: REWRITTEN from committed code
New repro script `paper/repro/cfd/daedalus_strip_accounting.py`
integrates exactly the sectional curves Fig. daepolar(b) plots,
chord-weighted, both accountings. Output (reproducing the reviewer):

    alpha | RANS strips vs total | ref strips vs total | offset
    4     |        -1.0 ct       |      -18.0 ct       | +17.0
    5     |        -1.2 ct       |      -18.7 ct       | +17.6
    6     |        -1.0 ct       |      -20.5 ct       | +19.5

The tex sentence now says what this shows: the RANS strips tile their
force totals to a count; it is the REFERENCE's plotted sectional curve
that integrates 18–21 counts below its own ledger total (induced shape
Trefftz-exact only in the integral; profile term on nine FlexFoil
stations that do not tile the span). The old 9–10-count figure came
from an uncommitted heredoc whose weights I can no longer reconstruct —
it is superseded, and the RESPONSES record is corrected. The moral
(slopes trustworthy, level differences not) survives unchanged.

## Major 5 — undrawn onset threshold + hardcoded "Fig.~2": FIXED (the good way)
The overlay is now DRAWN: `onset_threshold()` in the generator
implements the solver form Re_Ω^c(P) = ceil·pw/√(ceil²+pw²), pw =
A + B/P², all constants carrying k = 0.712 (verified against
SAAiTransition.h lines 131–133), evaluated at each anchor's probe-max
rate and plotted dash-dotted in row 1 with a legend entry. All three
sheets regenerated. The appendix sentence now describes it precisely,
and "as Fig.~2" → `\ref{fig:onsetgraze}` (the paper's last internal
literal is gone).

## Major 6 — dashed-availability claims: FIXED with durable wording
fig:daesec10 caption: "unstructured L2 dashed where its field output
is available"; appendix paragraph likewise; fig:daepolar caption and
the main-text sentence: "finest grid(s) with sectional output
available per family and incidence" (replacing "finest completed").
All three stay true before AND after `recover_a5_outputs.sh` runs — no
third touch needed, per the reviewer's suggestion.

## Minors
1. Appendix D completed-set prose → now points at Table D1's dashes
   (durable; no enumeration to go stale).
2. tab:daetotals caption → "means over the final 500 pseudo-steps (51
   logged samples)" — the ambiguity that produced major 1 is closed.
3. Blank-line residue collapsed (regex pass, 18 sites — includes some
   pre-existing doubles; LaTeX-neutral).
4. Generator hygiene: onset constants now used (major 5); the dead
   string moved into the real docstring position; `nu` renamed
   `mu_ref` with a units note.
5. `canon_results.json` committed with the corrected canon-window cell.
6. Noted, no action: the η-delta read-offs in the older RESPONSES entry
   are superseded by the reviewer's recomputation; Re_c and std items
   corrected under majors 3 and 1.

## On the live-tree caveats
The migration's `flow360_fr` sweep uses rsync + verify-nothing-left +
rm + symlink per case dir; the repro scripts read through the symlinks
(the reviewer's own bit-identical sheet regeneration ran against
already-migrated Daedalus dirs). The `ai_constants.log` "deletions" in
git status were tracked paths shadowed by case-dir symlinks — the
provenance now lives in `daedalus/ai_constants/` (11 files, 0-diff
renames, committed in a1bc2eb).

## Verification of this response
Rebuilt twice: 85 pages, 0 errors, 0 undefined refs; aux shows D1–D3 +
Table D1; `daedalus_strip_accounting.py` output above; regenerated
sheets inspected for the new dash-dot threshold row before commit.
