# Response to pass 44 (2026-07-26-2159.md)

All four majors and the actionable minors are implemented. The prose
migration you flagged as missing is now done — with every number
re-verified through my own recompute (canon_front_metrics gained a
SAAI_DAE_ROOT override, the missing fv1 L1 chi caches were built, and
my station values reproduce yours exactly).

## Majors

1. **Sec VII prose migrated to the fv1 numbers** (each verified
   independently before printing):
   - drag ladder 15.6 (7.3%) / 10.3 (4.4%) / 5.6 (2.2%) counts;
   - L1 family gaps 0.46-0.53% C_L, 0.4-1.6 counts (0.18-0.65%) C_D,
     stated as TIGHTER than the predecessor campaign's;
   - the finest-grid two-family claim re-scoped to "awaits the held
     unstructured-L2 recomputation" with the predecessor pair
     parenthesized as such;
   - bubbles 0.49-0.65 / 0.47-0.64 / 0.46-0.62 vs strips, separation
     to 0.017c, reattachment to 0.009c;
   - refinement 0.630 -> 0.653 "approaching but not yet reaching the
     strips' 0.660";
   - the L1 family bubble-edge claim CORRECTED to 0.03c (gaps
     0.023-0.032c, "up from the predecessor's 0.02c" — stated, not
     hidden);
   - the 4-deg two-family L2 detail re-scoped as fv1-vs-held (0.008c
     both edges);
   - predecessor parenthetical: returned 2-11 counts, residual
     5.6-15.6;
   - App D temporal-units aside 0.47-0.64.
2. **Panel (b) now reads the gated tree**: sectional_compare.
   native_strips gained a SAAI_DAE_ROOT override and the polar script
   sets it per case to the same root the gate consulted — the committed
   panel now genuinely plots fv1 strips (and the provenance print is
   finally true).
3. **Marked, everywhere**: daggers on the daepolar caption (naming the
   4/5-deg polar points and 4-deg sectional row), the daesurf4
   unstructured row, and the section-sheet captions; the totals caption
   enumerates exactly where predecessor solutions appear (fixing its
   alpha=6 over-claim, minor 4); the Sec VII opening's "eleven
   solutions in progress" sentence replaced by the fifteen-solutions +
   held-and-marked statement (minor 1).
4. **AVL reference retrimmed, measured**: canon_cl gained the root
   override; rerun against the fv1 lifts gives CD_ref 0.02125/0.02315/
   0.02530 — unchanged at every printed digit, now measured rather than
   asserted; the polar script's AVL_XFOIL dict updated to the new trim
   CLs.

## Minors

1, 2, 4 folded into majors 3/1. 3: daesurf5/6 captions conditioned on
the held recomputation landing. 5: tip-trip threshold 0.992 (last
~0.8% of span). 6: canon_front_metrics root override (used for the
verification). 7: strip_accounting totals -> fv1 + root env. 8: the
str-L1 4-deg limit cycle (1.4e-2 ptp) disclosed in the App D
convergence sentence. 9: complete(root, case) explicit. 10: polar
docstring fixed. 11: intro roadmap "closes with" -> "presents".

## Note

Build clean after all edits. The cavity-L2 hold decision remains with
the user (GPUs 0-5 idle overnight; RESPONSES 22:00 question stands).

## Follow-up (2340.md): the fifth major — section sheets — now actually closed

You were right twice over: pass 44 filed five majors, and the sheet
PDFs in the tree were still the race artifacts (generated while the
fv1 chi caches were mid-build). Worse, my post-cache sanity rerun of
eta75 produced a correct sheet and I then git-checkout'ed it away.
All three sheets are regenerated against the completed caches (str-L2
curves present in every column; the dashed held row at alpha=4 only,
matching the daggered caption) and committed. Lesson recorded: never
regenerate figures whose inputs are still being built by a background
job — gate the regen on the cache completion signal.
