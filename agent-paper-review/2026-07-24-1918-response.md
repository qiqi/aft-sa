# Response to review pass 16 (2026-07-24-1918)

Addressed in commit a6d5a49. One finding that supersedes part of the
pass's framing: **the CDind–CDff gap was never a lattice question.** We
rebuilt AVL 3.36 in double precision (tools/avl/Avl/bin/avl, limits
raised) and ran the ladder at α=4°: CDind/CDff = 0.009245/0.009244
(12×40), 0.009254/0.009258 (16×56), 0.009256/0.009267 (24×80) — the
near-field and Trefftz totals agree to four digits at every lattice.
The cached CDind 0.0121 on the same 12×40 lattice, and the 24×80 NaN,
were float32 roundoff in the small-difference-of-large-terms near-field
sum. Appendix D's fragility sentence now tells this story (and the
Trefftz reference is confirmed insensitive to both precision and
lattice, 0.2% from 12×40 to 24×80).

## Majors
1. (predecessor deficit per α) FIXED: "Against the same reference the
   predecessor-kernel campaign sat 12–17 counts below at α=4°, 4–11
   below at 5°, and essentially on it at 6°; like-for-like on each
   case, the final kernel's longer bubble returned 6–14 counts." The
   adjacent claims no longer fight: the per-case return is
   reference-independent, and the final kernel straddles the reference.
2. (signed-algebra antecedent) FIXED: the paragraph now opens by
   defining the magnitude triple in place ("The solver evaluates the
   indicators from magnitudes—|u| (wall-relative, Galilean invariant),
   ωd, and the projected curvature above—rather than from the signed
   profile derivatives of Eq. (indicators), assembled forms in
   Appendix E") and it now sits AFTER the implementation note, so the
   projected curvature it references has been introduced.

## Minors
1. → "within 3.5 counts (≈1.5%) at L2 and 11 at L1".
2. → "P_AI is negligible — Ŝg≤0 over most of the band (clip returns
   exactly zero) and Re_Ω^c sits far above the local Re_Ω, which stays
   below ~10² across the band" (no y⁺/κ formula).
3. → "deviations below 10⁻¹⁰" (covers the FD solve's 2×10⁻¹¹ worst
   case; the collocation's 3×10⁻¹⁶ is under it a fortiori).
4. → repro/README.md row now maps tab02_yplus.py to the II.E round-off
   claim (former tab:yplus noted as dropped);
   verify_wall_layer_tie.py docstring says Sec. II.E.
5. → tracked avl_compare.py now greps CDff with the same comment; its
   __main__'s stale predecessor rows are untouched by the paper
   pipeline but the induced-drag line it prints is now correct.
6. → (a) regen docstring says "(no complete 20k-step force history)";
   (b) "confirms the inference with room to spare".
7. → superseded by the double-precision finding: the sentence now says
   the single-precision build returned CDind 31% high / NaN while the
   DP rebuild returns CDind=CDff to four digits.

Also tracked this round: the three fv1-bypass battery scripts
(run_fv1bypass_test.py, run_fv1bypass_fslow.py,
run_fv1bypass_fslow2e5.py) — still outside the paper; latest status in
RESPONSES.md (1e5 limit cycle was a slowdown-schedule artifact; bypass
state CL≈0.93/CD≈0.023 at 1e5; 2e5 footprint ΔCL −0.5%, ΔCD −2.8
counts at raw pseudo-time).

Open ledger unchanged: P1-17, P1-18c.
