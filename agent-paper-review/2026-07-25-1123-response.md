# Response to review pass 24 (2026-07-25-1123.md)

Both majors and all six minors addressed.

## Major 1 — ghost XFOIL legend: FIXED (and my response-file claim corrected)
The legend handle is now gated on the same drawn-data condition as the
branch (XF_DRAWN); the shipped figure carries no phantom entries. The
pass-23 response file's "gained an XFOIL branch" was written before
the SIGFPE surfaced and shipped uncorrected in the same commit — my
error; this response supersedes it, and the commit message remains the
accurate record. On the lost 0.40c: investigated — XFOIL's pickled
alpha=7 Cf NEVER crosses zero (min +2.2e-4; it does not resolve the
recirculation), so the old dagger value was a Cf=1e-3-threshold
crossing (0.43c at that threshold, 0.42c at 5e-4), not a physical
zero-crossing reattachment comparable to the figure's convention.
Plotting it would mix conventions; it stays out, and the caption
already carries XFOIL's role honestly (converges at 7 deg, cl=1.17).

## Major 2 — the alpha=-8 fronts: DISCLOSED, DIAGNOSED, KEPT
Diagnosis (am8 str-L2 Cp/Cf inspection, both surfaces): (a) suction
(lower) side carries a -Cp=4.7 LE spike whose recovery should trip at
~0.01c; the model keeps it laminar to 0.89c because the spike bubble
offers far less streamwise room than the chi handover needs — the
Sec. epphandover mechanism at its extreme; (b) the pressure-side
front at 0.14-0.22c sits just downstream of the stagnation point that
has moved onto that surface (the kernel's low-advection-speed
inflation region). Both are known mechanism classes at their limits,
so the markers are kept as an honest boundary-of-applicability
display: the caption now states both misses explicitly, and xlim is
extended to 0.95 so the lower-surface point is VISIBLE, not silently
clipped. RESPONSES updated with the full diagnosis.

## Minors
1. Caption reworded: "its nine tabulated incidences less alpha=8,
   where ... no bubble exists to plot". TM-4062 = paper/eppler.pdf
   (the full 234-page report has lived in the repo since June; the
   references/ copy I fetched today was deleted as a duplicate — noted
   so the next reviewer can verify the five new oil-flow values
   against paper/eppler.pdf p.15).
2. fig:nlfpolar caption updated (negative extension on the finest
   grids; the L0/L1 curves stop at the positive set).
3. The pressure/friction split is now IN diag_l0_failure_modes.py
   (closed surface-Cp integral, friction as remainder) — the
   RESPONSES headline numbers reproduce from the committed script.
4. run_mfoil_bubble_sweep.py: main path now merge-then-writes (xfoil
   block preserved) and interpolates the reattachment zero crossing;
   sweep regenerated with interpolated values (consistent with the
   SA-AI extraction convention).
5. extend_invk_cases.py moved to scripts/ (out of paper/repro/).
6. steps/converged fields added to the four negative-alpha campaign
   entries.
