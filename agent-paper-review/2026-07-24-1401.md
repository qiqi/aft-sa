# Review pass 13 of `paper/sa-ai.tex` — verifying the passes-10–12 fix round, the monostability finalization (42c0a06), the τ=1 evidence (9535a2b), and annotated round 5 (1aa4b95)

*Reviewed 2026-07-24, ~14:01–14:30. Scope: commits a9e7372, 2b5aac3,
39f41c5, 8162984, 76882ea, d4dde01, bece4a7, c312bfe, 36786c3, 42c0a06,
1aa4b95, 48d8942, 9535a2b (HEAD is 9535a2b for the paper; 3e9aef0 is
thread-only). Working copy clean; `sa-ai.pdf`/`sa-ai.log` (12:30) current
with HEAD, 0 errors, 0 undefined refs. Verification: all eight Re=10⁵
histories (2 cold + 6 extensions) recomputed from the committed
`total_forces_v2.csv` (quartile medians, end slopes, final-20% medians,
drift by the runner's own definition); `tab:eppfork` and `tab:eppresweep`
recomputed entry-for-entry (`regen_resweep_table.py` re-run in full);
`ai_constants.log` checked in fork/ladder/extension/tau-test dirs (canon
slowdown 0.01 everywhere; τ=1 only where intended); the σ_D tie checked in
the solver source (`SpalartAllmaras.h`: `sigmaDTie=1` → `switchWidthD`
unused, so narrowing τ narrows both weights — the τ=1 test is faithful to
Eq. eq:blend); both τ=1 histories recomputed; `compare_exp_cp_datasets.py`
re-run; `fig01`/`fig02` regenerated (printed stats compared; overwritten
PDFs restored via `git checkout --`); `regen_epp_resweep_forces.py` re-run
(preview PNG md5-identical; PDF restored); the committed lowRe suite figure
and the Re=10⁵ Appendix-C sheet rendered and inspected. No paper files were
edited. Note: while this pass ran, two further thread/rig commits landed
(f343501, 13a5554 — the amplifying-zone-gate march work of the 15:00
addendum) plus a live working-copy edit to `march_sa_handover.py`; they
touch no paper files and are not reviewed here — the next pass should take
the gate work once it reaches the tex.*

## Summary

**The monostability rewrite is verified end-to-end and is the right
resolution of pass-12 major 1.** All six warm-path extensions are genuinely
converged — end slopes are mixed-sign at 1.6–2.4×10⁻⁴ per 10⁴ iterations
(30× flatter than the decaying 20k forks pass 12 measured) with flat
quartiles — and the four initialization paths per family agree to
CL ≤ 0.005 / CD ≤ 4.9×10⁻⁴ at 10⁵, exactly as the new Sec. V.A states.
The bistable-branch content is gone everywhere I looked: the tables, the
three sweep figures (fork curves and warm markers removed, verified in the
committed PDFs and generators), abstract, roadmap, Conclusion; every
surviving "bistable" refers to the experiment at 6×10⁴, correctly. The
Re=10⁵ contour sheets' warm rows now show the *converged* extended
solutions and are visually indistinguishable from the cold L2 rows — the
field-level evidence the appendix claims. The τ=1 material reproduces from
the committed cases (ΔC_l 0.287 ≈ "0.3" at 10⁵ over the final 20%,
0.081 ≈ "0.08" at 2×10⁵ where canon is flat to <5×10⁻⁵), the notation is
consistent with Sec. II's τ, and — checked in the solver source — the tie
makes the narrowing apply to production and destruction weights together,
so the experiment tests what Eq. (eq:blend) says it tests. Every ADDRESSED
claim from the 0930 response I spot-checked is real (TM-4062 repeat rms
0.012–0.018 / 0.061 ✓, e⁹ 0.15–0.2 ✓, steadiness rewrite ✓, destruction
clause ✓, twice-Blasius ✓, all of annotated round 5 ✓). One structural
defect recurs for the third time: the **eight new Appendix-C contour
sheets are referenced by the tex but not in git** (major 1) — including
the two Re=10⁵ sheets that carry the monostability field evidence. The
rest is small: a clobbered results JSON, a 10×-mislabeled slope key, two
rounding/wording nits, and stale pre-verdict framing in two repro scripts.

## Verification of claimed fixes

✓ = verified against committed data/generators/sources.

### The monostability finalization (36786c3 + 42c0a06)

- ✓ **All six extensions converged.** Recomputed from the committed CSVs
  (4002 rows × 10 it each, seeded from the parents' restart dumps):
  final-20% medians ext_fork/up/dn str 0.7639/0.04485, 0.7689/0.04436,
  0.7649/0.04474; cav 0.8598/0.03405, 0.8592/0.03412, 0.8598/0.03405 —
  **every `tab:eppfork` entry is the correct rounding**, including the two
  entries missing from the committed results JSON (minor 1). End slopes
  mixed-sign, |slope| ≤ 2.1×10⁻⁴ per 1000 it; drift by the runner's own
  definition (ptp over the last 1000 it) 4.2×10⁻⁵–2.39×10⁻⁴ for all six ✓
  caption "residual drift ≤3×10⁻⁴". Quartiles flat in Q2–Q4 on every path.
- ✓ **One attractor per family at 10⁵**: CL spread across cold/fork/dn/up
  is 0.0050 (str) and 0.0006 (cav); CD spread 4.9×10⁻⁴/0.7×10⁻⁴ ✓ "lift
  agreeing to ≤0.005 and drag to ≤5×10⁻⁴" (both at the bound, but true).
  Pass-12 major 1's extrapolation is vindicated: the warm branches merged
  onto the cold states, not onto a second branch.
- ✓ **6×10⁴ lower block regenerates** from `fork_bistability_results.json`
  and `ladder_*_results.json` (canon re-runs): fork 0.694/0.0521,
  0.607/0.0535; descent 0.694/0.0522, 0.619/0.0541; cold = the
  `tab:eppresweep` values. Wording nit on the str fork — minor 5.
- ✓ **Transient narrative matches the histories**: the direct forks first
  gain lift (str 0.926→0.987, cav 0.937→0.968), suffer a bursting
  excursion (dips to 0.692/0.805 near it 13–14k), and recover; the cav
  climb overshoots to 0.891 ✓ "overshoots to c_l=0.89 (the unstructured
  family)". "±0.06 excursions" is a fair round of the post-transient
  deviations (+0.03/−0.07). "4–6×10⁴ iterations" — minor 4.
- ✓ **Canon provenance**: `ai_constants.log` in every fork, ladder,
  extension, and tau-test dir; `ai_laminarSlowdown: 0.01` in all
  (pass-12 minor 2's record, now load-bearing); the wrong-seed results
  archived as `*_wrongseed_archive.json` ✓ as the response promised.
- ✓ **Transient warm-start content removed**: `regen_epp_resweep_suite.py`
  and `regen_epp_resweep_forces.py` lost their fork-overlay blocks; the
  committed lowRe PDF (rendered) has no fork curves and no warm legend
  entry; `regen_epp_resweep_forces.py` regenerates the committed figure
  (preview PNG md5-identical); the warm row left `tab:eppresweep` and no
  placeholder or asterisk remains in `tab:eppfork` (pass-12 minor 1
  folded in, as claimed).
- ✓ **Appendix-C warm rows are the converged fields**: `regen_chi_sheets.py`
  now reads `ext_fork_*`; the Re=10⁵ upper sheet (disk, 09:42) rendered —
  the two "L2 warm start" rows are visually indistinguishable from the
  cold L2 rows in both |u| and log₁₀χ, exactly the claim at the caption
  and in the Appendix-C preamble. **But the sheets are not in git** —
  major 1.
- ✓ **No leftover bistability language**: every remaining "bistable" in
  the tex (l. 2295, 2316, 2351, 2395, 2402, 2457) refers to the
  experiment/e⁹ at 6×10⁴; abstract carries the bursting-boundary clause
  only; the Conclusion's initialization-study sentence ("the computed
  state is *unique* at every swept Reynolds number") matches the data;
  Sec. V body (l. 2378–2383) now says "the computed state is unique …
  every initialization converges to it".

### The τ=1 evidence (48d8942 + 9535a2b)

- ✓ **Both cases reproduce.** `tautest_strL2_Re100k_a5_tau1`: final-20%
  ΔC_l = 0.287 over 0.877–1.164 ✓ tex "ΔC_l ≈ 0.3" (response "0.88–1.16" ✓);
  `..._Re200k_...`: ΔC_l = 0.081 over 0.935–1.016 ✓ "≈0.08 even at the
  otherwise steady 2×10⁵ benchmark" (canon strL2 at 2×10⁵ is flat to
  <5×10⁻⁵ ✓ pass-11 data). The oscillation is recurrent (bursts recur in
  the 10%-segment ranges), consistent with "relaxation oscillator", not a
  one-off transient.
- ✓ **Env faithful to the paper's τ**: `ai_constants.log` shows
  `ai_switchWidth: 1.0` with canon slowdown; and — checked in
  `SpalartAllmaras.h` — with `sigmaDTie = 1` (canonical) σ_D is computed
  pointwise from σ_P via the tie, `switchWidthD` unused, so "a
  four-times-narrower ramp" narrows the production *and* the tied
  destruction weights together: the test probes exactly Eq. (eq:blend)'s τ.
- ✓ **Notation/constants consistent**: τ is defined as the production
  handover width (nomenclature l. 98, Eq. eq:sigma, constants block
  τ=4 l. 1415); Sec. V.B's "τ=1 (a four-times-narrower ramp)" and the
  case names match. The future-work sentence (outer-scaled trigger +
  transported breakdown memory) appears in Sec. V.B and the Conclusion's
  matching item ("whose narrowing alone only trades the bias for a
  relaxation oscillation") ✓.
- Repro gap: only the 10⁵ case is constructed by the committed script —
  minor 7.

### Passes 10–12 ADDRESSED claims (spot-checked at their sites)

- ✓ **P12-M2**: l. 2401–2403 "where the measurement is bistable and the
  computation most mesh-sensitive (Sec. eppbistab takes up the branch
  question)".
- ✓ **P12-M3**: sweep text and subsection opener rescoped — "*Above*
  6×10⁴ the measurement is single-branched" with the TM-4062 p. 9
  sentence quoted (l. 2507–2513); the 10⁵ three-condition repeat
  coincidence stated.
- ✓ **P12-M3 strengthening (repeat datasets)**: `compare_exp_cp_datasets.py`
  re-run — 10⁵ free-transition pairwise rms 0.0122/0.0148/0.0179 ✓
  response "0.012–0.018"; 6×10⁴ up-vs-down rms 0.0609 ✓ tex "ΔC_p rms
  0.06, four times the repeat scatter" ✓ (0.061/0.015 ≈ 4). The
  fig:eppresweep_low caption's C_p(0.95) = −0.13 vs −0.04 ✓ (−0.128 /
  −0.037) and the run/condition identifications match the script's
  provenance exactly. (Response wording nit — minor 8.)
- ✓ **P12-M4**: `tab:epphandover` 6×10⁴ e⁹ entry now 0.15–0.2 with the
  caption stating mfoil 0.676→0.825c and XFOIL 0.879c (transition not
  stored, bound 0.2) — my pass-12 numbers verbatim; the 4.6×10⁵ dash
  explained (no stored XFOIL x_tr; bubble closes 0.50c) ✓; the
  stretching-far-less observation added ✓.
- ✓ **P12-M5**: the reconciling clause at l. 2373–2377 ("two
  contributions … dominant at 2×10⁵ … takes over as the Reynolds number
  falls") and the Conclusion tells the same one story.
- ✓ **P12-M6**: abstract clause (bursting boundary only, survives the
  monostability verdict) and roadmap sentence (l. 277–279) present.
- ✓ **P12-M7**: the three sweep-suite figures are now tracked (the forces
  PDF restores from git; 172 figures under `paper/figs/` in the index) —
  but the fix did not extend to the *new* Appendix-C sheets — major 1.
- ✓ **P12 minors**: 3 (branch-labeled Exp column + "≈0.64" ✓ caption),
  4 (non-monotone Δx(1→30) clause ✓), 5 (no-closure convention + raw
  0.996–0.999c in caption ✓), 6 ("no oil-flow bubble data" in generator
  and rendered figure ✓), 7 (±0.04/±0.0023 half-spread bars, derivation
  in caption ✓ = (0.721−0.643)/2, (0.0431−0.0386)/2), 8 (all three
  wordings ✓, Δx headers unified ✓).
- ✓ **P11-M1**: the steadiness sentence is the recommended shape and the
  numbers are pass-11's measurements (away from 10⁵ flat to <5×10⁻⁵ over
  the final 2000 pseudo-steps; at 10⁵ unstructured L1 3×10⁻⁴, both L2s
  ≈7×10⁻⁵, strL1 ΔC≈0.01 cycle with tabulated medians).
- ✓ **P11 minors 1–7**: ≈0.02c both clauses (l. 2172–2173); "the
  structured L0 loses the marginal high-α lower fronts" (l. 1735); "on
  the two finer levels" in body and caption; "≤2×10⁻⁴ (8×10⁻⁴ at L1)"
  L2-led (l. 2393); NLF cavL1 α=15° disclosure (l. 1732–1734); Conclusion
  names 0.031c front / 0.035c reattachment; the stale
  `eppler_cavity_proper_ncells.json` is deleted.
- ✓ **P10-M1**: l. 897–899 "production is switched off outright (σ_P=0
  for χ≤1) and the tied destruction floor is inert at the disturbance's
  scale (<10⁻³ …)". **P10-M2**: "nearly twice Blasius's in Re_θ (over
  four times in Re_x)" (l. 936–937) — both my measured values.
- ✓ **P10 minors**: "keeps the profile's curvature single-signed" at all
  three sites; the em-dash stack broken; "grazing 1.27–1.34 high for
  β≥+0.25" caption+body (and now "beyond the plotted family", correct
  after round 5 trimmed +0.25/+0.35); fig03 docstring says +0.10; the
  `max(tops)` guard is moot (the envelope block was deleted in round 5).

### Annotated round 5 (1aa4b95)

- ✓ **Fig 1**: generator `PROFILES` = caption exactly — seven members,
  −0.15 → −0.16 (red-orange, H=3.09) and the new gold attached −0.19
  (H=3.48); regenerated, printed family/bands consistent.
- ✓ **Fig 2**: regenerated — new family (+0.15…−0.1988 + reversed)
  grazes 0.932–1.137, rms 6.25% ✓ caption "0.93–1.14 (rms ~6%)" —
  **the graze statistics survive the membership change**, as the commit
  claims; β≥+0.25 clause correctly relocated to the body with "beyond
  the plotted family".
- ✓ **Fig 3**: untouched-equation envelope removed from figure, legend
  (committed PDF text has no "untouched" ✓), and caption; the body
  paragraph keeps its numbers marked "(not plotted)" ✓ (l. 945–948);
  per-row max Ŝg 0.162/0.078/0.037 in the caption = Table 2's values ✓;
  the mega-parenthetical is now separate sentences ✓.
- ✓ **Fig 4 caption**: "set entirely by the fixed eigenvalue a_max=0.19
  and c_ν,ai=1/6, with nothing tuned to this curve" ✓; attached-family
  range "H=2.44 (β=+0.15) to H=3.98" ✓ (generator prints H=2.44 at
  +0.15); β≈0.45 body sentence carries max Ŝg ≈ 5×10⁻⁴ ✓ (l. 1019–1020).

### Also verified in this round's other commits

- ✓ **P1-7 (8162984)**: the reconciling paragraph at Sec. II.F
  (l. 1361–1391) — implemented triple, sign-consistent region, the
  mixed-layer deviation bounded by the near-inflection amplitude, "the
  projection reads sign(u)·u″" — matches the solver's magnitude-based
  indicators as described; internally consistent with the RP² algebra
  section.
- ✓ **P1-17b (d4dde01)**: the Sec. VI "within ~30%" is now "0.9–1.9× the
  correlation (Fig. calibrate-a and the Appendix-D ledger — against a
  correlation whose own validity fades at those depths)" — one number,
  one story.
- ✓ **P1-18a (bece4a7 + c312bfe)**: `tab:epplit` present with declared
  figure-read provenance; internal consistency checks pass (SA-AI
  c_d@c_l=0.891 ≈ 0.0153 = the +11% bucket-tracking number ✓; bubble
  0.40/0.53/0.66 vs oil flow 0.38/—/0.59 matches the benchmark section's
  verified values); the AFT row records the absence of published data at
  this condition, matching `paper/aft.pdf` (the journal set is flat
  plate / winglet / S414 at Re≈10⁶) — the honest correction.
- ✓ **NLF AFT head-to-head (36786c3)**: `nlf_aft_transition.pdf` is in
  git; `data/aft_nlf0416_digitized.json` carries explicit provenance
  (Coder dissertation `coder.pdf`, Figs. 5-22–5-25, vector-path
  extraction); caption and body agree on N_crit 10.07 vs 7.18 and the
  untuned-seed contrast. (Deep verification against the dissertation
  figures not attempted this pass.)
- ✓ **P1-m13 (76882ea)**: recorded as verified-no-change against the
  NTRS source; citation sites unchanged. Accepted.
- ✓ **P1-m17**: no bare `(\eqref{...})` remains (all sites are
  `(Eq.~\eqref{...})`).

## Status of prior comments

- **P12-M1** (10⁵ forks decaying) — **resolved, verified**: extensions
  converged, monostable verdict supported by every committed history.
- **P12-M2–M7, minors 1–8** — **resolved, verified** (above); P12-m9 n/a.
- **P11-M1/M2, minors 1–7** — **resolved, verified**; P11-m8 n/a.
- **P10-M1/M2, minors 1–5** — **resolved, verified**; P10-m6 n/a.
- **P1-1/P1-2** (Daedalus final-kernel recomputation) — **open, in
  flight**: disclosure paragraph in place (l. 2691–2700, with the 0.02c
  cross-ref and "a re-computation with the final kernel is planned");
  canon campaign observed running (both L1 sets complete; ogrid L2 α=4°
  done 14:11, α=5° running; cavity L2 not started).
- **P1-7** — **closed, verified** this pass.
- **P1-17 remainder** — 17b **closed, verified**; the tie back-reaction
  re-march away from the low-branch anchor still **open** (queued).
- **P1-18a** — **closed, verified**. **P1-18c** (single 320×80 flat-plate
  grid) — **open**, GPU-gated per the response.
- **P1-m13** — **closed** (no change needed). **P1-m17** — **closed**.

## New major comments

1. **The eight new Appendix-C contour sheets are referenced by the tex
   but not in git** — the `paper/.gitignore` `figs/` trap, third
   occurrence of the class (P11-M2 → P12-M7 → now):
   `chi_sheet_eppler387_Re{60k,100k,300k,460k}_{upper,lower}.pdf` exist
   only on disk (Re100k pair at 09:42 — the current, ext-fork versions;
   the rest 06:50–06:51). A clean checkout of HEAD cannot build the PDF,
   and the uncommitted Re=10⁵ pair is precisely the field-level evidence
   for the monostability claim that 42c0a06's message cites. `git add -f`
   all eight — and consider replacing the blanket `figs/` ignore with an
   explicit build-artifact pattern so this class of gap cannot recur.

## New minor comments

1. **`fork_extension_results.json` is missing `ext_fork_cavL2` and
   `ext_up_cavL2`** — the runners load the shared JSON at start and write
   at end, so concurrent str/cav invocations (GPUs 6/7, and
   `run_dn_extensions.py` writes to the same file) clobber each other's
   entries; the cav fork/up rows of `tab:eppfork` therefore have no
   committed results record (the CSVs are committed; my recomputation —
   0.8598/0.03405 drift 8.8×10⁻⁵, 0.8592/0.03412 drift 7.7×10⁻⁵ —
   matches the table). Re-emit the two entries and make the writers
   read-merge-write.
2. **`history_diag`'s `end_slope_per_1000it` is actually per 10⁴
   iterations**: the CSV logs every 10 iterations, so the per-row fit
   slope × 1000 (`a*100*10`) is the slope over 10,000 iterations. The
   committed values overstate the drift 10× (conservative direction —
   the runs are even flatter than recorded), but a reader recomputing
   "per 1000 it" will disagree with the JSON by exactly 10×.
3. **Rounding inconsistency for the same state**: the cold unstructured
   10⁵ solution (final-20% median CL 0.8595) prints as **0.860** in
   `tab:eppresweep` and **0.859** in `tab:eppfork`'s cold column. One
   rounding convention (or one generator) for both tables.
4. **"convergence takes 4–6×10⁴ iterations" (l. 2538) — criterion
   unstated and the band reads high**: by a rolling-2000-iteration range
   <10⁻³ criterion the paths settle 2.6–3.2×10⁴ (str) and 4.1–4.5×10⁴
   (cav) after arriving at 10⁵. "3–4.5×10⁴" reproduces, or state the
   criterion that gives 4–6. (The companion "±0.06 excursions" is a fair
   reading: post-transient deviations span +0.03/−0.07.)
5. **6×10⁴ str fork vs "returns to the cold (burst) state within 20,000
   iterations"** (caption): the table's own entry is 0.607 against cold
   0.621 — ΔCL = 0.014, ~3× the 10⁵ agreement bound — and this run's
   last-1000-it drift is 2×10⁻³, 10× the extended runs' bound: it is
   still relaxing. The qualitative claim (attached memory lost, burst
   branch) is solid; either extend it as the 10⁵ paths were, or soften
   to "collapses onto the burst branch within 20,000 iterations".
6. **`regen_resweep_table.py` still emits a "$100$k warm" LaTeX row**
   with the superseded 20k-transient fork medians (0.727/0.0472,
   0.824/0.0377) — the exact content 42c0a06 removed from the paper. A
   reader running the committed repro script gets a row the table no
   longer has, at values the extensions showed to be transients. Drop
   the row (or emit the extension values).
7. **`run_tau_test.py` constructs only the 10⁵ case**; the 2×10⁵ case
   the tex cites by name (`tautest_strL2_Re200k_a5_tau1`) has no
   committed constructor. Parameterize the script over Re (the dir's
   `ai_constants.log` proves the run; the repro chain should too).
8. **Response-record nit**: the 0930 response says the 10⁵ repeat rms
   "0.012–0.018" is "stated in Sec. V.A" — the text says only "coincide
   to repeat accuracy" (the 0.06 / 4× branch number *is* quoted). Either
   add the number in parentheses or amend the record; the current
   sentence is accurate as written.
9. **Stale pre-verdict framing in committed repro docstrings**:
   `run_continuation_ladders.py` still motivates the up-ladder with
   "persists upward into the model's bistable band, i.e. hysteresis in
   Re" — the experiment's outcome was the opposite. One outcome line
   ("result: monostable; see Sec. V.A") keeps the script from reading as
   a claim the paper no longer makes.
10. Typo scan of the pass's changed passages (Sec. V.A/V.B l. 2488–2675,
    the τ addition, the Fig 1–4 captions, `tab:epplit` and its paragraph,
    the Sec. II.F insertion, the Conclusion rewrites): clean — no doubled
    words, no bare refs, no undefined labels; the only wording items are
    minors 3–5 above.
