# Response to review `2026-07-23-0658.md` (paper/main.tex)
*Edits applied 2026-07-23. This round addressed the adjudicated EASY items; the remaining items are deferred with reasons below. Compile verified: pdflatex + bibtex + pdflatex x2, zero errors except the known missing `figs/indicator_sphere.pdf` (artifact lives on another machine), no undefined citations.*

## Major comments

1. **Q1/Q2 gate terminology / predecessor kernel** — DEFERRED: requires either the Daedalus final-kernel re-run or a predecessor-kernel appendix; handled separately.
2. **Predecessor-kernel 0.02c cross-reference unsupported** — DEFERRED: requires the kernel-vs-kernel 2D comparison data (entangled with M1).
3. **N=9 prediction-vs-input contradiction** — ADDRESSED: only the N=1 crossing is imposed. Fig. 4 caption now reads "meets the correlation at N=1 by construction ...; its N=9 crossing is an untuned prediction, agreeing with Drela's to ~1%"; Conclusion now says the threshold takes "its single free scale from the N=1 crossing ... alone---the N=9 crossing is then reproduced to ~1% as an untuned check." Grepped all other `N\!=\!9` sites (Sec. 2.4 line ~638 already states prediction; constants list says "anchored at the Blasius N=1 inception"); all consistent.
4. **N=1 anchor operational definition (338 vs 353)** — DEFERRED: requires restating what quantity was set to 338 when fixing c_R; handled separately.
5. **g notation collision** — ADDRESSED: SA wall-function auxiliary renamed g→g_w in eq:sa_closure (definition and use in f_w), with parenthetical "(the wall-function auxiliary $g_w$ is Spalart's $g$, renamed to avoid the Rayleigh coordinate $g$ below)". Mesh "growth-ratio offset $(g-1)$" replaced by words: "growth-ratio offset (the wall-normal growth ratio minus one)".
6. **P notation collision** — ADDRESSED: rate-coordinate use of P removed. Line ~504 now "the rate coordinate $\hat S g$"; the assembled gather no longer defines "P = \hat S g" (the coordinate is always written $\hat S g$; $Re_\Omega^{\mathrm c}$ already takes $\hat S g$ as argument everywhere). Grep confirmed no other P-as-coordinate uses in Secs. 2.3–2.7. P is reserved for the blended production.
7. **Signed 2D theory vs magnitude 3D formulas** — DEFERRED: needs a careful reconciliation argument; handled separately.
8. **Bubble over-length magnitudes** — ADDRESSED: Conclusion now mirrors Sec. 5's verified reattachment offsets: "reattaches systematically late at low incidence---0.04–0.06c aft of the measured oil-flow reattachment at alpha<=5deg (0.78 vs 0.74c, 0.73 vs 0.67c, 0.64 vs 0.59c), and 0.03c early at 7deg"; phrased as reattachment offsets, not total length.
9. **e^9 cross-code agreement footnote** — ADDRESSED: tab:eppresweep footnote now reads "...agree to better than 1% in c_l and c_d, and to 0.005c in x_tr with one exception: the NLF alpha=0 lower-surface front, where the near-flat envelope lets mfoil (0.62c) and XFOIL (0.52c) separate by 0.10c (Sec. 4)."
10. **lambda_p undefined / lambda collision** — ADDRESSED: defined at first use in Sec. 4 as the local Pohlhausen-type parameter $\lambda_p = -d^{2}(\mathbf{u}\cdot\nabla p)/(\rho\nu|\mathbf{u}|^{2})$ (positive favorable, negative adverse), explicitly "a diagnostic only, not to be confused with the sphere longitude $\lambda$: the model itself contains no pressure-gradient parameter." Nomenclature entry added.
11. **Onset gate form arrives late; clip notations undefined** — ADDRESSED: eq:onset now gives $S(\zeta)=\tfrac12[1+\tanh((\zeta-1)/w)]$ inline with "$w$ the ramp half-width"; $\operatorname{clip}\langle x\rangle_0^1=\min(\max(x,0),1)$ defined inside eq:rate at first use; $\langle x\rangle_+=\max(x,0)$ defined at its first use (assembled model). S not renamed (per adjudication).
12. **Onset three-constants figure (LST collapse)** — DEFERRED: requires generating/committing the collapse figure; handled separately.
13. **Fig. 1 missing from build** — DEFERRED: `figs/indicator_sphere.pdf` generator/PDF live on another machine; known build artifact, handled separately.
14. **Numerics/implementation section** — ADDRESSED: the implementation paragraph (compact viscous-flux-style Laplacian for $u''$, one-ring dual-volume-weighted averaging, conservative failure mode, factor-3 reduction in cavity-vs-O-grid front spread, $\hat S$ regularization, analytic production Jacobian treated implicitly, one Laplacian pre-pass cost) inserted verbatim. Judgment call: the paper has no standalone "Solver and seeding" section (no `sec:impl` label exists), so the paragraph was placed immediately after the Flow360 solver-and-seeding paragraph at the head of Sec. 3 (sec:flatplate), which is where the solver is introduced; the paragraph's forward reference to Sec.~4 (sec:nlfval) works from there.
15. **Abstract minimality + missing 3D result** — ADDRESSED: (a) now "a blended production term---with the destruction tied to it and the molecular diffusion coefficient reduced in the $\tilde\nu$ equation only---drives the SA transport equation, unchanged in structure..."; (b) added "The model is also deployed in three dimensions on the wing of the Daedalus human-powered aircraft."
16. **Sec. 2.5/2.6 ordering** — DEFERRED: section reordering handled separately.
17. **Sec. 6/Fig. 3 vs Appendix B numbers (H=4.03 vs 3.98; 30% vs 0.9–1.9x)** — DEFERRED: number reconciliation handled separately.
18. **(a) literature-comparison table** — DEFERRED: handled separately. **(b) missing citations** — ADDRESSED: added bib entries and cites for AVL (Drela & Youngren, software, MIT; cited at first AVL mention in Sec. 6), Construct2D (Prosser, sourceforge; cited at first mention in Sec. 4), Falkner & Skan 1931 Phil. Mag. (cited at the first Falkner–Skan use in Sec. 2.1), and Stewartson 1954 (entry already existed in references.bib, was uncited; now cited at the reversed-flow/Stewartson mention in Sec. 2.1). Judgment call: the two software entries carry commonly cited release versions/years (AVL Ver. 3.36, 2017; Construct2D Ver. 2.1.4, 2014) to avoid "????" in the AIAA bibliography style — author should verify. **(c) flat-plate grid statement** — DEFERRED: handled separately.

## Minor comments

1. ADDRESSED: doubled word removed — "sphere rate coordinate $\hat S g$".
2. ADDRESSED: "the blended production $P$ (its amplification branch built on $\hat S g$)".
3. ADDRESSED: "Table 1 had one empty cell---...---which the last row fills".
4. ADDRESSED: eq:scaling now "$u'\ell/\nu \lesssim 1$".
5. ADDRESSED: "(with the two-dimensional sign convention $\omega=-u'$ used in this section; elsewhere $\omega$ denotes the vorticity magnitude)".
6. ADDRESSED: "$\alpha$ is locally the streamwise wavenumber, not the angle of attack, and the lower-case $p$ is this Orr–Sommerfeld density, distinct from the production $P$."
7. ADDRESSED: $\sigma_\mathrm{SA}$ → $\sigma$ in eq:transport and the following sentence (only two occurrences existed).
8. ADDRESSED: nomenclature — $\hat S g$ entry de-self-referenced ("the even product of shear fraction and Rayleigh coordinate"); $Re_\Omega^{\mathrm f}$ kill-line orphan completed via a new entry "$c_R, q, Re_\Omega^{\mathrm f}$: onset-threshold scale, exponent, and floor"; $w$ entry added; $\lambda_p$ entry added; $\sigma_P/\sigma_D$ were already listed (no sigma_t rename needed).
9. ADDRESSED: constants list line now shows the map itself, "$N_\mathrm{crit}(Tu)=-8.43-2.4\ln Tu_\mathrm{frac}$ — Mack $e^N$ receptivity map, Eq. (tumap), adopted wholesale"; symbolic (A,B) dropped.
10. ADDRESSED: verified against the figure generator (regen_daedalus_surface_maps.py: the chi=1 front is drawn black, linewidth 1.5 — bold, not cyan); body text now "(bold in the figure)", agreeing with the caption.
11. ADDRESSED: "the fault appears to lie in mfoil's Newton globalization".
12. ADDRESSED (verification only): the §II.2–II.5 staged-calibration citation at line ~952 already cites spalart_allmaras_1994 (the La Recherche Aérospatiale version); no change needed.
13. DEFERRED: Somers TP-1861 PDF is not in paper/references/ (folder holds only a README), so the "Fig. 11d" / "Fig. 9d" figure numbers could not be verified against the source; left as-is, owner to check against the NASA report.
14. ADDRESSED: at the assembled 3D restatement — "Velocities (and $|\mathbf u|$) are measured relative to the nearest wall point, which makes the indicators Galilean invariant; for the static-wall cases of this paper this is simply the absolute velocity."
15. ADDRESSED: $R=\sqrt{|\mathbf u|^2+(\omega d)^2+(\tfrac12 d^2\hat{\mathbf n}\cdot\nabla\omega)^2}$ written directly; the "$(X_0,Y_0,Z_0)$ unnormalized components" construction removed.
16. ADDRESSED: eq:transport text now "with the molecular part of the coupled model's cross-stream diffusion (...; the $\tilde\nu\nabla^2\tilde\nu$ self-diffusion and $c_{b2}$ gradient terms, $O(\chi)$ smaller in this range, are dropped---cf. Sec. 2.6)".
17. DEFERRED: equation citation-style unification (Eq.~\eqref vs bare \eqref) — cosmetic sweep handled separately.
18. ADDRESSED: $N_\mathrm{crit}\approx13.6$ now cites drela_daedalus_1988 ("the flight-environment amplification level of the Daedalus design work").
19. ADDRESSED: Fig. 2 caption "the one calibrated function, the threshold ...".
20. DEFERRED: rhetorical-question tone at line ~869 — style decision left to the author.

## Repository side effects

- references.bib: removed an exact-key duplicate `@article{alam_sandham_2000}` (second copy, wrong volume/pages 403/223–250; the kept first copy JFM 410/1–28 is the correct one and is what bibtex was already using). The duplicate was aborting bibtex before it reached the newly appended entries.
- New bib keys: falkner_skan_1931, drela_youngren_avl, prosser_construct2d.
- Build: `pdflatex; bibtex; pdflatex; pdflatex` — the only error is the pre-existing missing `figs/indicator_sphere.pdf`; no undefined citations, no new warnings beyond pre-existing bibtex page-field warnings.
