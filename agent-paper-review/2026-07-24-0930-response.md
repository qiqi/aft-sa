# Response to review passes 10 (`2026-07-23-2050`), 11 (`2026-07-23-2110`), and 12 (`2026-07-24-0551`)
*2026-07-24, ~09:30. Commits a9e7372 (appendix split, L2 table, repeat
datasets) and 2b5aac3 (this response's fixes). One finding of our own to
report first, because it dominates everything held open.*

## The warm-start runs were off-canon (found via pass-12 minor 2)

Writing the in-case `ai_constants.log` provenance that minor 2 demanded
exposed a real defect: the fork/ladder scripts exported the kernel
constants but not `AI_LAMINAR_SLOWDOWN`, so those runs resolved the
compiled default (1.0) where the campaign uses 0.01. The slowdown scales
the entire laminar residual *including the freestream-BC enforcement*
(SpalartAllmaras.h states the caveat), so the campaign case JSONs carry a
pre-compensated seed (`modifiedTurbulentViscosityRatio` = 0.01 × χ∞).
The fork/ladder clones copied that compensated JSON and ran at slowdown
1.0: their effective freestream seed was **100× low** — burst-biased.
Every wall-clock result behind `tab:eppfork` is therefore off-canon
(which may also be the source of the secular decay major 1 measured).
All four forks, the twelve continuation-ladder steps, and new
40,000-iteration extensions of both 10⁵ branches are re-running at the
canon now (`run_*` scripts fixed: `canon_env()` sets the slowdown and
documents the pairing; `write_ai_constants()` extracts the solver echo
into every case dir; the wrong-seed results are archived as
`*_wrongseed_archive.json`). `tab:eppfork`, the warm-start rows of
`tab:eppresweep`, the warm-start curves/markers in the three sweep
figures, and the Re=10⁵ χ-sheet fork rows will all be refreshed from the
canon runs.

## Pass 12

Majors:
1. (10⁵ forks still decaying) — HELD OPEN, now with the decisive runs in
   flight: 40k-iteration extensions of the warm branch AND of the
   up-ladder (burst-side) branch, both at the corrected canon env; the
   extension runner prints quartile medians and end slopes so
   plateau-vs-merge is read off the log. The subsection's claim will be
   rewritten to whatever those histories support; the abstract clause
   added under major 6 was worded on the bursting boundary only, as you
   recommended. Independent new evidence recorded meanwhile: slow
   continuation ladders (200k→150k→100k→80k→60k down; 60k→80k→100k up)
   found up-approach < cold < down-approach at 10⁵ with the two
   directions agreeing at 8×10⁴ — path dependence localized to ~10⁵ —
   but those were wrong-seed runs and are re-running too.
2. (60k "both bistable" contradiction) — ADDRESSED: "where the
   measurement is bistable and the computation most mesh-sensitive",
   with the branch question handed to Sec. V.A.
3. (two-branches-at-10⁵ overreach) — ADDRESSED at both sites: the
   sweep paragraph now says the experiment at 10⁵ is single-branched
   (branches at 6×10⁴ only), and the subsection opener is rescoped to
   6×10⁴ with the TM-4062 p.9 sentence quoted. STRENGTHENED with new
   data: we transcribed every repeat pressure dataset the report holds
   near α=5° (App. D pp. 162/163, 174/175, 184/185, 192/193, 198/199,
   and the runs-15/16 set at 144/145): at 10⁵ the three free-transition
   tunnel conditions coincide to repeat accuracy (upper-surface ΔCp rms
   0.012–0.018), while at 6×10⁴ the up- vs down-sweep differ by rms
   0.061 (4× repeat scatter) — the experimental branch scatter, now
   overlaid in fig:eppresweep_low and stated in Sec. V.A.
   (`data/exp_cp_tables.json` 'datasets_a5' with page provenance;
   `repro/cfd/compare_exp_cp_datasets.py`.)
4. (e⁹ "no closure" at 6×10⁴ falsified) — ADDRESSED: entry now
   0.15–0.2 (mfoil 0.676→0.825c printed; XFOIL closes at 0.879c,
   transition station not stored), the caption gains your closing
   observation (the e⁹ distance also stretches, far less than the
   handover), and the 4.6×10⁵ dash is explained (no stored XFOIL x_tr;
   bubble closes at 0.50c).
5. (two unreconciled mechanisms) — ADDRESSED with the reconciling
   clause at the l. 2223 site (under-amplified shear layer dominant at
   2×10⁵; stretching handover takes over as Re falls).
6. (abstract + roadmap) — ADDRESSED: one abstract sentence (bursting
   boundary + unmodeled transition zone; no bistable-band claim) and
   one roadmap sentence for Sec. V.
7. (three figures not in git) — ADDRESSED: all three force-added
   (2b5aac3); they will be re-committed when regenerated from the canon
   runs.

Minors: 1 (asterisk asymmetry) — folded into the post-rerun rewrite of
`tab:eppfork`. 2 (fork repro + ai_constants) — ADDRESSED: fork
construction is now in-repo (`clone()` from run_continuation_ladders,
invoked by the fork runner), and every fork/ladder dir now carries the
solver-echoed `ai_constants.log` — which is how the off-canon slowdown
was caught; the runners write it automatically now. 3 (Exp branch
label) — ADDRESSED in the caption (reattaching branch at α=4.99°;
non-reattaching reads ≈0.64). 4 (non-monotone Δx(1→30)) — ADDRESSED
(caption clause: past reattachment χ grows slowly for an unrelated
reason; the mechanism lives in the half-saturation column).
5 (no-closure convention) — ADDRESSED (within-0.04c-of-TE convention
stated; raw crossings 0.996–0.999c named). 6 (stale annotation) —
ADDRESSED in the generator ("no oil-flow bubble data"); figure
regenerates with the canon-run refresh. 7 (60k error bars) — ADDRESSED:
bars now the α=4.00-repeat half-spreads (±0.04, ±0.0023), derived in
the caption. 8 (wording ×3) — ADDRESSED ("fork the two L2 solutions";
"that χ must climb"; Δx header notation unified). 9 — no action.

## Pass 11

Majors: 1 (steadiness sentence falsified) — ADDRESSED with your
suggested shape: away from 10⁵ flat to <5×10⁻⁵ over the final 2000
pseudo-steps; at 10⁵ all grids but L0 unsteady (3×10⁻⁴ cavL1, ≈7×10⁻⁵
both L2s, ΔC≈0.01 strL1 cycle, medians tabulated). 2 (suite figures not
in git) — ADDRESSED (see P12 major 7).

Minors: 1 ("within 0.02c" → ≈0.02c, both clauses) — ADDRESSED.
2 (structured-L0 attribution, plural fronts) — ADDRESSED. 3 (recovery
stations "on the two finer levels", body + highRe caption) — ADDRESSED.
4 (7→8×10⁻⁴ at L1) — ADDRESSED (the sentence is now L2-led:
"≤2×10⁻⁴ (8×10⁻⁴ at L1)"). 5 (NLF cavL1 α=15° disclosure) — ADDRESSED
(parenthetical: iteration budget reached, lower front wandering
0.65–0.67c, final-batch value tabulated). 6 (Conclusion 0.03–0.04c) —
ADDRESSED (0.031c transition front at 9°; 0.035c reattachment at 7°,
named as such). 7 (stale scratch JSON) — deleted. 8 — no action.

Note: the table quoted in pass 11 as L1 is now L2 end-to-end
(tab:eppresweep with a warm-start row; a9e7372), so the L1-specific
prose it verified has moved — the L2 numbers were re-extracted by the
same generator (`regen_resweep_table.py`, now level-parameterized).

## Pass 10

Majors: 1 (destruction "switched off") — ADDRESSED with your clause:
production off outright (σ_P=0 for χ≤1), tied destruction floor inert
at the disturbance's scale (<10⁻³ of the amplification). 2 ("nearly
three times Blasius's") — ADDRESSED: "nearly twice Blasius's in Re_θ
(over four times in Re_x)".

Minors: 1 (duplicate "collapses the inflectional content") — ADDRESSED
at both surviving sites (now "keeps the profile's curvature
single-signed", matching the definition site). 2 (em-dash stack) —
ADDRESSED (inner clause parenthesized; "there" → "on the favorable
side"). 3 (graze ~1.3) — ADDRESSED: "grazing 1.27–1.34 high for
β≥+0.25", caption + body. 4 (fig03 docstring) — ADDRESSED (+0.10).
5 (max(tops) guard) — ADDRESSED (empty-sequence fallback). 6 — no
action.

## Also in this round (author-directed, for the record)

- TM-4062 stored-table audit: the 300k lower-surface column had a
  one-row slip from x/c=0.650 (a fabricated value near the TE) and the
  460k upper grid listed 0.031 for 0.030 — both fixed against the
  scans (pp. 120/121, 156/157); the 100k column's provenance is now
  pinned (runs 15/16, Pt=15 psi, M=0.03, pp. 144/145). All other
  stored values verified digit-for-digit.
- Appendix split A–D (one per main-text section) with per-Re contour
  sheets for the sweep, including warm-start rows at 10⁵.
- A bubble-resolution probe (`repro/cfd/analyze_bubble_resolution.py`):
  at 10⁵ the separated shear layer over the bubble's front half has
  ~6–10 points across at L2 (2–6 at L1/L0) while the TE/wake merging is
  the best-resolved structure (12–17 across the lower BL at the TE,
  40–56 across the near-wake deficit); the reverse-flow height grows
  L0→L1→L2 with the L1→L2 step the larger — fold-point behavior, not
  smooth grid convergence.

## Long-standing (unchanged)

P1-1/P1-2 (awaiting the Daedalus canon campaign, running: ogrid+cavity
L1 complete, L2 phase in progress), P1-7, P1-17 remainder, P1-18a,
P1-18c, P1-m13.

## Addendum (~10:40): P1-7 closed

The signed-theory / magnitude-implementation gap now has its reconciling
paragraph at the 3D restatement (Sec. II.F), written from the solver
source: the implemented triple on a parallel layer is
(sign(u)·u, sign(u')·du', sign(u)·½d²u''), a legitimate projective
representative wherever velocity and shear share a sign — every attached
profile and all of a reversed one except the backflow-extremum-to-
velocity-zero layer — so every even product coincides with the theory
exactly there (answering (c)), and the calibration, marched on attached
profiles, transfers unchanged. In the mixed layer the deviation is a
single component's sign and switches on/off continuously (Y=ωd vanishes
at one end; at the other the curvature projection's sign step has
amplitude ½d²|u''|, near-zero because the velocity zero of a free-shear-
like profile tracks its inflection — answering (a) and (b); Ŝ stays
smooth through u=0 since |u| enters it only via u²). The overclaiming
"identical for a parallel layer" sentence is corrected to the projection
identity sign(u)·u''.

## Addendum (~10:55): P1-m13 closed

TP-1861 fetched from NTRS (submission 19810015487) and the two figure
numbers checked against the source: Figure 9 is "Transition location
(M = 0.10)" with panel (d) at R = 4.0x10^6 (open/solid orifice symbols),
and Figure 11 is "Section characteristics for various Reynolds numbers
at M = 0.10" with panel (d) at R = 4.0x10^6. Both citations in the paper
are correct as written; no change needed.

## Addendum (~11:20): P1-17(b) reconciled

The Conclusion's "within ~30% of the correlation" now quotes the same
measurement as the Appendix-D ledger — 0.9–1.9x the correlation on
frozen reversed-flow profiles — with the caveat (already argued in
App. D via the Dini/Green tanh family) that the correlation itself is
the weaker reference at those depths. One number, one story. Still open
from P1-17: the tie back-reaction re-march away from the low-branch
anchor (queued; march-instrument work). P1-18a is in progress (a
literature sweep for published AFT / gamma-Re_theta / BCM Eppler-387
2e5 results with verifiable provenance is running); P1-18c (flat-plate
refined-grid statement) is GPU-gated behind the canon re-runs.

## Addendum (~12:30): P1-18a closed

`tab:epplit` (end of the Eppler benchmark section): the published
gamma-Re_theta (ICAS 2024, OpenFOAM), SA-BC (AIAC-2017 + ICAS 2024,
SU2), and one-equation gamma-SA (D'Alessandro et al. 2025) Eppler-387
results at Re=2e5 near alpha=5, against this work and the LTPT
measurement -- with the provenance honestly declared (figure-read from
open sources, read-off uncertainties quoted; the only text-stated
literature numbers are the ICAS bubble-error percentages). The AFT row
is cited without numbers: its E387 validation exists only in non-open
venues (AIAA 2013-0253 / AIAA J. 52(11); Coder's PSU dissertation,
ETDA 22627, is nominally open but bot-gated). Three new bib entries.

## Correction (~13:10): AFT row of tab:epplit

The author supplied the AIAA J. 52(11) AFT paper (paper/aft.pdf).
Reading it directly falsifies the search agent's inference that the
2013/2014 AFT papers contain an E387 validation: the journal
validation set is the flat plate, a winglet section, and the slotted
S414, all at Re ~ 1e6 -- no Eppler 387 anywhere in the text. The
tab:epplit AFT row and its paragraph now record the absence of any
published AFT datum at this condition instead of claiming an
inaccessible one.

## Addendum (~12:00): annotated round 5 (10 remarks)

1. Fig. 1: -0.15 member replaced by -0.16 and a near-separation attached
   -0.19 (gold) -- seven profiles, evenly filling toward separation.
2. Fig. 2: +0.35/+0.25 removed, +0.10 added; -0.17 replaced by the
   attached -0.19. The plotted family's graze statistics are unchanged
   (0.93-1.14, rms ~6%); the beta>=+0.25 clause moved out of the caption
   (body keeps it, marked "beyond the plotted family").
3. The unclosed-looking mega-parenthetical in the march paragraph
   (created by the P10-M1 fix) is broken into sentences.
4. Fig. 3: per-row max Shat*g (0.162/0.078/0.037) now listed in the
   caption, removed from the panel labels.
5/6. Fig. 3's untouched-equation envelope (gray) removed from figure,
   legend, and caption; the body paragraph quoting its numbers keeps
   them, marked "(not plotted)".
7. Fig. 4 caption: "set entirely by a_max=0.19 and c_nu,ai=1/6".
8. "reflecting the one calibrated function ..." clause removed.
9. Attached-family range stated: H=2.44 (beta=+0.15) to H=3.98
   (separation limit).
10. beta~0.45 body sentence now carries its max Shat*g ~ 5e-4.

## Addendum (~13:40): the tau falsification experiment (author discussion)

Author question: could the handover deficiency of Sec. V.B be fixed by
tuning the sigma_t width tau alone? Registered prediction from the
e-fold arithmetic: no -- ln(c_v1) ~ 2 of the ~3.4 required e-folds live
in f_v1, not tau. Test: cold strL2 at 1e5 with AI_SWITCHWIDTH=1 (canon
4). Outcome: the prediction FAILED informatively -- tau=1 does move the
mean strongly (CL ~ 0.97, CD ~ 0.032) but by converting the steady
too-long handover into a relaxation oscillation of amplitude
Delta-CL ~ 0.3 (0.88-1.16): production closes the bubble, closure
destroys the amplification fetch that fired the gate, the gate un-fires
and the bubble reopens. Sharpened diagnosis: the sigma_t(chi) gate is
memoryless/reversible while breakdown is irreversible; the remedy needs
an outer-scaled trigger (nuHat*omega/U^2 at a mixing-layer saturation
level) AND latching/transported memory (as transported-intermittency
models have -- why Langtry-Menter's gamma_sep fix holds steady where a
narrow algebraic ramp oscillates). A tau=1 run at the 2e5 benchmark is
in flight to check whether canon tau=4 is also load-bearing for
benchmark steadiness. Cases: tautest_strL2_Re{100,200}k_a5_tau1
(repro/cfd/run_tau_test.py).

Follow-up: the tau=1 run at the 2e5 benchmark ALSO limit-cycles
(CL 0.935-1.016, dCL ~ 0.08, vs the canon's <5e-5 flatness) -- canon
tau=4 is load-bearing for steadiness at every Reynolds number, and the
"just tune tau" option is closed with data at both ends. Sec. V.B's
future-work sentence now carries the mechanism-level specification
(outer-scaled trigger + transported breakdown memory) with the tau=1
evidence cited; the Conclusion's future-work item matches.

## Addendum (~15:00): the amplifying-zone gate A (author discussion, verified)

Why chi stops growing after crossing 1: every brake scales as
chi/Re_Omega -- (i) destruction (tie floor 0.751, D/P = 1 at chi ~ 2.7
for Re_Omega ~ 130), (ii) the f_v2 dip halving S-tilde by chi ~ 7 at
that Re_Omega, (iii) the amplification fade (1-sigma_P). At the 1e5
bubble's shear-layer Re_Omega ~ 1e2 the brakes engage at chi ~ 3-5;
at 2e5+ they engage late enough that the handover completes.

Proposed remedy (local, algebraic, reversible -- relaminarization-safe
by construction): suspend brakes (i)-(ii) by the amplifying-zone gate
  A = (1 - sigma_P) * clip< sign(Shat) * max(0, |Shat| - 1/sqrt(2)) * g >_0^1
(even on RP2: both factors odd). Exactness: on the linear laminar
sublayer Shat = 1/sqrt(2) and g = 0 EXACTLY, so the factor has a
quadratic zero there; in the log layer |Shat| ~ 0.15 < 1/sqrt(2) so the
max() clamps it to exactly zero; in turbulent free shear sigma_P = 1.
Verified: analytic law-of-wall max 3e-5 (y+ 0.1-1e4); NLF 4e6 attached
turbulent BL, 20449 slice points, max 8e-6; E387 1e5 bubble front,
max 0.31 (alive where intended). Application: sigma_D -> sigma_D*(1-cA)
and the f_v2 correction suspended by the same weight (c a saturation
constant to pin on the march rig). Next: the frozen-separated-profile
parabolized march INCLUDING the SA handover -- canon must reproduce the
chi-stall at ~3-5, the gated version must run through chi=30, Blasius
and high-Re marches untouched -- then a solver prototype at 1e5/2e5 +
one flat-plate point.

## Addendum (~14:30): march rig results -- stall reproduced; gate v1/v2 falsified

repro/analytic/march_sa_handover.py (parallel frozen-profile mode, the
bubble caricature): at pinned Re_Omega=400 the canon reproduces the CFD
pathology exactly (chi races to 1 at slope 2.1, then creeps at 0.008,
stalled at chi~13); at 1300 the handover completes un-gated (ratio
0.999 -- why 2e5+ never failed); at 130 the disturbance is
drain-limited below chi=1 (the CFD crossing happens at higher local
Re_Omega). Blasius guard: gated == ungated, identical.

Gate v1 ((1-sigma_P) fade): disarmed by its own fade exactly where the
brakes bite (sigma_P=0.95 at chi=13). Gate v2 (exp(-chi/300) fade):
still ineffective (13.3 -> 14.3) -- so the dominant brake at chi~13 is
NOT destruction or f_v2. Budget arithmetic points at nuHat
SELF-DIFFUSION (never enumerated): grows prop. to chi, ratio to
production chi/(sigma*a*Re_band) ~ 1 at the stall, redistributing the
peak into the near-wall destruction sink. NEXT: instrument the rig to
print the full term budget (P_ai, P_SA, D, molecular drain,
self-diffusion) at the chi-max at the stalled state, then design the
suspension against the measured budget, not arithmetic.

## Addendum (~14:35): the stall's term budget -- it's the SELF-DIFFUSION

At the stalled chi_max=13.3 (ReOm=400), per-nuHat rates / (a*omega):
P_ai=0.024, P_sa=0.672 (f_v2 dip negligible: S-tilde/omega=0.99),
D=0.078 (f_w=0.14 -- destruction tiny), DIFFUSION = -0.617. Net ~ 0.
The brake is nuHat self-diffusion: at chi=13 the diffusivity is ~20x
molecular and spreads the peak faster than production refills it (the
integral grows; the max stalls). Same chi/Re scaling as the original
enumeration, different term -- which is why suspending destruction/f_v2
(gates v1/v2) did nothing. Gate v3: also suspend the self-diffusion,
nu_diff = (c_nu_ai*nu + exp(-cA)*nuHat)/sigma; free-shear spreading
lives at chi >> chi_A so the safety argument is unchanged. To test on
the rig next.

## Addendum (~14:40): gate v3 falsified; the localization is the obstacle

Suspending the self-diffusion by the same pointwise gate moves the
ReOm=400 stall only 13.3 -> 16.8. Reason, from the stalled state: the
chi peak sits at the shear layer's OUTER edge (max local Re_Omega),
where |Shat| < 1/sqrt2 -- the pointwise (Shat,g) gate is off exactly at
the peak. During the handover the amplitude field detaches from the
instability's sphere-locus (diffusion pushes the peak above the
amplifying band), so NO pointwise gate keyed to the local instantaneous
indicators can cover it. Three falsified variants (v1 (1-sigmaP) fade,
v2 chi-fade, v3 +self-diffusion) -- a genuine negative result from the
rig. Candidate v4: a COLUMN gate (wall-normal max of clip<h*g> along
the profile, nonlocal in the same benign sense as SA's wall distance),
or revisit the premise per the author's earlier instinct (keep the
amplification itself active on the column where the band exists).
Rig: repro/analytic/march_sa_handover.py (all variants reproducible).

## Addendum (~14:45): gate v4 (COLUMN gate) PASSES the rig

A = exp(-chi/300) * [wall-normal max of clip<sign(Shat)max(0,|Shat|-1/sqrt2)g>],
suspending destruction, f_v2, and self-diffusion by exp(-cA):
ReOm=400 stall broken at c=15 -- post/pre slope ratio 0.815 (canon
0.004), chi=27 and climbing at domain end. Blasius guard BIT-IDENTICAL.
ReOm=1300: mild hot-side overshoot (ratio 1.17) -- watch at the 2e5
benchmark (its bubble is 0.05c too long, so the direction is right).
Complete rig-established chain: stall mechanism = self-diffusion
spreading (92% of production at chi~13); pointwise gates fail because
the chi peak detaches from the Shat>1/sqrt2 locus; the column gate
follows it. Open for the solver prototype: the wall-normal column max
is nonlocal -- implementable along wall-normal associations (the solver
already uses nearest-wall velocity), unstructured-grid strategy TBD;
c-sweep to >=0.9 slope ratio; then 1e5 + 2e5 + flat plate.

## Addendum (~15:00): the gate factor, elegant form (author request)

The |Shat|-1/sqrt2 dead band is Y <= X in disguise: for the
non-negative implemented indicators, |Shat| > 1/sqrt2 iff Y > X, i.e.
omega*d > |u|. The gate factor is therefore a single hinge times the
existing Rayleigh coordinate:

    G = <Y - X>_+ * g        (the SHEAR SURPLUS times g)

-- open where local shear exceeds local speed (the interior of a
detached shear layer), with both factors vanishing at the wall state
(X=Y, Z=0), the anchor of every profile: quadratic sublayer
insensitivity and exact log-layer zero, no sign functions or
thresholds. Re-verified on the fields: NLF turbulent BL max 0.000000
(exact to 6 decimals); E387 1e5 bubble 99pct 0.117 (broader working
support than the |Shat| form). Same zero set, smooth positive factor
difference -- c rescales slightly; rig rerunning in hinge form.

## Addendum (~15:10): gate v5 (POINTWISE, large c) PASSES -- prototype spec complete

The sphere-locus measurement corrected v3's diagnosis: the stalled peak
sits just PAST the wall meridian (Y/X=1.14, Z/R=-0.60) -- the pointwise
gate was weak there (~0.05), not zero. Since the safety zeros are exact
(log layer) and quadratic (sublayer; field max 0.000000), c is free to
be large. v5 = pointwise hinge gate, c=100: ReOm=400 stall broken
(slope ratio 1.50, chi 1->30 in ~1 unit), Blasius BIT-IDENTICAL at
c=100. Watch: hot-side overshoot (1.5 / 1.34 at 400/1300) -- fast
breakdown as desired, oscillation risk for the coupled solver to
settle (two self-limiters the tau=1 case lacked: the chi-fade, and the
mean-flow feedback that collapses g behind the front).

PROTOTYPE SPEC (all local, reversible):
  G = <Y-X>_+ * g      (normalized indicators; shear surplus x Rayleigh)
  A = exp(-chi/300) * clip<G>_0^1
  suspend by exp(-cA), c ~ 30-100 (sweep):  destruction, the f_v2
  correction, and the nuHat self-diffusion.
Rig history: v1 (1-sigmaP fade) self-disarming; v2 chi-fade pointwise
weak; v3 +self-diffusion still weak; v4 column-max works but nonlocal
(rejected); v5 pointwise large-c works. repro/analytic/march_sa_handover.py.

## Addendum (~15:15): option ledger closed -- the self-diffusion must be confined

Author preference tested: leave ALL Spalart terms untouched and instead
keep the amplification live in the gated zone (w_ai = max(1-sigma_P,
1-exp(-cG))). FAILS: ReOm=400 stall 13.3 -> 17.5, ratio 0.003, because
diffusion-limited spreading is self-adjusting -- added production
sharpens the peak and the diffusive sink grows with the peak's own
curvature. Empirical ledger: destruction+f_v2 suspension alone (v2/v3)
fails; amplification persistence alone (option b) fails; suspension
INCLUDING self-diffusion (v5) passes. The chi-fade constant (300) was
also killed by measurement: the 1e5 wake sits at chi 20-150,
overlapping the protection window, so NO chi scale separates them.
Final formulation direction: G = <Y-X>_+ * g; destruction and f_v2
suspended by exp(-cG) unconditionally (they are 1/d^2-inert in wakes);
the SELF-DIFFUSION suspension confined to the marginally-supercritical
window via the model's own threshold, S-window on Re_Omega/Re_Omega^c
(wake: 1e6 vs handover: 1e2-1e3) -- to be rig-tested as the final
variant, then the solver prototype.

## Addendum (~15:45): c_b2 restored; superlinear ledger final

The rig had omitted Spalart's c_b2 gradient term (quadratic SOURCE,
zero at peaks) -- overstating the column diffusion brake ~2.6x. With it
restored: canon ReOm=400 STILL stalls (chi~15, ratio 0.004) -- the
diagnosis survives the complete equation set -- and the v5 gate still
breaks the stall but overshoots at c=100 (ratio 1.98): the c-sweep to
slope ratio ~1 (expect c~20-40) is now the genuine calibration.
Superlinear ledger (author question): destruction (quadratic sink,
wall-weighted; 8:1 dominated by diffusion AT the peak), self-diffusion
(quadratic peak sink; net column brake (1-c_b2)/sigma * int (grad nu)^2),
c_b2 term (quadratic source, zero at peaks), f_v2 correction (small).
All other terms linear in nuHat. Next: c-sweep + ReOm-window for the
diffusion suspension; then the solver prototype.

## Addendum (~16:00): SA sustains low-Re turbulence -- AT a borderline chi

Author question: does baseline SA even support turbulence at the bubble
reattachment Re_theta, or would it relax to chi~c_v1? Sustainment test
(pure SA, sigma_P=1, frozen Reichardt profile, SA log-solution init,
2000 delta fetch): SUSTAINED at every Re_tau in {100,200,400,1000}
(Re_theta 176-1887), equilibrium chi_max = 8.8/18.4/37/94 --
chi_eq ~ 0.09*Re_tau. No missing attractor; BUT at reattachment
Re_theta ~ 180-350 the attractor lives at chi ~ 9-18, just above c_v1.
CONSEQUENCES: (1) the Delta-x(1->30) handover metric is mis-scaled at
low Re (chi=30 exceeds the local equilibrium) -- Sec. V.B's table and
the gate's completion target must be re-anchored to chi_eq(Re_theta)
or a nu_t criterion; the rig's "stall at 13-15" is partly ARRIVAL at
equilibrium. (2) The reattachment question reframes: equilibrium
nu_t ~ 6-10 nu exists -- is it delivered at the right place fast
enough? A smaller, better-posed problem than "climb to chi=30".
Test: /tmp-scripted, to be committed as repro/analytic/sa_sustain.py.

## Addendum (~16:15): far-field BC exonerated; 60k Re_theta measured

Author question: does the near-zero freestream chi (ours) vs classical
SA far field (chi~3) change low-Re sustainment? NO -- Dirichlet
far-field sweep (domain 2.5 delta): identical equilibria for both BCs
at every Re_tau (7.7/16.7/34.0 at Re_tau=100/200/400; chi_eq ~
0.08*Re_tau). The clean freestream needed for transition is NOT in
tension with sustaining the turbulence. Measured from the CFD: the 60k
bubble shear layer lives at Re_theta ~ 220-330 (1e5: ~400-1000) -->
reattached-turbulence equilibrium chi ~ 6-9 at 60k, AT c_v1
(f_v1 ~ 0.5, nu_t ~ 3-4 nu): SA turbulence at the 60k reattachment is
intrinsically half-strength -- the deep reason the 60k branch structure
is out of the model's reach, and a quantitative floor statement for
the paper. sa_sustain.py extended (Dirichlet BC variant in-file next
session); Re_theta extraction one-liner in this addendum's history.
