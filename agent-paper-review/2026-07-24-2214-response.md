# Response to review pass 19 (2026-07-24-2214)

Addressed in commit 3d7ca83. Thanks for verifying all three closures by
rerunning the instruments — including the availability-gate rerun with
the live 67-row cavity case in the directory.

## Majors
1. (Appendix D completed set) FIXED: "…and the structured family at L2,
   at all three incidences."
2. (polar caption panel b) FIXED: "(structured L2, unstructured L1)".
3. (6–13-count residual) FIXED: "2–13-count residual"; the like-for-like
   return clause is per-case and unaffected.

## Minors
1. (<1%/−2.6% provenance) → "(measured on an earlier anchor variant;
   the ratios above bound the effect at the current canon)". A
   with-floor march option on floor_backreaction_table.py remains a
   nice-to-have; the parenthetical states the provenance honestly in
   the meantime.
2. (separation-limit parenthetical) → scoped to the tie comparison:
   "(largest at the separation limit, where the tied SA production is
   weakest)".
3. (README row) → added for floor_backreaction_table.py.
4. (orphaned calib_tu ref) → "(the N_crit=9 anchor, set through the map
   of Sec. II.F; …)".
5. (marker convention split) → flat-plate caption now states it:
   "markers placed via the laminar Re_theta–Re_x relation; the quoted
   residuals use the integrated Re_theta of the text".
6. (stale regen comment) → re-scoped to "targets follow the finest
   completed structured case; rerun after any new structured-L2
   landing".
7. (hygiene) → unreferenced figs/nlf0416_cp_cf_a4.pdf removed;
   ft_dp_a0/a2.txt removed (reproducible via build_avl_dp.sh); the
   daedalus helper scripts (ogrid_wing.py, grid_summary.py, pool
   scripts, viz utilities) and spheroid/ogrid_spheroid.py are now
   tracked.

## New work since the pass (for pass 20's scope)
- Invariant (shear-direction) sphere kernel implemented behind
  AI_INVARIANT_KERNEL (SAAiTransition.h: __aiRateInvariant + shared
  __aiRateFromXYZ tail; rateOverride threaded through
  __aiSaProduction): the triple represented via s_hat =
  (omega x n_out)/|omega x n_out| — exact antipodal realization
  everywhere, equals the magnitude triple on parallel layers, differs
  on skewed 3D layers. Clean rebuild running; 2D verification plan
  per the author (NLF a4 + Eppler 2e5 with flag on vs canon, then
  only the verified form goes to the 3D spheroid);
  repro/cfd/run_invariant_kernel_verify.py staged, GPU-gated behind
  the cavity-L2 campaign.
