# Response to review pass 25 (2026-07-25-1207.md)

Both majors and all six minors addressed. The two majors were both
mine and both real — and major 2 corrects an explanation the author
had already liked, so it is flagged prominently in RESPONSES.md too.

## Major 1 — y+ factor of 10: FIXED
Exactly the diagnosed slip: u_tau = sqrt(Cf/2) without the xU_inf
(=M=0.1) in solver units. Corrected to y+~42 (window top) and y+~6
(peak 2). Agreed that peak 2 at y+~6 reads better — the "new
near-wall reattachment layer" now sits in the sublayer of the forming
wall layer, which is what the words claimed.

## Major 2 — the notch is probe-window truncation: REWRITTEN
Accepted in full. The paragraph now says what the committed data
shows: the lifted layer leaves the fixed 0.01c window at the notch
and stays saturated just above it until x~0.69 (full-depth envelope
never below 0.85); the trace re-rises as the layer descends through
reattachment; the chi crossings bracket the notch correlationally,
not causally. The "diffuses exactly the inflection" and "necessarily
notches at their crossing" sentences are gone. The row-2 window depth
(0.01c) is now stated where the trace is introduced in the paragraph.
My earlier "deeper probe shows the residue dying by 0.55" was a
misreading of an argmax jump between two branches both at ceiling —
corrected in RESPONSES.md as well, since the author had endorsed the
causal version.

## Minors
1. SA-BC calibration-circularity clause restored in the tab:epplit
   caption (cites cakmakcioglu_2017).
2. "slightly ahead of the e^9 crossing" — adopted verbatim (and
   agreed it strengthens the slow-handover argument).
3. "over-feeds" replaced by what was meant: the inflectional profile
   saturates the rate coordinate (P~1) across the mid-bubble.
4. "at grid convergence, both families reattach systematically late".
5. Comma added.
6. Per-level metric inputs (gamma, h_w, h_inf) now stated, the metric
   scoped to the interior, and the h_inf non-halving explicitly
   reconciled with the captions' halving claim.

## Status notes
- The fv1-bypass battery COMPLETED since the pass (32 runs).
  Headline: NLF and flat plate essentially inert (<=1.5 counts,
  mostly <=0.4); Eppler improves everywhere (-0.5..-3.9 counts at the
  converged conditions, -20 counts at Re=1e5) — but the sigma_t-tied
  s(chi) ramp appears to have WEAKENED the bypass relative to the
  original (1,2) window (the earlier exploration closed the 1e5
  bubble outright; the tied form only shifts it). Verdict discussion
  and next step (a width test) are with the author; if promoted, this
  becomes paper content and full review scope as you note.
- run_fv1bypass_battery.py's uncommitted edits are committed with
  this round.
