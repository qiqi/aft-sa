# Response to review passes 26 (remainder) + 27

All items addressed in one batch (commit follows).

## Pass 27
- Major 1 + minor 7: ALL 9-deg data dropped from fig:nlfworkshop (the
  points live in fig:nlfaft); caption scoped to "at every incidence
  shown", states the envelope's -4..8 domain, and notes the omission
  explicitly. xlim tightened to 8.5.
- Minor 1: "First AIAA CFD Transition Modeling and Prediction
  Workshop".
- Minor 2: "width reaches 0.4-0.6 at fixed incidence---the
  community-wide scatter the workshop documented" (attribution
  softened; no unquotable "headline result").
- Minor 3: caption now says the Piotrowski curves are digitized from
  the MANUSCRIPT version of Fig. 6(f), whose native ordinate is c_l,
  replotted against the sweep's alpha = -6..6.
- Minor 4: status sentence made pause-proof ("will be adopted
  wholesale when complete"). For the record the campaign RESUMED at
  ~16:45 after the Gram-form adoption (skip-completed guard added).
- Minor 5: data/fv1bypass_battery_results.json committed (16 slots x
  on/off CL/CD + dCD, protocol stated) — the II.F bounds now trace to
  a committed dataset.
- Minor 6: legend en dashes fixed; the envelope band is now GRAY so
  the C0 O-grid markers cannot vanish into it.

## Pass 26 remainder
- Major 3a: the inertness sentence now claims only the inner attached
  layer, states that the outer-edge descent DOES open the gate where
  fv1 is already near one, and cites the committed battery JSON for
  the <=1.5-count bound.
- Major 3b: the promised analytic note EXISTS —
  repro/analytic/fv1_qmargin_composite.py (Reichardt + Coles
  composite; q < 2 through the gated band for Pi <= 3.5 at any
  delta+ >= 100; near-separation Pi ~ 5-7 at low delta+ reaches
  q ~ 2.3-2.8, opening G partially). The II.F sentence is scoped to
  exactly that envelope. The reviewer's own Pi=7/delta+=100 probe
  (q ~ 2.8) is reproduced.
- Major 4: fig:nlfcfhigh caption corrected — the dash-dot Cp/Cf/xtr
  overlay is XFOIL's (with the x ue^2 conversion stated); the N
  envelope is FlexFoil's.
- Minor 1: Appendix E now says FOUR departures, the recovery recipe
  includes b == 0, and s, G, and Reichardt's U+ (with constants and a
  reichardt_1951 citation) are restated in the assembled model.
- Minor 2: the bypass blend weight is renamed b everywhere (w remains
  the onset ramp width alone); nomenclature updated.
- Minor 3: fig:eppbubble's minimum quoted in the freestream
  convention (+4.7e-4).
- Minor 4: a global conversion disclosure now lives in the Appendix-A
  conventions paragraph (covers the NLF sheets, the Re-sweep sheets,
  and the Daedalus strip rows).
- Minor 5: nomenclature nu_t line carries the blend.
- Minor 6: the eppresweep em-dash closes after the seed value.
- Minor 7: "no new calibrated constants".

## Also in this batch (context for the next pass)
The Gram-form presentation of the calibrated magnitude kernel was
ADOPTED (author decision; commit b8cac0e): Sec. III's realization
passage now defines the division-free representative (<u,u>,
sqrt(<u,u><d,d><omega,omega>), <l,u>) with the signed pairing's
four-variant rejection recorded and the odd crossflow pairing pointed
at the outlook. Algebraically identical to the implemented kernel —
no numerical changes anywhere. The fv1 campaign is resumed.
