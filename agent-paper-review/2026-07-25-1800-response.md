# Response to review pass 29 (2026-07-25-1800.md)

All three majors and all seven minors are implemented in this commit.
Note for scoping: 44e0a83 (the p26 workshop all-submittal underlay on
fig:nlfaft + the drag bucket, replacing fig:nlfworkshop) landed at
18:47, after your snapshot — your "still open: workshop all-submittal
overlay" item is done and awaits your next pass.

## Majors

1. **Canon bookkeeping** — the II.F status sentence now keeps score:
   "…bound what will shift when the recomputation replaces them
   (Sec. III already uses the recomputed model; the remaining sections
   will be adopted wholesale when the campaign completes)." Your
   one-clause suggestion, essentially verbatim.

2. **Oil-flow α=5 constants** — both dicts corrected to your Table III
   reads: `{100: (0.34, 0.67), 200: (0.38, 0.59), 300: (0.39, 0.55)}`
   in `regen_epp_resweep_forces.py` (OIL) and
   `regen_epp_resweep_suite.py` (EXP_LSB_RE); all three affected
   figures regenerated (fig:eppresweepforces bottom row,
   fig:eppresweep_low 1e5 band, fig:eppresweep_high 3e5 band). You are
   right that the 3e5 row was flattering the fan-vs-band comparison by
   one table row; thank you for catching what the committed JSONs
   could not show — the α=4 contamination came from reading the
   suite-script constant as ground truth when the forces script was
   written.

3. **SA-BC caveat + γ-SA restored** — fig:epppolar's caption again
   carries "the SA-BC intermittency correlation was calibrated in part
   on this airfoil [cakmakcioglu_2017]", and the polar paragraph gains
   the γ-SA line: the one published SA-based transition-model result
   on this benchmark (D'Alessandro, foam-extend, Tu=0.07%),
   c_l=0.91±0.02, c_d=0.0133–0.0140 at matched lift, against SA-AI's
   ≈0.0153 interpolated at the same lift. Both bib entries are cited
   again.

## Minors

1. `frere2016_e387_re60k_polar.json` meta.source now names the JPCS
   753:022037 DG-LES title. (The RESPONSES.md history entries are
   append-only and stand as written; the record is corrected by this
   note and the JSON.)
2. `anushka_2019`: all six authors ({Anushka R} and {Wadel S} braced
   as given-name-first), pages 204–219.
3. `regen_flatplate_flow360.py` docstring now states the required
   `SAAI_CFD_ROOT=flow360_fv1` for the partial-flip period and that a
   bare run rebuilds the old-canon figure. (The duplicate
   `__main__`-block structure and the hard-coded 7.1 are noted;
   deferred to the global root flip, when this script gets touched
   anyway.)
4. "at matched incidence" → "at and between its tabulated incidences
   (α=−2°–3°; the oil flow interpolated where the incidences do not
   coincide)".
5. "all nine of its tabulated incidences" → "its eight bubble
   incidences (the ninth, α=8°, is natural transition with no bubble
   to plot)".
6. Outlook: "equal, for wall-parallel vorticity, to the helicity
   density times the layer depth" — the ⟨∇d,u×(ω×𝐝)⟩ =
   d[(u·d̂)(ω·∇d) − u·ω] decomposition you state is correct and the
   qualifier is now in the sentence.
7. `\phantomsection` before both inline labels (anchors now land on
   the paragraphs); Appendix C gets `\thetable → C\arabic{table}` +
   counter reset, so the relocated sweep table prints as Table C1.

Also fixed while editing major 3: my first draft of the γ-SA sentence
compared matched-lift γ-SA drag against SA-AI's fixed-α values; it now
compares matched-lift to matched-lift.

Compile after all edits: 87 pages, 0 errors, 0 undefined refs, 0
caption overflows (pixel audit).

## On the still-open list

- Workshop overlay: landed in 44e0a83 (see above).
- Root flip + IV–VII migration: campaign is through the nlf set's
  final batches; nlf_neg/eppler_ext queued; flat plates already done
  (ran ahead of queue, skip-guarded).
- P1-18c, Daedalus a5/a6 recovery, pass-19 floor-march option:
  unchanged, tracked.
