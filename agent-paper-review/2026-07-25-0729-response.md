# Response to review pass 21 (2026-07-25-0729)

Addressed in the commit accompanying this record.

## Majors
1. (aphorism claimed deleted but not) FIXED — and the process point is
   taken. The round-5 replace target had a whitespace mismatch, silently
   no-op'd, and my per-edit verification covered the other items but not
   this one. Every replace in this round printed its own OK/FAILED
   check. The RESPONSES.md record is corrected in the same commit.
2. (sheet-conventions staleness, three sites) FIXED: (a) the Appendix A
   conventions now list the solid contours as chi = 1, c_v1, 30, and
   10^2 with their meaning; (b) the wall-normal-range sentence carries
   the Eppler-upper exception ("raised a further 5x, 0.0335c at 2e5, so
   the handover above the lifted bubble shear layer stays in frame");
   (c) the Appendix B intro quotes 0.0335c and drops the now-broken
   cross-airfoil scaling clause (the NLF frames are stated absolutely in
   Appendix A).
3. (fig:daepolar panel-b parenthetical) FIXED with the durable wording:
   "the finest completed grid per family and incidence" — survives the
   a5/a6 landings without another touch.

## Minors
1. (Somers provenance) → the cf-caption clause now cites Fig. 9d of
   Ref. somers_1981 in place.
2. (dx_tr/dN disappearance) → confirmed as a decision: the author's
   annotation struck the table and its paragraph as "no longer
   necessary" with the figure carrying the story; the sensitivity
   column went with its table. If a one-sentence N_crit-lever statement
   is wanted back, it can live in the fig:nlfaft caption — author's
   call, not restored unilaterally.
3. (regen_chi_sheets defaults/docs) → default root now flow360_fr
   (matching every sibling script; the flow360_tie default was the
   stale tree that caused the first regen run to silently skip
   everything — now it also cannot recur from a fresh checkout);
   docstring levels updated; the f_v1-90% comment corrected to chi~15
   with chi=30 as the >0.98 completion mark.
4. (two solver builds across the L2 pair) → verified no-op: the cavity
   a4 build's extra constant ai_fv1Bypass=0 gates a branch on
   (> 0.5) in SATurbulentViscosity.h:48 — at 0.0 the eddy-viscosity
   map is bit-identical to the prior build's code path, and no other
   constant differs between the two ai_constants.log files. Noted in
   RESPONSES.md alongside the family-agreement record.
5. (whitespace) → collapsed.
6. (log-accuracy nits) → RESPONSES.md corrected: at 4° the L2 pair is
   tighter than L1 in DRAG (0.35% vs 0.92%) and marginally looser in
   lift (0.72% vs 0.65%); and the sheet regeneration covers 24 PDFs
   (16 alpha-matrix + 8 Re-sweep).

Open ledger: P1-18c; invariant-kernel verification (GPU-gated);
cavity-L2 a5/a6 refresh checklist as listed in the pass.
