# Response to pass 42 (2026-07-26-1925.md)

All four majors and the actionable minors are implemented.

## Majors

1. **Scope corrected, twice.** The sentence now reads "the matrix covers
   the measured conditions spanning Stock's three regimes" with the
   per-condition status stated inline (low-Re ladder computed on the
   full ladder; anchors and workshop pair in progress) and the DFVLR
   record's remaining incidences (15-29.5 deg at 3-8.5e6) explicitly
   named as outside the matrix. "Every" is gone. The emitter now prints
   dash rows for staged conditions (the any_done drop removed), so the
   caption's "dashes = in progress" describes what the reader sees;
   rows also reordered to prose order (minor 4).
2. **alpha=0 claim replaced by the number**: "carries a residual lift
   C_L ~= 0.006 at L0, still decaying at the end of its run and under
   investigation" — matching the open RESPONSES flag rather than
   contradicting it. The L1/L2 solutions landing tonight will settle
   whether it is transient or a symmetry-breaking feature.
3. **Grid-convergence disclosure restored**: "several percent per
   level — up to 11% in C_L at alpha=10 on the finest step, not yet
   grid-converged".
4. **Deferral names the two owed low-Re comparisons** (alpha=5, Stock
   Fig. 14c; alpha=29.7, Fig. 16c — same laminar-to-separation regime,
   same machinery, awaiting digitization) alongside the 6.5/7.2e6
   conditions.

## Minors

1. Windward bound corrected to +-0.023 x/L.
2. Legend moved to the empty lower-left; the compare figure's contourf
   background replaced by labeled gray line contours (also the paper's
   line-contour convention — the user ordered the same for the maps);
   separation line now black on white.
3. JSON note wording fixed ("- - - - (short dashes)" per Stock's
   legend).
4. Folded into major 1 (emitter order = prose order).
5. Dead meshgrid lines in --from-npz removed.
6. **Probe rays now Reynolds-scaled** (shear and chi-ray inner heights
   x 1.5e6/Re_L): the overnight re65/re72 maps will probe at the
   correct y+ automatically. Good catch before those figures were cut.
7. The Cp block's dead nanmin line removed and the argmax caveat
   commented; the surface_map WIP (Cp extraction) and
   run_nlfneg_case.py (PYTHONPATH fix, which ran tonight's batch) are
   committed with this round. build_airfoil_case.py belongs to the
   OpenFOAM agent's working set — not mine to commit.
8. chi-panel dashing clause added to the fig:spheroidmaps caption.

## Notes

- Station-waterfall machinery (Stock Figs. 2-5 presentation) landed
  since your snapshot: regen_spheroid_station_profiles.py + Cp
  extraction in the prober; both L2 cases re-probing with Cp now.
  Measured-symbol digitization is task #37 (gate applies).
- Build clean, 108 pp.
