# Response to pass 41 (2026-07-26-1640.md)

Both majors and minors 1-2 are implemented; 3-4 answered.

## Majors

1. **Quantifiers corrected to the full-grid numbers.** The passage now
   reads "across the azimuth range the chi=1 and chi=c_v1 crossings sit
   within 0.05 L of each other (0.003-0.016 L at the measured azimuths),
   both between 0.90 and 0.99 x/L"; the pass-40 clause becomes "stays
   aft (0.92-0.99 x/L across the azimuth range)". Conclusion unchanged,
   as you note (0.05 max handover width << 0.13-0.56 amplification gap).
2. **Causal clause replaced by the three live hypotheses.** The sentence
   now states the diagnostic-backed fact (chi=1 arrives 0.13-0.56 L aft
   of the hot-film line) and lists, undecided: the lifted layer escaping
   the wall-anchored kernel (with the 0.04 L probe-reach caveat you
   found), the onset gate's in-bubble arm suppressing the wall-damped
   separated layer (cross-ref to the Daedalus passage that credits it),
   and genuinely subcritical attached-layer amplification at this Re
   (Stock's laminar-to-separation reading). The flank rate/gate probe is
   tied to the workshop-condition step.

## Minors

1. Caption updated: "the model-native chi=c_v1 front (solid cyan) with
   the chi=1 crossing (dotted cyan) hugging it --- the fast handover".
2. Script hygiene: run-hint path fixed, front-definitions block updated
   for the chi=1 front, dead f_cf line removed; still runs.
3. The .npz inputs are regenerable by the committed surface_map.py from
   the case volumes (which are large and untracked by convention, like
   every other case field). Agreed it belongs in the repro-refactor
   batch: the plan is a committed compact per-level front-summary JSON
   (same pattern as the bubble/resweep station dumps) so the diagnostic
   reproduces from the repo alone.
4. Intentional pre-staging; the a29.7 section lands with tonight's L2
   (its L2 is at ~12k/20k as of this writing) and the workshop-condition
   promise stays until that campaign step actually runs.

Compile clean after edits.
