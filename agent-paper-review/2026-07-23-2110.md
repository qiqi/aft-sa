# Review pass 11 of `paper/sa-ai.tex` — verifying the restored-mesh Secs. III–V refresh (98735ed)

*Reviewed 2026-07-23, ~21:10–21:40. Scope: commit **98735ed** ("Secs III–V
refresh complete on restored-mesh campaign"), the last big gated item of the
review thread — the archived-cavity-mesh restore and 36-case re-run promised
in the 1815 response, closing pass-9 majors 1–2, 4–5, 7. Working copy clean;
`sa-ai.pdf`/`sa-ai.log` (21:03) are current with the commit, 0 errors, 0
undefined refs. Line numbers refer to the committed HEAD file. Verification:
`regen_epp_reattach.py` and `regen_resweep_table.py` re-run in full; signed
$C_f$ zero-crossings recomputed via `regen_eppler_v2.airfoil_walk_contour`
on all 24 sweep cases and the 8 L2 benchmark cases; force-history flatness
recomputed from every sweep case's `total_forces_v2.csv`; bucket-tracking
percentages recomputed at matched $c_l$ against the committed digitized LTPT
polar (`EXP_POLAR`); mesh node/element counts read from the `mesh.cgns` zone
headers in the restored case dirs; cross-family/L1→L2 spreads recomputed
from the four committed campaign JSONs; `ai_constants.log` compared
(timestamp-stripped) across restored-cavity, structured, sweep, and NLF
cases. No paper files were edited; nothing needed restoring.*

## Summary

**The restored-mesh refresh is real, essentially complete, and verifies
almost everywhere.** The archived meshes are back — the `mesh.cgns` zone
headers in the case dirs match the printed Table 4/7 element counts to the
element (NLF cavity 29,855/105,481/385,665; Eppler cavity
29,757/105,335/384,141) — and every constant log across the restored-cavity,
structured, sweep, and NLF cases is identical, so l. 1377's "every
coupled-RANS computation of Secs. III–V" is now an honest sentence. Both
Eppler tables regenerate from the committed data to the last digit; the
polar captions, reattachment prose, sweep narrative, recovery stations,
NLF spread claims, and the Conclusion recap all now quote the restored-mesh
numbers, and the changed Re=10⁵ physics is owned in text and caption
exactly as pass 9 demanded (recovery incomplete on every grid, crossings
measured at 0.962–0.999c, drag +7% cavL1 to +44/+87% at L2). The
published-quality cross-family agreement did come back with the meshes:
NLF L2 ΔC_d = 0.42/0.36/1.08/1.12×10⁻³, Eppler L2 0.13–0.71×10⁻³, and the
cavity lower surface is laminar to the TE again on all 12 cases, so the
pass-6/7/9 "laminar to the trailing edge" flag finally dissolves on data
rather than on wording.

Two new problems, one of substance: the rewritten sweep steadiness sentence
("twenty-three of the 24 solutions are steady … flat to <5×10⁻⁵") is
contradicted by the committed force histories — at Re=10⁵ *four* cases,
not one, exceed the stated bound (major 1) — and the two Re-sweep suite
figures are the only two figures of the paper not in git at any revision
(major 2). Pass-10's two majors (destruction "switched off", "nearly three
times Blasius's") are untouched, as expected with no response yet.

## Refresh verification

Itemized against the task list; ✓ = verified against committed data/generators.

- ✓ **Meshes restored, counts match the printed tables.** Zone headers in
  the case dirs: Eppler cavity 30,438/106,218/385,428 nodes and
  29,757/105,335/384,141 elements (= Table 7, l. 1969–1970); NLF cavity
  30,536/106,364/386,952 nodes and 29,855/105,481/385,665 elements
  (= Table 4, l. 1635–1636; the 386,952 L2 node count is exactly the
  1815-response's restore check, 2×193,476). The rebuilt-era counts
  (27,905/96,715/350,095) survive only in an untracked scratch file —
  minor 7.
- ✓ **One canon everywhere.** `ai_constants.log`, timestamps stripped, is
  md5-identical across restored-cavity benchmark, structured benchmark,
  legacy-named L1 sweep, levels-suite sweep, and NLF cases (sphere kernel,
  a_max=0.19, ai_reOmA=124.6, k-anchor family). With the tables now printed
  from these runs, l. 1377 ("the same set is used … for every coupled-RANS
  computation of Secs. III–V") is now true — the pass-9 residue is closed.
- ✓ **`tab:eppxtr` reprints from the restored runs exactly.**
  `regen_epp_reattach.py` reproduces all 24 SA-AI entries digit-for-digit
  (strL2 0.788/0.737/0.655/0.449; the α=7° coarse-cavity pathology of the
  rebuilt meshes — cavL0 at 0.087c, cavL1 no reattachment — is gone: cavL0
  0.459, cavL1 0.435). The 0.40† mfoil column entry is XFOIL's per the
  footnote (the generator's raw mfoil 0.327 is flagged unreliable there,
  C_l=0.928 — footnote consistent).
- ✓ **`tab:eppresweep` reprints exactly.** `regen_resweep_table.py` output
  matches all 20 CFD entries including the strL1 10⁵ medians (0.818/0.0364)
  and the restored 3–4.6×10⁵ rows; the new caption's "L2 pairs agreeing to
  0.1–0.2×10⁻³ in c_d at 3–4.6×10⁵" measures 0.07/0.15×10⁻³ ✓, "diverging
  at 10⁵" measures 0.0103 ✓.
- ✓ **Re=10⁵ non-reattachment owned, and the numbers check.** Signed-C_f
  upward crossings on all six 10⁵ grids: 0.988/0.962/0.998 (cav L0/L1/L2),
  0.996/0.998/0.999 (str) — "regains zero only within 0.04c of the TE on
  every grid" holds (cavL1 at 0.962 is the binding case). Drag vs the
  0.0237 measurement: cavL1 +6.8% ✓ "+7%", cavL2 +43.7% / strL2 +87.3% ✓
  "+44–87% at L2"; refinement monotone toward the burst state in both
  families (cav +1.6→+6.8→+44%, str +52→+53→+87%) ✓. Text (l. 2189–2198),
  sweep-summary paragraph (l. 2227–2231), and the figure caption
  (l. 2287–2289) all tell the same story — the pass-9 "0.71c reattachment"
  contradiction is gone, and the C_p-plateau sentence now honestly splits
  3×10⁵ (full agreement) from 10⁵ (front half only).
- ✓ **Sweep text rewritten to the suite**: "reusing the grids … at all
  three refinement levels … 24 solutions in all" (l. 2106–2110); "All
  eight" is gone (P7-m3/P6-m11 closed); the table caption scopes itself to
  the L1 grids and points to the figures for L0/L2. Recovery stations
  re-read: quoted 0.56–0.57 at 3×10⁵ vs measured L1–L2 0.558–0.571 ✓;
  "≈0.50" at 4.6×10⁵ vs L1–L2 0.484–0.501 ✓ (L0 scope — minor 3).
  "C_l=0.77 unstructured vs 0.65 structured" at 6×10⁴ is back on the data
  (0.766/0.646) ✓. Family agreement at 3–4.6×10⁵ restated as ≤7×10⁻⁴ (L1)
  / 2×10⁻⁴ (L2): L2 ✓ exactly; L1 marginal (minor 4).
- ✗ **The steadiness sentence the rewrite introduced is false** — major 1.
- ✓ **Eppler polar caption ΔC_d** (l. 1934–1936): now "≲7×10⁻⁴
  (0.1–0.7×10⁻³ across the four incidences)"; measured L2 spreads
  0.13/0.21/0.71/0.17×10⁻³ ✓. The pass-6/9 "≲5×10⁻⁴ vs up to 2.1×10⁻³"
  discrepancy is resolved by the mesh restore, not wording.
- ✓ **Bucket-tracking sentence** (l. 1943–1949, the 1815 "already fixed"
  claim): at matched c_l against the committed digitized LTPT polar I get
  −0.8/+1.3/+11.0/−2.9% at α=0/2/5/7° ✓ "−1%, +1%, −3% … over-predicts by
  ~11% near 5°" (the old "~7%" is gone).
- ✓ **"Laminar to the trailing edge at every incidence"** (tab:eppxtr
  caption l. 2057–2058, body l. 2091): all 12 restored cavity cases have
  lower fronts at 0.9982–0.9989c ✓; the α=7° figure caption's "attached and
  laminar to x/c≈0.99" (l. 2016–2017) is the more precise statement.
- ✓ **Reattachment prose vs generator**: 0.79/0.74/0.66c vs measured
  0.74/0.67/0.59 ✓ (strL2 0.788/0.737/0.655); "0.05–0.07c too long" ✓;
  α=7° "0.45c, 0.03c ahead of 0.48" ✓ (0.449); "coarse grids scatter by
  ~0.14c before closing to 0.005c by L2" ✓ (L0 spread 0.135, L2 0.005).
  Bubble-extent captions re-checked from the L2 slices: sep/reattach
  0.51–0.79 (0°), 0.47–0.74 (2°), 0.39–0.66 (5°), 0.41–0.45 (7°) vs quoted
  0.52–0.78 / 0.47–0.73 / 0.40–0.64 / 0.41–0.45 ✓.
- ✓ **NLF cross-family drag claim (pass-9 major 4)**: quoted
  "≈0.4×10⁻³ through α=4°, rising to 1.1×10⁻³ at 9–15°" vs measured L2
  0.42/0.36/1.08/1.12×10⁻³ ✓ — the restore recovered the published-quality
  agreement exactly as the 1815 response predicted (the 1.9–7.7×10⁻³
  pass 9 measured was indeed the coarse-rebuild artifact).
- ✓ **Front-scatter claim (pass-9 major 5) restated and now true**: L2 pair
  ≤0.02c (max 0.019, α=9° upper) ✓; L1 pair ≤0.04c (max 0.039, α=9° upper)
  ✓; L0 "up to 0.1c at incidence" (max 0.099, α=4° upper) ✓; the rebuilt
  cavL1 α=0° lower-front outlier (0.356c) is gone (restored: 0.580 vs str
  0.552). "Lose the marginal high-α lower front" — attribution nit,
  minor 2.
- ✓ **Conclusion recap (pass-9 major 7) swept**: flat plate "±10%, 10% late
  at the lowest disturbance level, essentially exact at Tu=0.08%, 7–10%
  early as Tu rises" matches the pass-9-verified +9.8/+0.6/−6.6…−9.6%
  ladder including the sign flip ✓; reattachment 0.79/0.74/0.66 ✓;
  "0.005–0.021c on the Eppler reattachment at L2" ✓ (0.020/0.019/0.021/
  0.005); "agree to within 0.02c on every transition front" ✓ (L2 max:
  NLF 0.019, Eppler 0.011); "L1→L2 drift at or below 0.03c away from the
  marginal cases" ✓ (non-marginal max 0.018; marginal upper end — minor 6).
- ✓ **Table 5 (`tab:nlftrans`)** unchanged and still exact against the
  (unchanged) structured-L2 data; the restored-cavity entries enter only
  through the spread claims above, which verify.
- ✓ **NLF/Eppler turbulent-baseline captions** unaffected by the restore
  (baselines are structured-L2) and still match pass 9's extraction ✓.

## Status of prior comments

- **P9-M1** (Eppler tables/prose behind the refresh) — **resolved,
  verified**: both tables regenerate to the last digit; every flagged prose
  site swept.
- **P9-M2** (sweep narrative, 10⁵ physics) — **resolved, verified**, with
  one new defect introduced by the rewrite (new major 1).
- **P9-M3** (turbulent baselines) — resolved (verified pass 10).
- **P9-M4** (NLF cross-family drag claim) — **resolved, verified** on the
  restored data.
- **P9-M5** (0.03c scatter claim) — **resolved, verified** as restated
  (wording nit: new minor 2).
- **P9-M6** (XFOIL→FlexFoil) — resolved (verified pass 10).
- **P9-M7** (Conclusion recap) — **resolved, verified** (upper-end nit:
  new minor 6).
- **P9-M8** (H≤5 cap vs Fig. 4) — resolved (verified pass 10).
- **P9 l. 1362/1377** ("every coupled-RANS computation") — **now true**;
  constants logs identical across all case families.
- **P10-M1** (destruction "switched off" contradicts Sec. II.E's tie) —
  **open**, l. 888–890 unchanged.
- **P10-M2** ("a run nearly three times Blasius's", measured 1.76–2.0× in
  Re_θ) — **open**, l. 927–928 unchanged.
- **P10 minors 1–5** — **open** (spot-checked: `fig03_fs_transport_rows.py`
  docstring still says "favorable +0.30"; the l. 544–548 em-dash stack and
  l. 1837/2507 "collapses the inflectional content" duplicates unchanged;
  graze "~1.3 at β≥+0.25" unchanged).
- **P1-1/P1-2** (Daedalus predecessor-kernel disclosure + onset-constant
  difference; 0.02c cross-ref) — open, queued per the 1815 response.
- **P1-7** (RP² evenness argument) — open.
- **P1-17 remainder** ("within ~30%" vs "0.9–1.9×"; tie back-reaction at
  the low-branch anchor) — open.
- **P1-18a** (literature table), **P1-18c** (single flat-plate grid) — open.
- **P1-m13** (Somers Fig. 11d source-PDF check) — open.
- **P1-m17** (bare `(\eqref{...})`, l. 574, 1725) — open.

## New major comments

1. **"Twenty-three of the 24 solutions are steady---force coefficients flat
   to ΔC_l, ΔC_d < 5×10⁻⁵ over the final 2000 iterations" is contradicted
   by the committed force histories.** l. 2115–2120. Recomputed from every
   sweep case's `total_forces_v2.csv` over the final 2000 pseudo-steps
   (the reading most favorable to the claim; the histories log every 10
   steps): **four** cases exceed the bound, all at Re=10⁵ — strL1
   ΔC_l=1.04×10⁻² (the disclosed limit cycle, and its "ΔC≈0.01" ✓), but
   also **cavL1 ΔC_l=3.2×10⁻⁴** (6× over), cavL2 7.6×10⁻⁵, and strL2
   6.5×10⁻⁵. Over the final 2000 *logged rows* it is worse (seven cases,
   adding the three finest 6×10⁴ grids at 0.6–5×10⁻⁴). So 20/24 meet the
   printed criterion, not 23/24. The honest — and physically better —
   statement is one line: every solution away from 10⁵ is flat to <5×10⁻⁵;
   at 10⁵, the near-bursting condition, all grids but L0 carry residual
   unsteadiness, from ΔC~10⁻⁴ ripples to strL1's ΔC≈0.01 cycle. That
   version *supports* the bursting-boundary story the section now tells,
   instead of contradicting the data.

2. **The two Re-sweep suite figures are the only figures of the paper not
   in git.** `paper/figs/eppler_resweep_lowRe.pdf` and
   `eppler_resweep_highRe.pdf` (regenerated 21:01–21:02 from the restored
   runs, referenced at l. 2249/2276) are blocked by `paper/.gitignore`'s
   `figs/` rule and have **never been committed at any revision** — the 170
   other figures were all force-added; 98735ed's 38-figure refresh skips
   exactly these two. Consequences: a clean checkout of any commit,
   including this one, fails to build the PDF, and the committed record of
   the "refresh complete" milestone is missing precisely the suite figures
   the rewritten sweep section is about (the thread has been burned here
   before — pass 7's md5-identical placeholders). Fix: `git add -f` both,
   ideally in the same commit that fixes major 1.

## New minor comments

1. Sec. V body l. 2084–2085: reattachment "the two mesh families agree to
   within 0.02c at L2" — measured L2 spreads are 0.020/0.019/**0.021**/
   0.005c, and the Conclusion (l. 2549) correctly prints "0.005–0.021c".
   Same sentence, next clause: the e⁹ reference sits 0.021c from the data
   at α=2° (0.691 vs 0.67), against "within 0.02c". Round both up or say
   "≈0.02c".
2. l. 1681–1683 "the coarsest grids scatter by up to 0.1c at incidence and
   lose the marginal high-α lower front altogether": it is only the
   **structured** L0 that loses it, at *both* 9° and 15° (lower fronts at
   0.290/0.365 vs its own L2's 0.633/0.662); the cavity L0 holds 0.62–0.63
   there. Attribute the failure ("the structured L0 loses…", plural
   fronts) so the sentence doesn't indict the cavity family the passage is
   defending.
3. Sweep summary l. 2230–2231 and Fig. caption l. 2279–2280: the recovery
   stations "0.56–0.57 (3×10⁵)" and "≈0.50 (4.6×10⁵)" are the L1–L2
   readings; the L0 grids recover at 0.502–0.535 at 3×10⁵ and never cross
   zero at 4.6×10⁵ (C_f minimum exactly 0.0000). One qualifier ("on the
   two finer levels"; the existing "only grazes zero" already covers the
   460k L0) makes the figures and the quoted band agree at every line
   weight.
4. l. 2214–2216 "ΔC_d ≤ 7×10⁻⁴ (L1) … at 3–4.6×10⁵": the underlying
   medians at 3×10⁵ give 7.6×10⁻⁴ (0.01244 vs 0.01168); the claim holds
   only in the table's 4-decimal rounding (0.0124−0.0117). "≤8×10⁻⁴", or
   tie the sentence explicitly to the tabulated values.
5. The NLF cavity-L1 α=15° case hit the batch limit without meeting the
   convergence criterion (`converged: false` in the committed JSON; its
   lower front wanders 0.649–0.670 over the final batches — stable in the
   commit message's sense, but formally unconverged). The L1-pair 0.04c
   claim (l. 1681) and the polar figure use it silently. One footnote —
   as the Eppler sweep now does for its limit-cycle case — closes the
   disclosure gap.
6. Conclusion l. 2545–2547 "the marginal cases … reach 0.03–0.04c": the
   largest L1→L2 *transition-front* drift in the committed data is 0.031c
   (NLF cavity 9° upper); 0.04c is reached only if the Eppler α=7°
   *reattachment* drift (0.035c) is counted. Say "≈0.03c", or name the
   reattachment explicitly.
7. Record-keeping: the untracked scratch file
   `flow360_fr/eppler_cavity_proper_ncells.json` (10:09 today) still
   carries the rebuilt-mesh counts (27,905/96,715/350,095) that contradict
   the restored meshes now in the case dirs — delete or regenerate before
   it misleads a future provenance check. Similarly, the commit message's
   "3–4.6e5 rows land back on the printed values to the last digit" is off
   by one last digit at 3×10⁵ strL1 c_d (old print 0.0123, restored
   0.0124) — noted so the message isn't treated as provenance.
8. Typo scan of the refresh edit sites (l. 1670–1684, 1930–1951,
   2046–2135, 2186–2235, 2262–2290, 2515–2551): clean — no doubled words,
   no bare refs, no British spellings; the only wording items are minors
   1–3 and 6 above.
