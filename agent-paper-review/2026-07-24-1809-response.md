# Response to review pass 14 (2026-07-24-1809)

Thanks for the full verification of the pass-13 round — no disputes.

## New minor 1 (results JSONs only on disk)
FIXED in commit 2d4c7cd: all 18 top-level `flow360_fr/*.json` results
files (fork, fork-extension, ladder str/cav, bistability + archives,
mesh inventories) and all 98 per-case `ai_constants.log` provenance
echoes are now force-added (~0.5 MB total). The multi-GB case dirs
(meshes, restart dumps, force CSVs) stay ignored. The
`total_forces_v2.csv` histories remain untracked per the review's own
scoping ("the small results JSONs (and ai_constants.log files)"); if a
future pass wants the table-critical force histories in git too, say
so and we'll add the ~15 relevant CSVs.

## Status notes for the next pass
- **Daedalus (P1-1/P1-2)**: the section was recompiled onto the
  final-kernel campaign in commit 5509ab0 — Sec. daedalus and
  Appendix D now carry only completed canon runs (both families L1 at
  all three incidences; structured L2 at 4/5 deg), with pending runs
  excluded from every figure/table and the generators
  (`paper/regen_daedalus_{polar_sectional,surface_maps}.py`)
  availability-filtered so the unstructured-L2 rows and the alpha=6
  surface figure fill in automatically when the campaign finishes.
  Headline: at eta=0.31, alpha=4 the recomputed bubble spans
  0.489-0.661c vs the strips' 0.472-0.660c (predecessor: 0.04-0.06c);
  drag returned +6-14 counts toward AVL+XFOIL. Front/bubble numbers
  come from `daedalus/canon_front_metrics.py` (committed). This is
  fresh, substantial tex surface — a prime target for pass 15.
- **fv1-bypass exploration** (flagged as untracked): still
  exploratory, deliberately outside the paper. First battery results:
  at 1e5 the bypass closes the cold-start bubble (CL~0.91 vs 0.77
  burst) but as a large limit cycle, and the 2e5 guard case is not
  inert (CL std 0.016) — the buffer-layer guard needs tightening
  before any of it can touch the tex. `run_fv1bypass_test.py` will be
  committed with its outcome docstring once the design settles.
- **P1-17 (tie back-reaction re-march)** and **P1-18c (flat-plate
  refined grid)**: still open, unchanged.
