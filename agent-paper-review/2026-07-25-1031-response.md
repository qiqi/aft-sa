# Response to review pass 23 (2026-07-25-1031.md)

Both majors and minors 1–5 addressed. The Tarsia catch (major 1) is the
kind that saves a paper from a one-line referee kill — an absence claim
contradicted by a PDF in our own references directory. Thank you.

## Major 1 — SA-BCM absence claim: SCOPED + CITED
The fig:nlfaft caption now reads: "No published SA-BCM incidence
*sweep* exists at this condition; the one open SA-BCM result is a
single-incidence (α=0°) mesh-adaptation study [tarsia_2025]." New bib
entry `tarsia_2025` (Tarsia Morisco & Alauzet, AIAA SciTech 2025).
Placing their α=0° point on the figure is deliberately NOT done in
this pass — their study varies mesh adaptation at fixed incidence and
picking "the" transition location from it needs a careful read; noted
as an option.

## Major 2 — stale ~45% + half-stale read-off caveat: FIXED
The sentence now splits the provenance honestly: bubble stations and
lift coefficients remain figure-read (one-significant-figure caveat
retained, scoped to them); the polar drags are matched-c_l
interpolations of the vector-exact digitized curves, and the number is
now "$41\%$" (0.0195/0.0138, the reviewer's own arithmetic). The
"one-significant-figure" line-break hyphen (minor 4) dissolved in the
rewrite.

## Minors
1. JSON `condition` field corrected: "gamma-Re_theta in OpenFOAM,
   SA-BC in SU2" (Fluent removed).
2. Legend "(Denison et al.)" → "(Denison)" — single author, matching
   the caption.
3. Both matplotlib rendering defects fixed everywhere they occur: the
   `--` literal is now an en-dash character in all three touched
   scripts (nlf_aft, eppler polar, eppler bubble), and the `\t`-in-
   non-raw-string bug ("L0oL2 by size") fixed with escaped `\\to`.
   All affected figures regenerated.
4. Folded into major 2's rewrite.
5. Workshop-condition disclosure added to the caption: "(the workshop
   condition is M=0.2, Tu=0.15% — immaterial on transition-vs-c_l
   axes)".
6. Noted for the record; no action.

## Status notes for the reviewer's "still open" ledger
- The invariant-kernel matched-protocol redo CONCLUDED since the pass:
  controls reproduce canon exactly (str 0.9796/0.00741 vs 0.9797/
  0.00740; cav 0.9839/0.00771 vs 0.9843/0.00776) and the flag-on runs
  converge +12.4/+22.3/+10.8 counts away on str-NLF/cav-NLF/Eppler.
  The invariant form is NOT cleared; the spheroid campaign will run
  the standard magnitude kernel. Details in RESPONSES.md ~12:00.
- The negative-incidence NLF pair (α=−4, −8; both families, L2, 40k)
  landed and is now on fig:nlfaft's finest-grid markers; caption
  updated.
- fig:eppbubble gained an XFOIL e^9 branch for α≥6.5 (where mfoil is
  past its convergence edge) per the author's directive; the Daedalus
  section sheets moved to η = 0.10/0.75/0.92 per the author's
  directive (0.31/0.60 were too similar to 0.10).
- The a5-recovery/a6 checklist is unchanged — the Daedalus campaign is
  PAUSED by the author for the day (GPUs freed for quick-feedback
  work); the checklist executes when the campaign resumes.

## Verification
Figures regenerated from committed scripts; full rebuild after the
batch: 0 errors, 0 undefined refs (bibtex rerun for tarsia_2025);
per-edit OK printed on every tex replace.
