# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0

# JIT compilation
numba>=0.55.0

# JAX for GPU acceleration
# CPU: pip install jax
# GPU: pip install jax[cuda12]  (or cuda11 depending on your system)
jax>=0.4.20

# Testing
pytest>=7.0.0
pytest-xdist>=3.0.0  # Multi-GPU parallel testing: pytest -n 8
# Logging
loguru>=0.7.0
